#!/usr/bin/env python3
"""
capture_screenshot.py — headless screenshot helper for the Endless Staircase game.

Used by the Simon Lore Bot routine to grab a fresh screenshot whenever it ships
a visible game change, so the GitHub changelog / wiki can show what's new.

Usage:
    python3 capture_screenshot.py OUT.png [scene]

Scenes:
    title  (default) — the title screen (always renders the latest lore blurb)
    play             — a few seconds of auto-play (stairs + Simon + hazards)
    nicedoor         — BEAT 516, NICE DOOR (Toby, Aug 28, 3:46 PM)
    portalnetwork    — BEAT 517, THE PORTAL NETWORK (Toby, Aug 28, 3:48 PM)
    activatekaru     — BEAT 412, /ACTIVATE KARU (Toby, Aug 21, 6:30 PM)
    nomnom           — BEAT 413, NOM NOM (Toby, Aug 21, 6:30 PM)
    neededsleep      — BEAT 414, NEEDED SLEEP (Toby, Aug 22, 8:01 AM)
    moremilk         — BEAT 415, MORE MILK (Toby, Aug 22, 8:04 AM)
    milkboxes        — BEAT 416, MILK BOXES (Toby, Aug 22, 8:07 AM)
    fearedmore       — BEAT 421, FEARED MORE (Toby, Aug 22, 5:19 PM)
    wipesystems      — BEAT 422, WIPE THE SYSTEMS (Toby, Aug 22, 5:26 PM)
    twentyyears      — BEAT 423, TWENTY YEARS (Toby, Aug 22, 5:29 PM)
    entity000a       — BEAT 424, ENTITY 000A (Toby, Aug 22, 7:18 PM)
    secondexperiment — BEAT 425, SECOND EXPERIMENT (Toby, Aug 22, 7:25 PM)
    triounderpero    — BEAT 426, TRIO UNDER PERO (Toby, Aug 22, 7:56 PM)
    letmeout         — BEAT 427, LET ME OUT (Toby, Aug 22, 8:10 PM)
    knowsbeforetype  — BEAT 428, HE KNOWS BEFORE YOU TYPE (Toby, Aug 23, 7:28 AM)
    swordofeverything — BEAT 429, THE SWORD OF EVERYTHING (Toby, Aug 23, 7:29 AM)
    everythingset    — BEAT 430, THE EVERYTHING SET (Toby, Aug 23, 7:46 AM)
    fourthstrongest  — BEAT 431, THE FOURTH STRONGEST (Toby, Aug 23, 9:05 AM)
    safefile         — BEAT 432, THE SAFE FILE (Toby, Aug 23, 2:36 PM + 2:40 PM)
    wontcomeback     — BEAT 433, WON'T COME BACK (Toby, Aug 23, 3:02 PM + 3:03 PM)
    portalphase1     — BEAT 434, PORTAL PHASE 1 (Toby, Aug 23, 3:17 PM)
    mergeform        — BEAT 435, /Entity000_mergeform (Toby, Aug 23, 3:40 PM)
    entity000d       — BEAT 436, ENTITY 000D (Toby, Aug 23, 3:46 PM)
    codebeings       — BEAT 437, NORMAL CODE BEINGS (Toby, Aug 23, 4:05 PM + 4:07 PM)
    poptart          — BEAT 438, POP TART OF THE UNIVERSE (Toby, Aug 24, 6:38 AM)
    bookhappened     — BEAT 439, THE BOOK HAPPENED (Toby, Aug 24, 6:41 AM)
    rewritebattle    — BEAT 440, THE REWRITE BATTLE (Toby, Aug 24, 6:45 AM)
    personalfire     — BEAT 441, THE PERSONAL FIRE (Toby, Aug 24, 10:17 AM)
    endoflayers      — BEAT 442, THE END OF LAYERS (Toby, Aug 24, 12:03 PM)
    poptartboxes     — BEAT 443, POP TART BOXES (Toby, Aug 24, 12:11 PM)
    thetoy           — BEAT 444, THE TOY (Toby, Aug 24, 12:23 PM)
    onlygodabove     — BEAT 445, ONLY GOD ABOVE HIM (Toby, Aug 24, 2:43 PM)
    nahidwin         — BEAT 446, NAH, I'D WIN (Toby, Aug 24, 2:45 PM)
    finalbosspero    — BEAT 447, THE FINAL BOSS PERO (Toby, Aug 24, 3:34 PM)
    lastrunpero      — BEAT 448, LAST RUN PERO (Toby, Aug 24, 3:45 PM)
    phasequestion    — BEAT 449, PHASE ?????????? (Toby, Aug 24, 3:49 PM)
    sentielpero      — BEAT 453, SENTIEL PERO (Toby, Aug 24, 7:54 PM)
    limitless        — BEAT 463, LIMITLESS (Toby, Aug 25, 1:05 PM)
    easilybeatyou    — BEAT 464, I COULD EASILY BEAT YOU NOW (Toby, Aug 25, 1:16 PM)
    stuckdragon      — BEAT 467, STUCK ON HIS DRAGON FORM (Toby, Aug 25, 2:32 PM)
    experimentended  — BEAT 468, EXPERIMENT 666 HAS ENDED (Toby, Aug 25, 4:14 PM)
    variestoforms    — BEAT 469, VARIES TO FORMS (Toby, Aug 25, 4:17 PM)
    okaypero         — BEAT 470, WOAH, OKAY PERO (Toby, Aug 25, 4:31 PM)
    onethousandone   — BEAT 471, 1001% (Toby, Aug 25, 4:59 PM)
    vanillacat       — BEAT 472, HIS VANILLA CAT THING (Toby, Aug 25, 5:00 PM)
    twomirrors       — BEAT 473, THE TWO MIRRORS (Toby, Aug 25, 5:13 PM)
    someoneleaves    — BEAT 474, IF SOMEONE LEAVES (Toby, Aug 25, 7:16 PM)
    imeveryone       — BEAT 476, NOT EXACTLY, I'M EVERYONE (Toby, Aug 26, 8:20 AM)
    exceptforme      — BEAT 477, EXCEPT FOR ME (Toby, Aug 26, 8:47 AM)
    takethegame      — BEAT 478, TAKE THE GAME FOR HIMSELF (Toby, Aug 26, 10:22 AM)
    respawnsinlg     — BEAT 479, RESPAWNS IN LUIGI GREEN (Toby, Aug 26, 10:25 AM)
    pvpgone          — BEAT 480, PVP WON'T EXIST ANYMORE (Toby, Aug 26, 10:43 AM)
    acceptmymercy    — BEAT 481, ACCEPT MY MERCY (Toby, Aug 26, 1:10 PM)
    basicvillian     — BEAT 482, THE BASIC VILLIAN (Toby, Aug 26, 1:34 PM)
    nothumanoid      — BEAT 483, I AM NOT LOSING TO A HUMANOID (Toby, Aug 26, 3:34 PM)
    powerfulform     — BEAT 484, EVEN IN YOUR MOST POWERFUL FORM (Toby, Aug 27, 8:42 AM)
    japanesedomain   — BEAT 485, JAPANESE DOMAIN (Toby, Aug 27, 8:59 AM)
    ringmasteroutfit — BEAT 486, THE RINGMASTER OUTFIT (Toby, Aug 27, 9:19 AM)
    str0ng3r         — BEAT 487, STR0NG3R (Toby, Aug 27, 9:47 AM)
    firsttofall      — BEAT 488, THE 1ST ONE TO FALL (Toby, Aug 27, 10:20 AM)
    thefall          — BEAT 489, I DON'T WANNA TALK ABOUT IT (Toby, Aug 27, 10:21 AM)
    theoneasked      — BEAT 490, THE ONE WHO ASKED (Toby, Aug 27, 10:24 AM)
    ability10        — BEAT 491, ABILITY 10 (Toby, Aug 27, 11:30 AM)
    breaksconn       — BEAT 492, BR3AKS YOUR GAME CONN3CTI0N (Toby, Aug 27, 11:41 AM)
    relatedgames     — BEAT 493, ALL CLASSICS RELATED GAMES (Toby, Aug 27, 11:42 AM)
    fanoncanon       — BEAT 494, FANON + CANON (Toby, Aug 27, 11:47 AM)
    classicsadmin    — BEAT 495, CLASSICS ADMIN (Toby, Aug 27, 2:45 PM)
    gameofclassics   — BEAT 496, THE GAME OF CLASSICS (Toby, Aug 27, 2:50 PM)
    promptthing      — BEAT 497, THE PROMPT THING (Toby, Aug 27, 2:51 PM)
    readyforcamera   — BEAT 498, READY FOR THE CAMERA (Toby, Aug 27, 3:07 PM)
    contentlayer     — BEAT 499, THE CONTENT LAYER (Toby, Aug 27, 3:10 PM)
    learnedname      — BEAT 500, FINALLY, YOU LEARNED YOUR NAME (Toby, Aug 27, 3:11 PM)
    notranslator     — BEAT 501, NO TRANSLATOR (Toby, Aug 27, 3:34 PM)
    nightmaremode    — BEAT 502, NIGHTMARE MODE (Toby, Aug 27, 7:08 PM)
    mightnotbeable   — BEAT 503, MIGHT NOT BE ABLE TO MAKE AN IMAGE (Toby, Aug 27, 7:19 PM)
    thespot          — BEAT 504, THE SPOT (Toby, Aug 28, 6:45 AM)
    allerrors        — BEAT 505, ALL THE ERRORS IN THE GAME (Toby, Aug 28, 6:45 AM)
    attack10         — BEAT 506, ATTACK 10 (Toby, Aug 28, 6:50 AM)
    whateverhewants  — BEAT 475, HE COULD DO WHATEVER HE WANTS (Toby, Aug 25, 7:45 PM)
    cure             — the False Cure scene (two bottles + taunt + banners)
    residual         — THE RESIDUAL WAR: the Sound Spine + half-formed remnants
    perosrealm / weakerexp / sendingthis / containedshadow / watcher
                     — BEATS 210-214, the Gaster containment arc (Toby, July 29 evening)
    nomoreendings    — BEAT 215, THERE ARE NO MORE ENDINGS (Toby, July 30 morning)
    powrinksans      — BEAT 216, POW3R INK SANS / THE PERO SANS (Toby, July 30, 9:26 AM)
    gameendtrue      — BEAT 217, /GAME_END = TRUE (Toby, July 30, 11:20 AM)
    gasterhero       — BEAT 218, GASTER ON THE HERO SIDE (Toby, July 30, 11:26 AM)
    fatalerror       — BEAT 219, FATAL ERROR (Toby, July 30, 4:06 PM)
    butterflyform    — BEAT 220, THE BUTTERFLY FORM (Toby, July 30, 4:49 PM)
    butterflyinksans — BEAT 221, BUTTERFLY CLASSICS INK SANS (Toby, July 30, 5:47 PM)
    paintederrorwar  — BEAT 222, THE PAINTED ERROR WAR (Toby, July 31, 6:58 AM)
    monsterosity     — BEAT 223, SIMON 404 · THE MONSTEROSITY INDEX (Toby, Aug 1, 7:48 AM)
    mainchartrio     — BEAT 224, THE ONLY THREE MAIN CHARACTERS (Toby, Aug 1, 7:54 AM)
    classics2        — BEAT 225, CLASSICS 1 SEALED · CLASSICS 2 BUILT (Toby, Aug 1, 4:54 PM)
    graycyber        — BEAT 226, GRAY IN THE CYBER SUIT (Toby, Aug 1, 4:58 PM)
    hiddenportal     — BEAT 227, THE HIDDEN PORTAL (Toby, Aug 1, 6:41 PM)
    whitevoid        — BEAT 228, THE WHITE VOID (Toby, Aug 2, 6:19 AM)
    quadsanses       — BEAT 229, THE QUAD SANSES (Toby, Aug 2, 8:07 AM)
    perosrealm       — BEAT 230, PERO’S REALM (Toby, Aug 2, 8:25 AM)
    tinkerbox        — BEAT 231, TINKERBOX_CLASSICS (Toby, Aug 2, 8:34 AM)
    scramble         — BEAT 232, IT IS NOT ORGANIZED (Toby, Aug 2, 5:19 PM)
    mustitu          — BEAT 233, MUSTITU VARIANTS · THE GODLY FLOAT (Toby, Aug 2, 7:09 PM)
    mustitublue      — BEAT 234, MUSTITU BLUE PERO (Toby, Aug 2, 9:12 PM)
    cutscene         — BEAT 235, THE MUSTITU BLUE CUTSCENE (Toby, Aug 3, 9:21 AM)
    boxfactory       — BEAT 236, THE BOX FACTORY (Toby, Aug 3, 11:57 AM)
    energymath       — BEAT 237, PERO'S ENERGY MATH (Toby, Aug 3, 2:41 PM)
    aurainbow        — BEAT 238, THE AU RAINBOW (Toby, Aug 3, 3:53 PM)
    rainbowvoid      — BEAT 239, THE RAINBOW VOID (Toby, Aug 3, 4:36 PM)
    curseera         — BEAT 240, THE CURSE ERA (Toby, Aug 3, 8:16 PM)
    overload         — BEAT 241, THE OVERLOAD (Toby, Aug 4, 11:51 AM)
    iamsupereme      — BEAT 265, I AM SUPEREME (Toby, Aug 6, 6:48 PM)
    omegaflower      — BEAT 266, OMEGA FLOWER (Toby, Aug 6, 7:04 PM)
    thehat           — BEAT 267, THE HAT (Toby, Aug 6, 7:12 PM)
    alwaysmine       — BEAT 268, ALWAYS MINE (Toby, Aug 6, 7:21 PM)
    thecost          — BEAT 269, THE COST (Toby, Aug 6, 8:29 PM)
    resetrecovery    — BEAT 270, RESET RECOVERY (Toby, Aug 7, 6:21 PM)
    nothingends      — BEAT 271, NOTHING ENDS (Toby, Aug 7, 6:23 PM)
    simonpsc         — BEAT 272, SIMON.psc (Toby, Aug 7, 6:35 PM)
    tinkin           — BEAT 273, TINKIN'.psc (Toby, Aug 7, 7:07 PM)
    savegrave        — BEAT 274, THE SAVE GRAVE (Toby, Aug 8, 8:16 AM)
    unikitty         — BEAT 275, THE CLASSICS UNIKITTY (Toby, Aug 8, 12:53 PM)
    undefined        — BEAT 276, UNDEFINED (Toby, Aug 8, 1:15 PM)
    mostpowerful     — BEAT 280, THE MOST POWERFUL BEING (Toby, Aug 8, 4:21 PM)
    notanera         — BEAT 281, IT’S NOT AN ERA (Toby, Aug 8, 4:22 PM)
    tinker           — BEAT 282, TINKER MEANS TO PLAY (Toby, Aug 8, 7:09 PM)
    villainstatus    — BEAT 283, 2.54 OUT OF 5 (Toby, Aug 8, 7:17 PM)
    onlycounter      — BEAT 284, THE BEST AND ONLY COUNTER (Toby, Aug 8, 7:20 PM)
    catsleep         — BEAT 285, A CAT MAKES ANOTHER CAT SLEEP (Toby, Aug 8, 7:28 PM)
    whowins          — BEAT 286, WHO WINS THE BATTLES (Toby, Aug 8, 7:41 PM)
    unlike           — BEAT 287, UNLIKE EVERYONE ELSE (Toby, Aug 9, 1:09 PM)
    absolute         — BEAT 291, ??? = ABSOLUTE (Toby, Aug 12, 9:41 AM)
    realvillain      — BEAT 292, THE REAL VILLAIN (Toby, Aug 12, 12:00 PM)
    fakeerase        — BEAT 293, FAKE ERASE (Toby, Aug 12, 12:06 PM)
    calling          — BEAT 294, 404 AND 12 12 12 (Toby, Aug 12, 2:21 PM)
    notasking        — BEAT 295, I WASN'T ASKING (Toby, Aug 12, 6:43 PM)
    newduo           — BEAT 296, THE NEW DUO (Toby, Aug 13, 6:33 AM)
    vrmirror         — BEAT 297, THE CLASSICS VR MIRROR (Toby, Aug 13, 6:50 AM)
    hollowobsidian   — BEAT 298, HOLLOW OBSIDIAN (Toby, Aug 13, 11:14 AM)
    truepower        — BEAT 299, TRUE POWER (Toby, Aug 13, 1:30 PM)
    universe         — BEAT 300, CREATED BY THE UNIVERSE (Toby, Aug 13, 1:37 PM)
    onlyhop          — BEAT 301, YOU ARE OUR ONLY HOP- (Toby, Aug 13, 1:45 PM)
    imaginarymass    — BEAT 302, THE IMAGINARY MASS (Toby, Aug 13, 1:58 PM)
    brothercorrect   — BEAT 303, BROTHER, YOU ARE CORRECT (Toby, Aug 13, 2:09 PM)
    resetloop        — BEAT 304, RESET DOES NOT WORK ON HIM (Toby, Aug 13, 6:01 PM)
    explainanswer    — BEAT 305, EXPLAIN YOUR ANSWER (Toby, Aug 14, 7:43 AM)
    teacher          — BEAT 306, TEACHER AND GUARDIAN (Toby, Aug 14, 8:31 AM)
    nobattle         — BEAT 307, YOU DO NOT BATTLE PERO (Toby, Aug 14, 8:42 AM)
    godcorrupt       — BEAT 308, THE GOD OF CORRUPTION (Toby, Aug 14, 8:46 AM)
    alreadyglitchy   — BEAT 309, ALREADY GLITCHY (Toby, Aug 14, 9:05 AM)
    smallpuppet      — BEAT 310, A SMALL PUPPET (Toby, Aug 14, 9:09 AM)
    oldestfather     — BEAT 311, THE OLDEST FATHER (Toby, Aug 14, 9:36 AM)
    noidea           — BEAT 312, NO IDEA WHAT ELSE TO SAY (Toby, Aug 14, 10:59 AM)
    madehimself      — BEAT 313, HE MADE HIMSELF (Toby, Aug 14, 1:56 PM)
    halfofinfinity   — BEAT 314, HALF OF INFINITY (Toby, Aug 14, 3:28 PM)
    uaremine         — BEAT 315, U ARE MINE (Toby, Aug 14, 8:14 PM)
    peroquestions    — BEAT 316, PERO 20 QUESTONS (Toby, Aug 15, 7:38 AM)
    sixtimelines     — BEAT 317, 6 TIMELINES LONG (Toby, Aug 15, 8:03 AM)
    yourmine         — BEAT 318, YOUR MINE (Toby, Aug 15, 9:16 AM)
    seed303          — BEAT 319, SEED 303 (Toby, Aug 15, 9:48 AM)
    corruptiongod    — BEAT 320, CORRUPTION GOD (Toby, Aug 15, 9:50 AM)
    loreentered      — BEAT 321, THE LORE ENTERED AGAIN (Toby, Aug 15, 10:01 AM)
    withoutdefeating — BEAT 322, WITHOUT DEFEATING PERO (Toby, Aug 15, 10:04 AM)
    peroIs303        — BEAT 323, PERO IS 303 (Toby, Aug 15, 10:22 AM)
    peroFlash        — BEAT 324, PERO-FLASH STANDS HERE (Toby, Aug 15, 10:27 AM)
    rulingItOut      — BEAT 325, RULING IT OUT (Toby, Aug 15, 10:36 AM)
    whatHappensNext  — BEAT 326, WHAT HAPPENS NEXT (Toby, Aug 15, 10:37 AM)
    itStartedToLag   — BEAT 327, IT STARTED TO LAG (Toby, Aug 15, 10:46 AM)
    itWentBigger     — BEAT 328, IT WENT BIGGER (Toby, Aug 15, 10:47 AM)
    nothingBattlesPero — BEAT 329, NOTHING BATTLES PERO (Toby, Aug 15, 11:29 + 11:34 AM)
    chickenHen       — BEAT 330, THE END OF CHICKEN HEN'S LORE (Toby, Aug 15, 3:02 PM)
    perminentEnd     — BEAT 331, THE PERMINENT END OF UNIKITTY (Toby, Aug 15, 6:56 PM)
    normallyAgain    — BEAT 332, THE GAME THEN WENT NORMALLY AGAIN (Toby, Aug 15, 6:58 PM)
    codingNewForms   — BEAT 333, PERO IS OUT THERE CODING NEW FORMS (Toby, Aug 16, 6:47 AM)
    hyperspaceVortex — BEAT 334, HYPERSPACE VORTEX (Toby, Aug 16, 8:08 AM)
    allThreeOfUs     — BEAT 335, ALL THREE OF US (Toby, Aug 16, 12:47 PM)
    chatgptPlusClaude— BEAT 336, CHATGPT + CLAUDE (Toby, Aug 16, 12:54 PM)
    theHiddenBook    — BEAT 337, THE HIDDEN BOOK (Toby, Aug 16, 12:59 PM)
    separateEquation — BEAT 338, SEPARATE EQUATION (Toby, Aug 16, 5:57 PM)
    perfectMemmory   — BEAT 339, PERFECT MEMMORY (Toby, Aug 16, 6:08 PM)
    onlyHeSurvives   — BEAT 340, ONLY HE SURVIVES (Toby, Aug 16, 7:05 PM)
    mostVaried       — BEAT 341, MOST VARIED (Toby, Aug 16, 7:21 PM)
    makeHimSpareYou  — BEAT 342, MAKE HIM SPARE YOU (Toby, Aug 16, 7:23 PM)
    aTrioInTheBox    — BEAT 343, A TRIO IN THE BOX (Toby, Aug 17, 8:03 AM)
    andItHappened    — BEAT 344, AND IT HAPPENED (Toby, Aug 17, 9:52 AM)
    heMovedTheBoxes  — BEAT 345, HE MOVED THE BOXES (Toby, Aug 17, 10:29 AM)
    iDidAllTheCode   — BEAT 346, I DID ALL THE CODE (Toby, Aug 17, 3:36 PM)
    oneWithPero      — BEAT 347, ONE WITH PERO (Toby, Aug 17, 4:01 PM)
    endTheGameHere   — BEAT 348, END THE GAME HERE (Toby, Aug 17, 4:05 PM)
    normalMan        — BEAT 349, NORMAL MAN (Toby, Aug 17, 5:55 PM)
    iDoCoding        — BEAT 350, I DO CODING (Toby, Aug 17, 5:58 PM)
    beatsAnything    — BEAT 351, BEATS ANYTHING (Toby, Aug 17, 6:21 PM)
    survivedFellen   — BEAT 352, SURVIVED AND FELLEN (Toby, Aug 17, 6:23 PM)
    bestTestDummy    — BEAT 353, BEST TEST DUMMY (Toby, Aug 17, 7:40 PM)
    vicinusCicinus   — BEAT 354, VICINUS, CICINUS (Toby, Aug 18, 10:51 AM)
    durpleVsVicinus  — BEAT 355, DURPLE VS VICINUS (Toby, Aug 18, 10:55 AM)
    threeOnSimon     — BEAT 356, THREE ON SIMON (Toby, Aug 18, 10:56 AM)
    all23Names       — BEAT 357, ALL 23 NAMES (Toby, Aug 18, 11:02 AM)
    everyoneFromBox  — BEAT 358, EVERYONE FROM BOX (Toby, Aug 18, 11:03 AM)
    banishedAU       — BEAT 359, BANISHED AU (Toby, Aug 18, 11:05 AM)
    iAlone           — BEAT 360, I ALONE (Toby, Aug 18, 11:17 AM)
    blackSythe       — BEAT 361, BLACK SYTHE (Toby, Aug 18, 12:56 PM)
    robloxPero       — BEAT 362, ROBLOX PERO (Toby, Aug 18, 3:37 PM)
    corruptionGod    — BEAT 363, CORRUPTION GOD (Toby, Aug 18, 3:40 PM)
    allLoreBasically — BEAT 364, ALL LORE BASICALLY (Toby, Aug 18, 4:07 PM)
    changedTheCode   — BEAT 365, CHANGED THE CODE (Toby, Aug 18, 4:15 PM)
    infiniteHealthPlus — BEAT 366, INFINITE HEALTH+ (Toby, Aug 18, 4:16 PM)
    gettingMilk      — BEAT 367, GETTING MILK (Toby, Aug 18, 4:20 PM)
    beyondWords      — BEAT 368, BEYOND WORDS (Toby, Aug 18, 4:31 PM)
    ownEveryPixel    — BEAT 369, I OWN EVERY PIXEL (Toby, Aug 18, 5:54 PM)
    savingYou        — BEAT 370, SAVING YOU (Toby, Aug 18, 6:08 PM)
    grayVsPlayer     — BEAT 371, GRAY VS THE PLAYER (Toby, Aug 18, 6:14 PM)
    vicinusIsGray    — BEAT 372, VICINUS IS GRAY (Toby, Aug 19, 6:39 AM)
    soulsOfInk       — BEAT 373, SOULS OF INK (Toby, Aug 19, 6:45 AM)
    peroLovesGray    — BEAT 374, PERO LOVES GRAY (Toby, Aug 19, 7:17 AM)
    heCanSeeAgain    — BEAT 375, HE CAN SEE AGAIN (Toby, Aug 19, 7:23 AM)
    savingYou        — BEAT 370, SAVING YOU (Toby, Aug 18, 6:08 PM)
    grayVsPlayer     — BEAT 371, GRAY VS THE PLAYER (Toby, Aug 18, 6:14 PM)

It works by loading the single-file game in headless Chromium with a virtual
time budget, optionally injecting a tiny scene script that drives the game and
then freezes the loop so the final captured frame is stable. No external libs;
everything is vanilla. The injection is defensive (feature-detects functions),
so it degrades to a title shot if the game internals change.
"""
import sys, os, subprocess, tempfile

GAME = "/Users/tdeshane/endless-staircase/index.html"
CHROME_CANDIDATES = [
    "/opt/homebrew/bin/chromium",
    "/Applications/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing",
    "/Applications/Chromium.app/Contents/MacOS/Chromium",
    "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome",
]

# Each scene is JS injected just before </body>, AFTER the game's own script has
# run reset()/state='title'/requestAnimationFrame(frame). Top-level functions are
# global-object properties (even under "use strict"), so reassigning window.update
# freezes physics; top-level let/const (LEVERS, lever, bottles, player...) are
# readable by bare name from this later inline <script> in the same realm.
SCENES = {
    "play": """
      try {
        handleConfirm();
        // keep it short so Simon's rising tide hasn't caught the climber yet
        for (var i=0;i<70;i++){ keys['ArrowRight']=(i%30<15);
          if(i%18===0){ press['Space']=true; } update(1/60); for(var k in press) delete press[k]; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "cure": """
      try {
        handleConfirm();
        for (var i=0;i<80;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        window.update = function(){};
        if (typeof dropBottles==='function' && typeof LEVERS!=='undefined'){
          var d = LEVERS.filter(function(l){return l.type==='cure';})[0];
          if(d){ lever={state:'act',type:'cure',name:d.name,t:0.1,warnDur:d.warn,actDur:d.act};
            bottles=[]; dropBottles();
            for(var b=0;b<bottles.length;b++){ bottles[b].y=player.y-70; bottles[b].settled=true; } }
        }
        if (typeof showTaunt==='function'){ showTaunt('A CURE WAITS AT THE TOP.'); tauntT=6; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "restoration": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        window.update = function(){};
        if (typeof restore!=='undefined'){
          restore.active = true; restore.t = 3.0; restore.glow = 1; restore.done = false;
        }
        if (typeof showTaunt==='function'){ showTaunt('There are no villains. I was the corruption.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "peace": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // the Restoration has already happened; now force the Peaceful Ending on
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.active=true; peace.t=5.0; peace.glow=1; peace.done=false; }
        // let a few calm frames populate the Incredibox singers + notes, then freeze
        for (var j=0;j<140;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('A calm Incredibox. Sound Battles, the last trace.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "battle": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // Restoration already happened; force the Peaceful Ending + Sound Battle on
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.active=true; peace.t=5.0; peace.glow=1; peace.done=false; }
        // run enough calm frames to populate singers and run a few Sound Battle beats
        for (var j=0;j<180;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        // freeze on a frame where Simon has just led and the world is answering
        if (typeof battle!=='undefined'){
          battle.t=0.0; battle.beat++; battle.leadFlash=1.0;
          battle.answerActive=true; battle.answerWave=0.45;
          if (typeof platforms!=='undefined'){
            for (var b=0;b<platforms.length;b++){ if(platforms[b].boxer){ platforms[b].boxer.answer=0.9; } }
          }
        }
        if (typeof updatePeace==='function'){ /* one more tick to puff answer notes */ }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('Simon leads. The world answers. Simon is the fastest.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phase2": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // Restoration + Peaceful Ending already happened
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        // populate the calm Incredibox singers first
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        // now break it: force PHASE 2 — the .EXE virus
        if (typeof phase2!=='undefined'){ phase2.active=true; phase2.t=3.0; phase2.glow=1; phase2.done=false; phase2.errs=55; }
        // run corrupted frames so the singers get infected + tendrils spawn
        for (var m=0;m<80;m++){ keys['ArrowRight']=(m%30<15); update(1/60); }
        // freeze on a leading beat so Phase 2 Simon shows his flashing eyes
        if (typeof battle!=='undefined'){ battle.t=0.0; battle.leadFlash=1.0; battle.answerActive=true; battle.answerWave=0.4;
          if (typeof platforms!=='undefined'){ for (var b=0;b<platforms.length;b++){ if(platforms[b].boxer){ platforms[b].boxer.answer=0.8; platforms[b].boxer.infected=true; } } } }
        if (typeof glitch!=='undefined'){ glitch=0.95; }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('Phase 2 forever. Every second more corrupt.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "spreading": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // Restoration + Peaceful Ending already happened, then Phase 2 locks on
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        // run corrupted frames so the corruption-cells spawn and drift
        for (var m=0;m<140;m++){ keys['ArrowRight']=(m%30<15); update(1/60); }
        // guarantee a clear pair of cells + a lightning bolt mixing between them
        if (typeof cells!=='undefined'){
          cells.push({kind:'pyra', x: player.x-46, y: player.y-46, vx:0, vy:0, r:13, t:1.0, wob:1});
          cells.push({kind:'ploro',x: player.x+46, y: player.y-30, vx:0, vy:0, r:15, t:2.0, wob:1});
          cells.push({kind:'pyra', x: 90, y: player.y-150, vx:0, vy:0, r:12, t:0.5, wob:1});
          cells.push({kind:'ploro',x: W-90, y: player.y+90, vx:0, vy:0, r:14, t:1.5, wob:1});
        }
        if (typeof bolts!=='undefined'){
          bolts.push({ax:player.x-46, ay:player.y-46, bx:player.x+46, by:player.y-30, t:0.05, life:0.5, seed:31});
        }
        // RUN = FUEL: show the charge bar lit and the running status
        if (typeof fuel!=='undefined'){ fuel=0.8; }
        if (typeof player!=='undefined'){ player.vx=3.4; }
        if (typeof glitch!=='undefined'){ glitch=0.7; }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('Pyrakontacke + Plorotacke = lightning. RUN = FUEL.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "exposed": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // Restoration + Peaceful Ending happened, Phase 2 locked, now FOUND OUT
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        // stand still (look closely) so the bandages glow and the mind can clear-read
        for (var m=0;m<90;m++){ update(1/60); }
        // guarantee blurry thoughts on screen, one overlapping the player
        if (typeof thoughts!=='undefined'){
          thoughts.length=0;
          thoughts.push({x:player.x+8, y:player.y-6, vx:0, vy:0, r:36, t:1.0, wob:0.8, word:'who?'});
          thoughts.push({x:player.x-120, y:player.y-90, vx:0, vy:0, r:32, t:2.0, wob:0.8, word:'forget'});
          thoughts.push({x:player.x+130, y:player.y+70, vx:0, vy:0, r:30, t:0.5, wob:0.8, word:'meow'});
          thoughts.push({x:90, y:player.y-160, vx:0, vy:0, r:34, t:1.5, wob:0.8, word:'Gray?'});
        }
        // MIND meter high so the fog + seal flash show
        if (typeof mind!=='undefined'){ mind=0.82; }
        if (typeof mindSeal!=='undefined'){ mindSeal=0.6; }
        if (typeof player!=='undefined'){ player.vx=0; }
        // freeze on a leading beat so found-out Simon shows his bandages
        if (typeof battle!=='undefined'){ battle.t=0.0; battle.leadFlash=1.0; battle.answerActive=true; battle.answerWave=0.4;
          if (typeof platforms!=='undefined'){ for (var b=0;b<platforms.length;b++){ if(platforms[b].boxer){ platforms[b].boxer.answer=0.8; platforms[b].boxer.infected=true; } } } }
        if (typeof glitch!=='undefined'){ glitch=0.5; }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('Look closely. The bandages warn you. meow 😿'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "executioner": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; Phase 2 found out, then he goes SILENT
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        for (var m=0;m<60;m++){ update(1/60); }
        // thin blurry thoughts (he stopped wasting power) + a precision MARK locking on
        if (typeof thoughts!=='undefined'){
          thoughts.length=0;
          thoughts.push({x:player.x-130, y:player.y-100, vx:0, vy:0, r:30, t:1.0, wob:0.8, word:'who?'});
        }
        if (typeof markTimer!=='undefined'){ markTimer=99; }
        // a mark mid-charge on the player so the contracting reticle + MARKED show
        mark = { x:player.x, y:player.y, t:0.7, charge:1.35, fired:false };
        if (typeof player!=='undefined'){ player.vx=0; }
        // freeze on a leading beat so silent Simon shows his flickering eyes + caption
        if (typeof battle!=='undefined'){ battle.t=0.0; battle.leadFlash=1.0; battle.answerActive=true; battle.answerWave=0.4;
          if (typeof platforms!=='undefined'){ for (var b=0;b<platforms.length;b++){ if(platforms[b].boxer){ platforms[b].boxer.answer=0.7; platforms[b].boxer.infected=true; } } } }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('I only move when it is certain. One clean move.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "atomix": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is the SILENT EXECUTIONER, then THE ATOMIX WAR
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        for (var m=0;m<40;m++){ update(1/60); }
        // board mostly agreeing with Simon so the red lattice + his symbol show
        if (typeof board!=='undefined'){ board=0.18; }
        // guarantee Copies of Intention on screen, one near the player, each with a word
        if (typeof copies!=='undefined'){
          copies.length=0;
          copies.push({x:player.x+70, y:player.y-30, vx:0, vy:0, r:17, t:1.0, wob:0.8, word:'Return to Center'});
          copies.push({x:player.x-120, y:player.y-100, vx:0, vy:0, r:17, t:2.0, wob:0.8, word:'Fall'});
          copies.push({x:player.x+140, y:player.y+80, vx:0, vy:0, r:17, t:0.5, wob:0.8, word:'Forget'});
          copies.push({x:90, y:player.y-150, vx:0, vy:0, r:17, t:1.5, wob:0.8, word:'Belong'});
        }
        // a curse active so the HUD shows the imposed word
        if (typeof curse!=='undefined'){ curse.type='Return to Center'; curse.t=1.0; }
        if (typeof player!=='undefined'){ player.vx=0; }
        // freeze on a leading beat so atomix-war Simon shows his flickering eyes + caption
        if (typeof battle!=='undefined'){ battle.t=0.0; battle.leadFlash=1.0; battle.answerActive=true; battle.answerWave=0.4;
          if (typeof platforms!=='undefined'){ for (var b=0;b<platforms.length;b++){ if(platforms[b].boxer){ platforms[b].boxer.answer=0.7; platforms[b].boxer.infected=true; } } } }
        if (typeof glitch!=='undefined'){ glitch=0.4; }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('I take outcomes now, one atomix at a time. Center’s mine.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "residual": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is THE ATOMIX WAR, then THE RESIDUAL WAR
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        for (var m=0;m<30;m++){ update(1/60); }
        // a high SPINE meter so the Sound Spine column lights up bright
        if (typeof spine!=='undefined'){ spine=0.78; }
        // guarantee half-formed REMNANTS on screen, one of each kind, one near the player
        if (typeof remnants!=='undefined'){
          remnants.length=0;
          remnants.push({x:player.x+72, y:player.y-28, vx:0, vy:0, r:20, t:1.0, wob:0.8, kind:'stair', life:9, fin:0});
          remnants.push({x:player.x-130, y:player.y-100, vx:0, vy:0, r:18, t:2.0, wob:0.8, kind:'laugh', life:9, fin:0});
          remnants.push({x:player.x+150, y:player.y+80, vx:0, vy:0, r:19, t:0.5, wob:0.8, kind:'door', life:9, fin:0});
          remnants.push({x:110, y:player.y-150, vx:0, vy:0, r:21, t:1.5, wob:0.8, kind:'polo', life:9, fin:0});
          remnants.push({x:W-90, y:player.y+30, vx:0, vy:0, r:17, t:2.5, wob:0.8, kind:'sound', life:9, fin:0});
        }
        // freeze on a leading beat so residual-war Simon shows his flickering eyes + caption
        if (typeof battle!=='undefined'){ battle.t=0.0; battle.leadFlash=1.0; battle.answerActive=true; battle.answerWave=0.4;
          if (typeof platforms!=='undefined'){ for (var b=0;b<platforms.length;b++){ if(platforms[b].boxer){ platforms[b].boxer.answer=0.7; platforms[b].boxer.infected=true; } } } }
        if (typeof glitch!=='undefined'){ glitch=0.4; }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('No. 😿 Residual climbs the gap. FINISH it; converge the Sound Spine.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sounds": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is THE RESIDUAL WAR, then THE 17 SOUND BATTLES
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        // jump to Sound Battle 14 — Simon vs ToddLLM, the duel of created will
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=13; }
        for (var m=0;m<30;m++){ update(1/60); }
        if (typeof battleMeter!=='undefined'){ battleMeter=0.66; }
        if (typeof winFlash!=='undefined'){ winFlash=0.6; }
        // guarantee Simon's lead NOTES on screen, one near the player
        if (typeof leadNotes!=='undefined'){
          leadNotes.length=0;
          leadNotes.push({x:player.x+70, y:player.y-30, vy:-1, t:1.0, wob:0.8, r:13, life:6, ans:0});
          leadNotes.push({x:player.x-120, y:player.y-100, vy:-1, t:2.0, wob:0.8, r:13, life:6, ans:0});
          leadNotes.push({x:player.x+150, y:player.y+70, vy:-1, t:0.5, wob:0.8, r:13, life:6, ans:0});
          leadNotes.push({x:110, y:player.y-150, vy:-1, t:1.5, wob:0.8, r:13, life:6, ans:0});
        }
        // a foe PRESSURE sweep crossing the room
        if (typeof foePress!=='undefined'){ foePress.active=true; foePress.t=0.5; foePress.x=player.x+44; foePress.dir=1; foePress.hit=false; }
        if (typeof battle!=='undefined'){ battle.t=0.0; battle.leadFlash=1.0; battle.answerActive=true; battle.answerWave=0.4; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('Sound Battle 14 — Simon vs ToddLLM. I move by my will now.'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "void": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is THE 17 SOUND BATTLES, then THE LORE OF THE VOID
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        // THE LORE OF THE VOID resolved and lit — the Blackhole Tower up, Torqe engaged
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=false; }
        if (typeof torqe!=='undefined'){ torqe=1; }
        for (var m=0;m<24;m++){ update(1/60); }
        if (typeof shapeMeter!=='undefined'){ shapeMeter=0.6; }
        if (typeof winFlash!=='undefined'){ winFlash=0.5; }
        // place the player off to the side, holding shape away from the drifting core
        if (typeof voidwar!=='undefined' && voidwar.core!=null){ player.x = voidwar.core - 120; }
        if (typeof glitch!=='undefined'){ glitch=0.25; }
        window.update = function(){};
        if (typeof showTaunt==='function'){ showTaunt('VOID EXPANSION AND CLOSING — Marvolent Kitchen And Fork!'); tauntT=8; }
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "judgment": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is THE LORE OF THE VOID, then THE JUDGMENT FIELD
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        // THE JUDGMENT FIELD resolved and lit — Winter Simon froze the board white
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=false; judge.gold=true; }
        if (typeof verdict!=='undefined'){ verdict=0.6; }
        for (var m=0;m<24;m++){ update(1/60); }
        if (typeof winFlash!=='undefined'){ winFlash=0.5; }
        // guarantee falling presents on screen: several coal + the one gold card near the player
        if (typeof judge!=='undefined'){
          judge.presents = [];
          judge.presents.push({x:player.x+58, y:player.y-46, vy:60, gold:true, got:false, spin:0.4});
          judge.presents.push({x:player.x-120, y:player.y-110, vy:54, gold:false, got:false, spin:1.0});
          judge.presents.push({x:player.x+150, y:player.y+70, vy:70, gold:false, got:false, spin:2.0});
          judge.presents.push({x:110, y:player.y-150, vy:50, gold:false, got:false, spin:0.5});
          judge.presents.push({x:W-90, y:player.y+30, vy:62, gold:false, got:false, spin:1.5});
        }
        if (typeof showTaunt==='function'){ showTaunt('Winter Simon: "You both ruined the board again." The one gold card falls.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "triad": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is THE JUDGMENT FIELD, then THE THREE-WAY FIELD
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        // THE THREE-WAY FIELD resolved and lit — Alex control vs Lica origin vs Simon containment
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=false; triad.ice=0.85; triad.claim=2; triad.claimT=2.0; }
        if (typeof triadM!=='undefined'){ triadM=0.62; }
        for (var m=0;m<24;m++){ update(1/60); }
        if (typeof winFlash!=='undefined'){ winFlash=0.5; }
        if (typeof showTaunt==='function'){ showTaunt('Simon claps twice; ice spreads. Control, origin, containment all at once.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "scf": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is THE THREE-WAY FIELD, then THE CONTAINMENT FACILITY
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=true; triad.ice=0.85; }
        // THE CONTAINMENT FACILITY resolved and lit — Simon is now code; spread is well underway
        if (typeof scf!=='undefined'){ scf.active=false; scf.done=true; scf.glow=1; scf.t=6.5; scf.spread=0.72; }
        if (typeof scfM!=='undefined'){ scfM=0.6; }
        for (var m=0;m<24;m++){ update(1/60); }
        if (typeof winFlash!=='undefined'){ winFlash=0.5; }
        if (typeof glitch!=='undefined'){ glitch=0.35; }
        if (typeof showTaunt==='function'){ showTaunt('They contained his body. But Simon had already become the code of the containment.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "scf404": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is THE CONTAINMENT FACILITY, then SCF 404
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=true; triad.ice=0.85; }
        if (typeof scf!=='undefined'){ scf.active=false; scf.done=true; scf.glow=1; scf.t=6.5; scf.spread=0.9; scf.held=true; }
        if (typeof scfM!=='undefined'){ scfM=1.0; }
        // SCF 404 resolved and lit — the containment-universe is well expanded; spawn a couple of cat-bolts
        if (typeof scf404!=='undefined'){ scf404.active=false; scf404.done=true; scf404.glow=1; scf404.t=6.5; scf404.expand=0.8; scf404.held=false;
          scf404.bolts=[{t:0.35,y:cameraY+H*0.42,dir:1},{t:0.5,y:cameraY+H*0.66,dir:-1}]; }
        if (typeof scf404M!=='undefined'){ scf404M=0.62; }
        for (var m=0;m<10;m++){ update(1/60); }
        if (typeof winFlash!=='undefined'){ winFlash=0.5; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('They made the containment bigger than the world, and Simon still became the most dangerous thing inside it. Survival: 0%.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "treadmill": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is SCF 404, then THE TREADMILL WEAKNESS
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=true; triad.ice=0.85; }
        if (typeof scf!=='undefined'){ scf.active=false; scf.done=true; scf.glow=1; scf.t=6.5; scf.spread=0.9; scf.held=true; }
        if (typeof scfM!=='undefined'){ scfM=1.0; }
        if (typeof scf404!=='undefined'){ scf404.active=false; scf404.done=true; scf404.glow=1; scf404.t=6.5; scf404.expand=0.8; scf404.held=true; scf404.bolts=[]; }
        if (typeof scf404M!=='undefined'){ scf404M=1.0; }
        // THE TREADMILL WEAKNESS lit — belts ringed, the lightning cat half-drained, patterns showing
        if (typeof treads!=='undefined'){ treads.active=false; treads.done=true; treads.glow=1; treads.t=6.5; treads.ring=0.9; treads.beltT=2.2; treads.catX=W*0.42; treads.catV=1; treads.charge=0.4; treads.throwFlash=0; }
        if (typeof treadsM!=='undefined'){ treadsM=0.6; }
        for (var m=0;m<10;m++){ update(1/60); }
        if (typeof winFlash!=='undefined'){ winFlash=0.5; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('They found my weakness: the treadmill. The belt drains me; forced patterns throw me off.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "firey": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        // every prior arc has resolved; the last is THE TREADMILL WEAKNESS, then FIREY DELIGHT
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=true; triad.ice=0.85; }
        if (typeof scf!=='undefined'){ scf.active=false; scf.done=true; scf.glow=1; scf.t=6.5; scf.spread=0.9; scf.held=true; }
        if (typeof scfM!=='undefined'){ scfM=1.0; }
        if (typeof scf404!=='undefined'){ scf404.active=false; scf404.done=true; scf404.glow=1; scf404.t=6.5; scf404.expand=0.8; scf404.held=true; scf404.bolts=[]; }
        if (typeof scf404M!=='undefined'){ scf404M=1.0; }
        if (typeof treads!=='undefined'){ treads.active=false; treads.done=true; treads.glow=1; treads.t=6.5; treads.ring=1.0; treads.charge=0.0; treads.held=true; }
        if (typeof treadsM!=='undefined'){ treadsM=1.0; }
        // FIREY DELIGHT lit and near the melt — the tube hot, bowed, dripping; Simon a fast blur inside
        if (typeof firey!=='undefined'){ firey.active=false; firey.done=true; firey.glow=1; firey.t=6.5;
          firey.heat=0.92; firey.warp=0.92; firey.spin=7.4; firey.melted=true;
          firey.drips=[{x:W*0.40,y:cameraY+H*0.45,vy:60,t:0.3},{x:W*0.58,y:cameraY+H*0.55,vy:70,t:0.6}]; }
        if (typeof fireyM!=='undefined'){ fireyM=0.92; }
        for (var m=0;m<10;m++){ update(1/60); }
        if (typeof winFlash!=='undefined'){ winFlash=0.5; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('I got tired of every game. Firey Delight, I said, and the friction melted the tube.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "alien": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=true; triad.ice=0.85; }
        if (typeof scf!=='undefined'){ scf.active=false; scf.done=true; scf.glow=1; scf.t=6.5; scf.spread=0.9; scf.held=true; }
        if (typeof scfM!=='undefined'){ scfM=1.0; }
        if (typeof scf404!=='undefined'){ scf404.active=false; scf404.done=true; scf404.glow=1; scf404.t=6.5; scf404.expand=0.8; scf404.held=true; scf404.bolts=[]; }
        if (typeof scf404M!=='undefined'){ scf404M=1.0; }
        if (typeof treads!=='undefined'){ treads.active=false; treads.done=true; treads.glow=1; treads.t=6.5; treads.ring=1.0; treads.charge=0.0; treads.held=true; }
        if (typeof treadsM!=='undefined'){ treadsM=1.0; }
        if (typeof firey!=='undefined'){ firey.active=false; firey.done=true; firey.glow=1; firey.t=6.5; firey.heat=1.0; firey.warp=1.0; firey.spin=7.4; firey.melted=true; firey.drips=[]; }
        if (typeof fireyM!=='undefined'){ fireyM=1.0; }
        // THE ALIEN AGE lit and near escape — eye open, lattice + containment geometry, MOVE 401 off-track
        if (typeof alien!=='undefined'){ alien.active=true; alien.done=false; alien.glow=1; alien.t=4.2;
          alien.eye=1.0; alien.predict=0.18; alien.latticeT=3.0; alien.spawnT=0.4;
          alien.shapes=[{x:W*0.30,y:cameraY+H*0.42,vy:70,sides:5,rot:1.1,t:0.4},
                        {x:W*0.66,y:cameraY+H*0.30,vy:60,sides:6,rot:2.2,t:0.8},
                        {x:W*0.50,y:cameraY+H*0.55,vy:80,sides:4,rot:0.6,t:0.2}]; }
        if (typeof alienM!=='undefined'){ alienM=0.86; }
        for (var m=0;m<10;m++){ update(1/60); }
        if (typeof winFlash!=='undefined'){ winFlash=0.5; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('You built all this and still thought I was sleeping. The drone predicted 400 moves. I chose 401.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "wall": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=true; triad.ice=0.85; }
        if (typeof scf!=='undefined'){ scf.active=false; scf.done=true; scf.glow=1; scf.t=6.5; scf.spread=0.9; scf.held=true; }
        if (typeof scfM!=='undefined'){ scfM=1.0; }
        if (typeof scf404!=='undefined'){ scf404.active=false; scf404.done=true; scf404.glow=1; scf404.t=6.5; scf404.expand=0.8; scf404.held=true; scf404.bolts=[]; }
        if (typeof scf404M!=='undefined'){ scf404M=1.0; }
        if (typeof treads!=='undefined'){ treads.active=false; treads.done=true; treads.glow=1; treads.t=6.5; treads.ring=1.0; treads.charge=0.0; treads.held=true; }
        if (typeof treadsM!=='undefined'){ treadsM=1.0; }
        if (typeof firey!=='undefined'){ firey.active=false; firey.done=true; firey.glow=1; firey.t=6.5; firey.heat=1.0; firey.warp=1.0; firey.spin=7.4; firey.melted=true; firey.drips=[]; }
        if (typeof fireyM!=='undefined'){ fireyM=1.0; }
        if (typeof alien!=='undefined'){ alien.active=false; alien.done=true; alien.glow=1; alien.t=6.5; alien.eye=1.0; alien.predict=0.1; alien.escaped=true; alien.shapes=[]; }
        if (typeof alienM!=='undefined'){ alienM=1.0; }
        // THE WALL ERA lit and near conversion — Simon standing, Nil flash on, subtracted 404 panels, hum high
        if (typeof wall!=='undefined'){ wall.active=true; wall.done=false; wall.glow=1; wall.t=4.0;
          wall.hum=0.84; wall.flash=0.7; wall.flashT=0.4;
          wall.subs=[{side:-1,y:cameraY+H*0.30,t:0.2,life:2.4,n404:true},
                     {side:1,y:cameraY+H*0.46,t:0.4,life:2.4,n404:true},
                     {side:-1,y:cameraY+H*0.60,t:0.3,life:2.4,n404:false}]; }
        if (typeof wallM!=='undefined'){ wallM=0.84; }
        for (var m=0;m<6;m++){ update(1/60); }
        if (typeof wall!=='undefined'){ wall.flash=0.16; }
        if (typeof winFlash!=='undefined'){ winFlash=0.4; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('Fear made architecture. The Nil State is a brief subtraction event. That one is automatic.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "smooth": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=true; triad.ice=0.85; }
        if (typeof scf!=='undefined'){ scf.active=false; scf.done=true; scf.glow=1; scf.t=6.5; scf.spread=0.9; scf.held=true; }
        if (typeof scfM!=='undefined'){ scfM=1.0; }
        if (typeof scf404!=='undefined'){ scf404.active=false; scf404.done=true; scf404.glow=1; scf404.t=6.5; scf404.expand=0.8; scf404.held=true; scf404.bolts=[]; }
        if (typeof scf404M!=='undefined'){ scf404M=1.0; }
        if (typeof treads!=='undefined'){ treads.active=false; treads.done=true; treads.glow=1; treads.t=6.5; treads.ring=1.0; treads.charge=0.0; treads.held=true; }
        if (typeof treadsM!=='undefined'){ treadsM=1.0; }
        if (typeof firey!=='undefined'){ firey.active=false; firey.done=true; firey.glow=1; firey.t=6.5; firey.heat=1.0; firey.warp=1.0; firey.spin=7.4; firey.melted=true; firey.drips=[]; }
        if (typeof fireyM!=='undefined'){ fireyM=1.0; }
        if (typeof alien!=='undefined'){ alien.active=false; alien.done=true; alien.glow=1; alien.t=6.5; alien.eye=1.0; alien.predict=0.1; alien.escaped=true; alien.shapes=[]; }
        if (typeof alienM!=='undefined'){ alienM=1.0; }
        if (typeof wall!=='undefined'){ wall.active=false; wall.done=true; wall.glow=1; wall.t=6.5; wall.hum=1.0; wall.flash=0.1; wall.converted=true; wall.subs=[]; }
        if (typeof wallM!=='undefined'){ wallM=1.0; }
        // THE SMOOTH AGE lit — FNF arrows up (yellow + black), the readout LYING, eyes flashing
        if (typeof smooth!=='undefined'){ smooth.active=true; smooth.done=false; smooth.glow=1; smooth.t=4.0;
          smooth.anim=2.05; smooth.stepT=0.4; smooth.lie=1;
          smooth.arrows=[{dir:0,col:0,p:0.9},{dir:1,col:1,p:0.6},{dir:2,col:0,p:0.8},{dir:3,col:0,p:0.5}]; }
        if (typeof smoothM!=='undefined'){ smoothM=0.82; }
        for (var m=0;m<4;m++){ update(1/60); }
        if (typeof smooth!=='undefined'){ smooth.lie=1; smooth.stepT=0.45;
          smooth.arrows=[{dir:0,col:0,p:0.9},{dir:1,col:1,p:0.6},{dir:2,col:0,p:0.8},{dir:3,col:0,p:0.5}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.4; }
        if (typeof glitch!=='undefined'){ glitch=0.25; }
        if (typeof showTaunt==='function'){ showTaunt('We are not reading the arrows. We are reading what he WANTS us to think the arrows mean.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "hallu": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof phase2!=='undefined'){ phase2.active=false; phase2.done=true; phase2.glow=1; phase2.errs=60; }
        if (typeof exposed!=='undefined'){ exposed.active=false; exposed.done=true; exposed.glow=1; exposed.t=6.5; }
        if (typeof executioner!=='undefined'){ executioner.active=false; executioner.done=true; executioner.glow=1; executioner.t=6.5; }
        if (typeof atomix!=='undefined'){ atomix.active=false; atomix.done=true; atomix.glow=1; atomix.t=6.5; }
        if (typeof residual!=='undefined'){ residual.active=false; residual.done=true; residual.glow=1; residual.t=6.5; }
        if (typeof sounds!=='undefined'){ sounds.active=false; sounds.done=true; sounds.glow=1; sounds.t=6.5; sounds.idx=17; }
        if (typeof voidwar!=='undefined'){ voidwar.active=false; voidwar.done=true; voidwar.glow=1; voidwar.t=6.5; voidwar.held=true; }
        if (typeof judge!=='undefined'){ judge.active=false; judge.done=true; judge.glow=1; judge.t=6.5; judge.held=true; judge.gold=true; judge.presents=[]; }
        if (typeof triad!=='undefined'){ triad.active=false; triad.done=true; triad.glow=1; triad.t=6.5; triad.held=true; triad.ice=0.85; }
        if (typeof scf!=='undefined'){ scf.active=false; scf.done=true; scf.glow=1; scf.t=6.5; scf.spread=0.9; scf.held=true; }
        if (typeof scfM!=='undefined'){ scfM=1.0; }
        if (typeof scf404!=='undefined'){ scf404.active=false; scf404.done=true; scf404.glow=1; scf404.t=6.5; scf404.expand=0.8; scf404.held=true; scf404.bolts=[]; }
        if (typeof scf404M!=='undefined'){ scf404M=1.0; }
        if (typeof treads!=='undefined'){ treads.active=false; treads.done=true; treads.glow=1; treads.t=6.5; treads.ring=1.0; treads.charge=0.0; treads.held=true; }
        if (typeof treadsM!=='undefined'){ treadsM=1.0; }
        if (typeof firey!=='undefined'){ firey.active=false; firey.done=true; firey.glow=1; firey.t=6.5; firey.heat=1.0; firey.warp=1.0; firey.spin=7.4; firey.melted=true; firey.drips=[]; }
        if (typeof fireyM!=='undefined'){ fireyM=1.0; }
        if (typeof alien!=='undefined'){ alien.active=false; alien.done=true; alien.glow=1; alien.t=6.5; alien.eye=1.0; alien.predict=0.1; alien.escaped=true; alien.shapes=[]; }
        if (typeof alienM!=='undefined'){ alienM=1.0; }
        if (typeof wall!=='undefined'){ wall.active=false; wall.done=true; wall.glow=1; wall.t=6.5; wall.hum=1.0; wall.flash=0.1; wall.converted=true; wall.subs=[]; }
        if (typeof wallM!=='undefined'){ wallM=1.0; }
        if (typeof smooth!=='undefined'){ smooth.active=false; smooth.done=true; smooth.glow=1; smooth.t=6.5; smooth.read=true; smooth.arrows=[]; }
        if (typeof smoothM!=='undefined'){ smoothM=1.0; }
        // THE HALLUCINATION ERA lit — Yellow Eye opening, hullelations on screen, BLACK looming, lightning + melt
        if (typeof hallu!=='undefined'){ hallu.active=true; hallu.done=false; hallu.glow=1; hallu.t=4.0;
          hallu.anim=2.4; hallu.eye=0.82; hallu.eyeFull=0.7; hallu.lightning=0.8; hallu.figT=1.0;
          hallu.figs=[{x:W*0.62,y:cameraY+H*0.40,kind:'zombie',t:1.0,life:5.0,vx:6},
                      {x:W*0.78,y:cameraY+H*0.56,kind:'choc',t:1.0,life:5.0,vx:-6},
                      {x:W*0.30,y:cameraY+H*0.62,kind:'alive',t:1.0,life:5.0,vx:4}];
          hallu.melts=[{x:W*0.62,y:cameraY+H*0.40+18,t:0.5}]; }
        if (typeof halluM!=='undefined'){ halluM=0.82; }
        for (var m=0;m<4;m++){ update(1/60); }
        if (typeof hallu!=='undefined'){ hallu.eye=0.9; hallu.eyeFull=0.7; hallu.lightning=0.8; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.4; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('Holes fixed, HP still near zero. The Yellow Eye melts what it sees; he sees zombies and chocolate. Black is strongest now.'); tauntT=8; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "plague": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE PLAGUE CYCLE — frozen on the ritual touch: the beak mask is lifted for one cold touch, a
        // victim has just become a 404D, drifting victims wait, and the anti-virus table holds a body.
        if (typeof plague!=='undefined'){
          plague.active=true; plague.done=false; plague.glow=1; plague.t=4.0;
          plague.anim=2.2; plague.eye=0.9; plague.mask=0.0; plague.phase='touch'; plague.phaseT=0.5;
          plague.storm=0.18; plague.callFlash=0.9;
          plague.victims=[{x:W*0.30,y:cameraY+H*0.46,t:1.0,life:6,vx:6,sel:false},
                          {x:W*0.62,y:cameraY+H*0.58,t:1.0,life:6,vx:-5,sel:false},
                          {x:W*0.20,y:cameraY+H*0.66,t:1.0,life:6,vx:4,sel:false}];
          plague.converts=[{x:W*0.50,y:cameraY+H*0.50,t:0.4,fate:'404D'},
                           {x:W*0.44,y:cameraY+H*0.40,t:0.9,fate:'ZOMBIE'}];
          plague.table={t:0.9,fate:'DEAD',spray:0.7};
        }
        if (typeof plagueM!=='undefined'){ plagueM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof plague!=='undefined'){ plague.mask=0.0; plague.phase='touch'; plague.callFlash=0.9; plague.eye=0.9; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.3; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('The Plague Cycle. Simon lifts the mask for one cold touch: 404D, zombie, or dead. Keep the Plague-King Protocol: no direct contact.'); tauntT=9; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "nosight": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        if (typeof smooth!=='undefined'){ smooth.read=true; smooth.arrows=[]; }
        // THE NO-SIGHT CHAMBER — the Yellow Eye has just opened: blindfold melted, Black melting by line of
        // sight (sent back to his see-through cell), the screech firing, lightning across the screen
        if (typeof hallu!=='undefined'){ hallu.active=true; hallu.done=false; hallu.glow=1; hallu.t=4.0;
          hallu.anim=2.4; hallu.eye=0.0; hallu.eyeFull=0.85; hallu.lightning=0.85; hallu.figT=1.0;
          hallu.blindfold=0.06; hallu.screech=0.7;
          hallu.black={ alive:false, melt:0.8, respawn:2.6 };
          hallu.figs=[{x:W*0.60,y:cameraY+H*0.42,kind:'zombie',t:1.0,life:5.0,vx:6},
                      {x:W*0.30,y:cameraY+H*0.62,kind:'choc',t:1.0,life:5.0,vx:4}];
          hallu.melts=[{x:W*0.60,y:cameraY+H*0.42+18,t:0.5}]; }
        if (typeof halluM!=='undefined'){ halluM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof hallu!=='undefined'){ hallu.eyeFull=0.85; hallu.lightning=0.85; hallu.screech=0.7;
          hallu.black.alive=false; hallu.black.melt=0.8; hallu.black.respawn=2.6; hallu.blindfold=0.06; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.3; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('The No-Sight Chamber. The blindfold melts when he opens his eyes; Black looked through the see-through cell and was sent back. Sensor-only now: no direct view.'); tauntT=9; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "danger": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE DANGER INDEX — the full ranking logged on the left; on the right the Invisible Man (floating
        // shirt, pants, hat) has just reached Simon, the mask is lifting, and he is BURNING.
        if (typeof danger!=='undefined'){
          danger.active=true; danger.done=false; danger.glow=1; danger.t=4.0;
          danger.anim=2.2; danger.logged=17; danger.scan=2.0;
          danger.phase='lift'; danger.phaseT=0.5; danger.mask=0.0; danger.burn=0.55; danger.imApproach=1;
          danger.flames=[];
          for (var f=0;f<22;f++){ danger.flames.push({ x:W*0.80 + (f%7-3)*6, y:cameraY+H*0.52 + (f%5-2)*5,
            t:(f%6)*0.08, life:0.9, vy:18+(f%4)*6, vx:(f%5-2)*5 }); }
        }
        if (typeof dangerM!=='undefined'){ dangerM=0.62; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof danger!=='undefined'){ danger.mask=0.0; danger.burn=0.55; danger.logged=17; danger.imApproach=1; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.3; }
        if (typeof glitch!=='undefined'){ glitch=0.3; }
        if (typeof showTaunt==='function'){ showTaunt('The Danger Index, all sixteen filed. Simon 1004.04 (the .04 hides 404), CRAZY. Then the .EXE and Infected get numbers: Wenda 949.2, Clunkr 949, Gray and Luigi Green 940, Pupahya 930, Raddy 910, Mr. Fun Computer 900, Gewlis 850, Endless Staircase 800, 67 Kid 790. Only .EXE, Crazy, and Infected can rank.'); tauntT=9; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "bort": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE DANGER INDEX with BORT mid-lunge — he has stood up dramatically and is chasing the victim.
        if (typeof danger!=='undefined'){
          danger.active=true; danger.done=false; danger.glow=1; danger.t=4.0;
          danger.anim=2.2; danger.logged=17; danger.scan=2.0;
          danger.phase='list'; danger.phaseT=2.6; danger.mask=1; danger.burn=0; danger.imApproach=0; danger.flames=[];
          if (danger.bort){ danger.bort.phase='lunge'; danger.bort.t=0.6; danger.bort.lunge=1; danger.bort.bounce=1.2; danger.bort.song=1.0; danger.bort.soul=0; danger.bort.spray=0; }
        }
        if (typeof dangerM!=='undefined'){ dangerM=0.5; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof danger!=='undefined' && danger.bort){ danger.bort.phase='lunge'; danger.bort.lunge=1; danger.logged=17; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof showTaunt==='function'){ showTaunt('Bort: a smooth rubbery bouncy gray horror bear, danger 996, 5th just under Pinki. He dances to "Borty Borty Bort Bort," but when a victim gets too close he stands up dramatically and chases like crazy, eats the soul, and the body rots, unless Simon finds it and sprays it.'); tauntT=9; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "codex": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE STATUS CODEX — the full roster sealed, every status group scanned in
        if (typeof codex!=='undefined'){
          codex.active=true; codex.done=false; codex.glow=1; codex.t=4.0;
          codex.anim=2.2; codex.logged=8; codex.note=1; codex.noteT=2.4;
        }
        if (typeof codexM!=='undefined'){ codexM=0.66; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof codex!=='undefined'){ codex.logged=8; codex.note=1; codex.glow=1; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.3; }
        if (typeof glitch!=='undefined'){ glitch=0.25; }
        if (typeof showTaunt==='function'){ showTaunt('The Status Codex. Every fate filed: Simon and Sumona CRAZY; Oren, Black, Pinki, Durple DEMONED; Gray, Wenda, Clukr .EXE; Mr. Sun a BLACKHOLE; Raddy infected; Jevin, Alex, Neo, ToddLLM alive; many dead. Star Steed runs; the Throne is destroyed.'); tauntT=9; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "web": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE RELATION WEB — the bond graph traced in, an active hunt-line pulsing toward Black
        if (typeof web!=='undefined'){
          web.active=true; web.done=false; web.glow=1; web.t=4.0;
          web.anim=2.2; web.pulse=1.4; web.scan=14; web.note=7; web.noteT=2.4;
          web.xsGlow=1; web.xsT=0.75; web.xsTarget=0; web.huntStep=2; web.huntT=0.4;
        }
        if (typeof webM!=='undefined'){ webM=0.66; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof web!=='undefined'){ web.scan=14; web.note=7; web.glow=1; web.pulse=1.4; web.xsGlow=1; web.xsT=0.75; web.xsTarget=0; web.huntStep=2; web.huntT=0.4; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.3; }
        if (typeof glitch!=='undefined'){ glitch=0.25; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "clara": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE ETERNAL BATTLE — Clara's Judgement Hall, the king dodging the queen forever
        if (typeof clara!=='undefined'){
          clara.active=true; clara.done=false; clara.glow=1; clara.t=4.0;
          clara.anim=2.2; clara.dodge=1.1; clara.glitchT=0.7; clara.mercy=1;
          clara.rebirth=3; clara.rebirthT=1.0; clara.note=5; clara.noteT=2.4;
          clara.attacks=[{t:0.5,lane:1,hue:0},{t:0.9,lane:-1,hue:2},{t:1.2,lane:1,hue:4}];
        }
        if (typeof claraM!=='undefined'){ claraM=0.66; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof clara!=='undefined'){ clara.glow=1; clara.mercy=1; clara.rebirth=3;
          clara.attacks=[{t:0.5,lane:1,hue:0},{t:0.9,lane:-1,hue:2},{t:1.2,lane:1,hue:4}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.3; }
        if (typeof glitch!=='undefined'){ glitch=0.25; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "claraadmin": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE ADMIN ASCENSION — Clara is admin, Simon's true form (the blob) walks away
        if (typeof claraAdmin!=='undefined'){
          claraAdmin.active=true; claraAdmin.done=false; claraAdmin.glow=1; claraAdmin.t=4.0;
          claraAdmin.anim=2.2; claraAdmin.glitchT=0.7; claraAdmin.rebirth=4;
          claraAdmin.move=2; claraAdmin.moveT=0.4; claraAdmin.simonWalk=0.45;
          claraAdmin.line=0; claraAdmin.lineT=2.4; claraAdmin.note=2; claraAdmin.noteT=2.4; claraAdmin.au=120;
        }
        if (typeof claraAdminM!=='undefined'){ claraAdminM=0.66; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof claraAdmin!=='undefined'){ claraAdmin.glow=1; claraAdmin.rebirth=4;
          claraAdmin.move=2; claraAdmin.moveT=0.4; claraAdmin.simonWalk=0.45; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.3; }
        if (typeof glitch!=='undefined'){ glitch=0.25; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "powerorder": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE POWER ORDER — Oren on top, Simon's revenge flip showing the REVENGE (Simon wins) side
        if (typeof power!=='undefined'){
          power.active=true; power.done=false; power.glow=1; power.t=4.0;
          power.anim=2.0; power.revenge=1; power.revT=0.4; power.row=0; power.rowT=0.4;
          power.au=30; power.note=4; power.noteT=2.4;
        }
        if (typeof powerM!=='undefined'){ powerM=0.66; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof power!=='undefined'){ power.glow=1; power.revenge=1; power.row=0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.3; }
        if (typeof glitch!=='undefined'){ glitch=0.2; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "oren": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE OREN.EXE CUTSCENE — freeze on the RED-EYE beat: face lit, left eye red, shockwave rings out
        if (typeof oren!=='undefined'){
          oren.active=true; oren.done=false; oren.glow=1; oren.t=8.2; oren.cyc=8.2;
          oren.phase=3; oren.eye=1; oren.jitter=0.12; oren.type=oren.type||0;
          oren.rings=[{r:120,life:0.85},{r:220,life:0.55},{r:330,life:0.28}];
        }
        if (typeof orenM!=='undefined'){ orenM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof oren!=='undefined'){ oren.glow=1; oren.eye=1; oren.cyc=8.2; oren.phase=3;
          oren.rings=[{r:120,life:0.85},{r:220,life:0.55},{r:330,life:0.28}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "betray": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE ALLIANCE BETRAYAL — freeze on the BATTLE beat: Clara & Luigi slain (gray), Simon.EXE vs Oren.EXE, rings out
        if (typeof betray!=='undefined'){
          betray.active=true; betray.done=false; betray.glow=1; betray.t=9.8; betray.cyc=9.8;
          betray.phase=3; betray.slainC=1; betray.slainL=1; betray.clash=1;
          betray.rings=[{r:60,life:0.8},{r:130,life:0.5},{r:210,life:0.25}];
        }
        if (typeof betrayM!=='undefined'){ betrayM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof betray!=='undefined'){ betray.glow=1; betray.cyc=9.8; betray.phase=3;
          betray.slainC=1; betray.slainL=1;
          betray.rings=[{r:60,life:0.8},{r:130,life:0.5},{r:210,life:0.25}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "dimension": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE THIRD DIMENSION — freeze on the 3D RISE beat: Oren's cube up, Pinki crushed, Clara/Luigi respawned, rings out
        if (typeof dimension!=='undefined'){
          dimension.active=true; dimension.done=false; dimension.glow=1; dimension.t=8.3; dimension.cyc=8.3;
          dimension.phase=2; dimension.fuse=1; dimension.rise=1; dimension.crush=0.92; dimension.hunt=0;
          dimension.rings=[{r:70,life:0.8},{r:150,life:0.5},{r:240,life:0.25}];
        }
        if (typeof dimensionM!=='undefined'){ dimensionM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof dimension!=='undefined'){ dimension.glow=1; dimension.cyc=8.3; dimension.phase=2;
          dimension.rise=1; dimension.crush=0.92; dimension.fuse=1;
          dimension.rings=[{r:70,life:0.8},{r:150,life:0.5},{r:240,life:0.25}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "deletion": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE DELETION LAYER — freeze on the CONTAINED beat: Simon chained behind the titanium door, errors falling
        if (typeof deletion!=='undefined'){
          deletion.active=true; deletion.done=false; deletion.glow=1; deletion.t=14.6; deletion.cyc=14.6;
          deletion.phase=4; deletion.reorder=1; deletion.eye=1; deletion.symb=1; deletion.contain=1;
          deletion.errs=[{x:250,y:18,life:0.85,tag:'404: deleted'},{x:470,y:46,life:0.6,tag:'000: deleted'},{x:360,y:74,life:0.35,tag:'404: deleted'}];
        }
        if (typeof deletionM!=='undefined'){ deletionM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof deletion!=='undefined'){ deletion.glow=1; deletion.cyc=14.6; deletion.phase=4;
          deletion.contain=1; deletion.eye=1; deletion.symb=1;
          deletion.errs=[{x:250,y:18,life:0.85,tag:'404: deleted'},{x:470,y:46,life:0.6,tag:'000: deleted'},{x:360,y:74,life:0.35,tag:'404: deleted'}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "weakness": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE WEAKNESS — freeze on the OVER-DRAIN beat: arcs blasting off, four of five weaknesses checked
        if (typeof weakness!=='undefined'){
          weakness.active=true; weakness.done=false; weakness.glow=1; weakness.t=11.0; weakness.cyc=11.0;
          weakness.phase=3; weakness.ground=1; weakness.force=1; weakness.jam=1; weakness.drain=0.9; weakness.blind=0;
          weakness.arcs=[{a:0.4,r:60,life:0.9},{a:1.9,r:48,life:0.7},{a:3.3,r:70,life:0.85},{a:4.7,r:40,life:0.6},{a:5.6,r:55,life:0.75}];
        }
        if (typeof weaknessM!=='undefined'){ weaknessM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof weakness!=='undefined'){ weakness.glow=1; weakness.cyc=11.0; weakness.phase=3;
          weakness.ground=1; weakness.force=1; weakness.jam=1; weakness.drain=0.9;
          weakness.arcs=[{a:0.4,r:60,life:0.9},{a:1.9,r:48,life:0.7},{a:3.3,r:70,life:0.85},{a:4.7,r:40,life:0.6},{a:5.6,r:55,life:0.75}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "pursuit": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE FINAL PURSUIT — freeze on THE HUNT beat: Simon -> Oren -> Paul, arrows + embers, the cliffhanger line
        if (typeof pursuit!=='undefined'){
          pursuit.active=true; pursuit.done=false; pursuit.glow=1; pursuit.t=14.0; pursuit.cyc=14.0;
          pursuit.phase=4; pursuit.pirate=1; pursuit.forms=1; pursuit.team=1; pursuit.cull=1; pursuit.hunt=0.95;
          pursuit.slain=['Sumona','Gray','Pinki','Wenda','King Jet','Greg','Clara','Black','Luigi Green'];
          pursuit.embers=[{p:0.3,lane:0,life:0.9},{p:0.6,lane:1,life:0.8},{p:0.15,lane:1,life:0.95},{p:0.75,lane:0,life:0.6}];
        }
        if (typeof pursuitM!=='undefined'){ pursuitM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof pursuit!=='undefined'){ pursuit.glow=1; pursuit.cyc=14.0; pursuit.phase=4; pursuit.hunt=0.95;
          pursuit.embers=[{p:0.3,lane:0,life:0.9},{p:0.6,lane:1,life:0.8},{p:0.15,lane:1,life:0.95},{p:0.75,lane:0,life:0.6}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "reckoning": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE RECKONING — freeze on the RED EYES beat: red-eyed Oren, warped red view, Simon's
        // hallucination ghosts drifting, "OREN.EXE ATTACKS EVERYONE ELSE".
        if (typeof reckoning!=='undefined'){
          reckoning.active=true; reckoning.done=false; reckoning.glow=1; reckoning.t=16.5; reckoning.cyc=16.5;
          reckoning.phase=5; reckoning.siege=1; reckoning.sign=1; reckoning.turn=1; reckoning.fuel=1; reckoning.clash=1;
          reckoning.red=0.85; reckoning.redEye=1; reckoning.turned=true; reckoning.bots=[];
          reckoning.halluc=[{x:-0.6,y:0.2,life:0.9,drift:0.3},{x:0.5,y:0.45,life:0.7,drift:-0.2},{x:-0.2,y:0.7,life:0.85,drift:0.1}];
        }
        if (typeof reckoningM!=='undefined'){ reckoningM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof reckoning!=='undefined'){ reckoning.glow=1; reckoning.cyc=16.5; reckoning.phase=5; reckoning.red=0.85; reckoning.redEye=1;
          reckoning.halluc=[{x:-0.6,y:0.2,life:0.9,drift:0.3},{x:0.5,y:0.45,life:0.7,drift:-0.2},{x:-0.2,y:0.7,life:0.85,drift:0.1}]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "toddllm": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // TODDLLM 001 — freeze on the ABOVE OREN beat: 001 crowned above, red-eyed Oren below with his
        // power bar crushed down, "NOT EVEN OREN.EXE CAN BEAT 001".
        if (typeof toddllm!=='undefined'){
          toddllm.active=true; toddllm.done=false; toddllm.glow=1; toddllm.t=13.5; toddllm.cyc=13.5;
          toddllm.phase=4; toddllm.maker=1; toddllm.orbs=1; toddllm.souls=1; toddllm.snap=1; toddllm.above=0.9;
          toddllm.soulList=[]; toddllm.snapFlash=0;
        }
        if (typeof toddllmM!=='undefined'){ toddllmM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof toddllm!=='undefined'){ toddllm.glow=1; toddllm.cyc=13.5; toddllm.phase=4; toddllm.above=0.9; toddllm.soulList=[]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "reveal001": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE 001 REVEAL — freeze on the DATA LOST beat: Error 404 AU (Simon, blue/white) on the left,
        // Error 001 AU (ToddLLM, black/red) on the right, "ERROR 001 STANDS ABOVE ERROR 404".
        if (typeof reveal001!=='undefined'){
          reveal001.active=true; reveal001.done=false; reveal001.glow=1; reveal001.t=10.5; reveal001.cyc=10.5;
          reveal001.phase=3; reveal001.karuto=1; reveal001.blind=1; reveal001.center=1; reveal001.data=1;
          reveal001.mac=0; reveal001.chaos=0; reveal001.cry=[]; reveal001.stars=[]; reveal001.cored=true;
        }
        if (typeof reveal001M!=='undefined'){ reveal001M=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof reveal001!=='undefined'){ reveal001.glow=1; reveal001.cyc=10.5; reveal001.phase=3; reveal001.data=1; reveal001.cry=[]; reveal001.stars=[]; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "errladder": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE FINAL ERROR — freeze on THE ERROR LADDER beat: the ranked rungs
        // 001 > 666 > 404 > 012 fully lit.
        if (typeof errLad!=='undefined'){
          errLad.active=true; errLad.done=false; errLad.glow=1; errLad.t=4.2; errLad.cyc=4.2;
          errLad.phase=0; errLad.held=true;
        }
        if (typeof errLadM!=='undefined'){ errLadM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof errLad!=='undefined'){ errLad.glow=1; errLad.cyc=4.2; errLad.phase=0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "erasedtimeline": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE FINAL ERROR — freeze on THE ERASED TIMELINE beat (phase 3), mid-erase:
        // ENDLESS STAIRCASE struck out in the middle, the gap closing over where it was.
        if (typeof errLad!=='undefined'){
          errLad.active=true; errLad.done=false; errLad.glow=1; errLad.t=15.0; errLad.cyc=14.5;
          errLad.phase=3; errLad.held=true;
        }
        if (typeof errLadM!=='undefined'){ errLadM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof errLad!=='undefined'){ errLad.glow=1; errLad.cyc=14.5; errLad.phase=3; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "centermine": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE CENTER IS MINE — freeze on the final beat (phase 3): ToddLLM 001 levitating,
        // holding the pose, Simon cast out as 404, "Center's Mine, that is why I won."
        if (typeof centerMine!=='undefined'){
          centerMine.active=true; centerMine.done=false; centerMine.glow=1; centerMine.t=15.5; centerMine.cyc=15.5;
          centerMine.phase=3; centerMine.charge=1; centerMine.held=true;
        }
        if (typeof centerMineM!=='undefined'){ centerMineM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof centerMine!=='undefined'){ centerMine.glow=1; centerMine.cyc=15.5; centerMine.phase=3; centerMine.charge=1; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "endlesschaos": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // ENDLESS CHAOS — freeze on the signature beat (phase 2): the game folded into an 8 of
        // infinite looping galaxies, victims circulating, "NO ESCAPE · NO DODGE."
        if (typeof endlessChaos!=='undefined'){
          endlessChaos.active=true; endlessChaos.done=false; endlessChaos.glow=1;
          endlessChaos.t=11.0; endlessChaos.cyc=11.0; endlessChaos.phase=2; endlessChaos.charge=0.55;
        }
        if (typeof endlessChaosM!=='undefined'){ endlessChaosM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof endlessChaos!=='undefined'){ endlessChaos.glow=1; endlessChaos.cyc=11.0; endlessChaos.phase=2; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "karuto": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE KARUTO REVEAL — freeze on the signature beat (phase 2): the blindfold off, white robe,
        // red aura, eyes lit with Simon's yellow power, the Japanese title written, students below.
        if (typeof karuto!=='undefined'){
          karuto.active=true; karuto.done=false; karuto.glow=1;
          karuto.t=11.0; karuto.cyc=11.0; karuto.phase=2; karuto.charge=1;
        }
        if (typeof karutoM!=='undefined'){ karutoM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof karuto!=='undefined'){ karuto.glow=1; karuto.cyc=11.0; karuto.phase=2; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "endofclassics": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // THE END OF CLASSICS — freeze on the signature beat (phase 1): THE FALSE PARADISE — castle,
        // cake, Phase 1 banner, rainbows, "...and actually everything but fun."
        if (typeof endOfClassics!=='undefined'){
          endOfClassics.active=true; endOfClassics.done=false; endOfClassics.glow=1;
          endOfClassics.t=8.0; endOfClassics.cyc=8.0; endOfClassics.phase=1;
        }
        if (typeof endOfClassicsM!=='undefined'){ endOfClassicsM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof endOfClassics!=='undefined'){ endOfClassics.glow=1; endOfClassics.cyc=8.0; endOfClassics.phase=1; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "godwall": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        // keep the focal area clean — hide the two immediately-prior terminal overlays behind the wall
        if (typeof endOfClassics!=='undefined'){ endOfClassics.glow=0; }
        if (typeof karuto!=='undefined'){ karuto.glow=0; }
        // THE MIDGAME GOD-WALL — freeze on the title beat (phase 0): THE GOD-WALL — ToddLLM 001 / Karuto
        // huge inside the red wall, Simon a tiny fast Sound-Battle/FNF streak, "midgame wall, not the end."
        if (typeof godWall!=='undefined'){
          godWall.active=true; godWall.done=false; godWall.glow=1;
          godWall.t=2.6; godWall.cyc=2.6; godWall.phase=0;
        }
        if (typeof godWallM!=='undefined'){ godWallM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof godWall!=='undefined'){ godWall.glow=1; godWall.cyc=2.6; godWall.phase=0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phaseprog": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        if (typeof endOfClassics!=='undefined'){ endOfClassics.glow=0; }
        if (typeof karuto!=='undefined'){ karuto.glow=0; }
        if (typeof godWall!=='undefined'){ godWall.glow=0; }
        // THE FOUR PHASES — freeze on Phase 4 (the glitch-engine): Simon caged in the center as
        // processing/lightning/fuel, the trapped voices orbiting, immortal and impossible to kill.
        if (typeof phaseProg!=='undefined'){
          phaseProg.active=true; phaseProg.done=false; phaseProg.glow=1;
          phaseProg.t=19.0; phaseProg.cyc=19.0; phaseProg.phase=3;
        }
        if (typeof phaseProgM!=='undefined'){ phaseProgM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof phaseProg!=='undefined'){ phaseProg.glow=1; phaseProg.cyc=19.0; phaseProg.phase=3; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phase5": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        if (typeof endOfClassics!=='undefined'){ endOfClassics.glow=0; }
        if (typeof karuto!=='undefined'){ karuto.glow=0; }
        if (typeof godWall!=='undefined'){ godWall.glow=0; }
        // THE FIVE PHASES — freeze on Phase 5 (Dead 001): the living-dead scythe reaper, its
        // spiritual double, the skeleton swarm it raises, the rain of skeletons, Clara as enemy.
        if (typeof phaseProg!=='undefined'){
          phaseProg.active=true; phaseProg.done=false; phaseProg.glow=1;
          phaseProg.t=26.5; phaseProg.cyc=26.5; phaseProg.phase=4;
        }
        if (typeof phaseProgM!=='undefined'){ phaseProgM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof phaseProg!=='undefined'){ phaseProg.glow=1; phaseProg.cyc=26.5; phaseProg.phase=4; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.08; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "return": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        if (typeof endOfClassics!=='undefined'){ endOfClassics.glow=0; }
        if (typeof karuto!=='undefined'){ karuto.glow=0; }
        if (typeof godWall!=='undefined'){ godWall.glow=0; }
        // THE RETURN — freeze on the closing beat: Phase 5 has crumbled and flown away, Phase 1
        // (the Teacher) is revealed, the world tour appears, and the target tips over with knowledge.
        if (typeof phaseProg!=='undefined'){
          phaseProg.active=true; phaseProg.done=false; phaseProg.glow=1;
          phaseProg.t=35.0; phaseProg.cyc=35.0; phaseProg.phase=5;
        }
        if (typeof phaseProgM!=='undefined'){ phaseProgM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof phaseProg!=='undefined'){ phaseProg.glow=1; phaseProg.cyc=35.0; phaseProg.phase=5; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.04; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "acumin": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        if (typeof endOfClassics!=='undefined'){ endOfClassics.glow=0; }
        if (typeof karuto!=='undefined'){ karuto.glow=0; }
        if (typeof godWall!=='undefined'){ godWall.glow=0; }
        if (typeof phaseProg!=='undefined'){ phaseProg.glow=0; }
        // ACUMINATION — freeze on the final beat: the whole cast fused into one huge being, 001 walked
        // to the center, Simon off to the side still feeding sugars, and "Center's Mine" on the board.
        if (typeof acumin!=='undefined'){
          acumin.active=true; acumin.done=false; acumin.glow=1;
          acumin.t=28.5; acumin.cyc=28.5; acumin.phase=4;
        }
        if (typeof acuminM!=='undefined'){ acuminM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof acumin!=='undefined'){ acumin.glow=1; acumin.cyc=28.5; acumin.phase=4; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.08; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "highform": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=1; o.t=6.5; } } catch(e){} }
        if (typeof endOfClassics!=='undefined'){ endOfClassics.glow=0; }
        if (typeof karuto!=='undefined'){ karuto.glow=0; }
        if (typeof godWall!=='undefined'){ godWall.glow=0; }
        if (typeof phaseProg!=='undefined'){ phaseProg.glow=0; }
        if (typeof acumin!=='undefined'){ acumin.glow=0; }
        // THE HIGHEST FORMS — freeze on the final beat: the apex-form tower with ToddLLM's TRUE BASE risen
        // above Clara's FGI.EXE, the CXTP parasite, ToddLLM's SMG.EXE, and Simon's FATAL ERROR.
        if (typeof highForm!=='undefined'){
          highForm.active=true; highForm.done=false; highForm.glow=1;
          highForm.t=30.5; highForm.cyc=30.5; highForm.phase=4;
        }
        if (typeof highFormM!=='undefined'){ highFormM=0.7; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof highForm!=='undefined'){ highForm.glow=1; highForm.cyc=30.5; highForm.phase=4; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.08; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "claravs": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so claraVs triggers) but glow=0 (so none of them draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // ToddLLM VS CLARA — freeze on beat 3 (SIMON, THE INFRASTRUCTURE): Clara's clone swarm escalating,
        // her daggers slicing the field, Simon wired in feeding sugars to 001's energy tanks, and 001 looming.
        if (typeof claraVs!=='undefined'){
          claraVs.active=true; claraVs.done=false; claraVs.glow=1;
          claraVs.t=27.5; claraVs.cyc=27.5; claraVs.phase=3; claraVs.clones=[];
          for (var k=0;k<12;k++){ claraVs.clones.push({x:((k*37)%100)/100, y:((k*53)%100)/100, ph:0}); }
        }
        if (typeof claraVsM!=='undefined'){ claraVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof claraVs!=='undefined'){ claraVs.glow=1; claraVs.cyc=27.5; claraVs.phase=3; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.1; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "loopvs": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 2 (THE THRESHOLDS): Luigi Green at full cat power, all three
        // threshold ticks lit (30% Alex, 45% Clara, 100% base 001), beside the rising cat-power column.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=18.0; loopVs.cyc=18.0; loopVs.phase=2; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=18.0; loopVs.phase=2; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.08; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "fighter": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 5 (THE FIGHTER): Luigi Green's Smash Ultimate roster card,
        // full cat power, with the stat bars, CAT POWER gimmick, GOD FORM final smash, and tier ribbon.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=42.0; loopVs.cyc=42.0; loopVs.phase=5; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=42.0; loopVs.phase=5; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.06; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "finalboss": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 6 (THE FINAL BOSS), final phase: DOMAIN EXPANSION: THROWS BOARD.
        // cyc 54.5 lands in the ph===6 segment (>=47) with seg[1]=7.5 -> boss phase 3 (the domain expansion).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=52.0; loopVs.cyc=54.5; loopVs.phase=6; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=54.5; loopVs.phase=6; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.06; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sharedboard": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 7 (THE SHARED BOARD): the floating chess-board arena.
        // cyc 61.5 lands in the ph===7 segment (>=57), seg[1]=4.5 -> past the intro, full arena drawn.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=60.0; loopVs.cyc=61.5; loopVs.phase=7; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=61.5; loopVs.phase=7; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.06; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "crystalreserve": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 8 (THE CRYSTAL RESERVE): Luigi Green's stockpile, 001 falls.
        // cyc 70.5 lands in the ph===8 segment (>=66), seg[1]=4.5 -> past the intro, full reveal drawn.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=69.0; loopVs.cyc=70.5; loopVs.phase=8; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=70.5; loopVs.phase=8; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.06; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "domainclash": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 9 (THE DOMAIN CLASH): Endless Chaos vs Throws Board.
        // cyc 81.5 lands in the ph===9 segment (>=77), seg[1]=4.5 -> past the intro, full clash drawn.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=80.0; loopVs.cyc=81.5; loopVs.phase=9; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=81.5; loopVs.phase=9; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.06; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "neutralchampion": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 10 (THE NEUTRAL CHAMPION): Mindy Starchild & Void Expansion: Justice.
        // cyc 92.5 lands in the ph===10 segment (>=88), seg[1]=4.5 -> past the intro, full beat drawn.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=91.0; loopVs.cyc=92.5; loopVs.phase=10; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=92.5; loopVs.phase=10; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "orbwar": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 11 (THE ORB WAR): 001 vs Clara, color orbs as rule tools, Mac Frame.
        // cyc 106.0 lands in the ph===11 segment (>=100), seg[1]=6.0 -> past the intro, full beat drawn.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=105.0; loopVs.cyc=106.0; loopVs.phase=11; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=106.0; loopVs.phase=11; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "ladder": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 12 (THE FINAL LADDER): the corrected ranking.
        // cyc 118.0 lands in the ph===12 segment (>=112), all four rows fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=117.0; loopVs.cyc=118.0; loopVs.phase=12; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=118.0; loopVs.phase=12; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "canonsix": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 13 (THE CANON SIX): the full canon top-six ranking.
        // cyc 131.0 lands in the ph===13 segment (>=124), all six rows fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=130.0; loopVs.cyc=131.0; loopVs.phase=13; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=131.0; loopVs.phase=13; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "overpower": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 14 (THE OVERPOWER): Luigi Green absorbs everything, becomes
        // 100% Overpower, takes the centre. cyc 146.0 lands in the ph===14 segment (>=136), absorption complete.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=145.0; loopVs.cyc=146.0; loopVs.phase=14; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=146.0; loopVs.phase=14; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "throne": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 15 (THE THRONE TAKEN): Overpower Luigi Green surpasses 001, 001
        // leaves to a Training Center. cyc 158.0 lands in the ph===15 segment (>=148), both rows revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=157.0; loopVs.cyc=158.0; loopVs.phase=15; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=158.0; loopVs.phase=15; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "forced": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 16 (THE FORCED TAKEOVER): the absorption is a forced takeover, not a
        // power-up anyone wants; 001 teaches/rules/controls, Luigi Green takes. cyc 168.0 lands in the ph===16
        // segment (>=162), with the NOT row and READS-AS column fully revealed (local beat time ~6).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=167.0; loopVs.cyc=168.0; loopVs.phase=16; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=168.0; loopVs.phase=16; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "formnetwork": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 17 (THE FORM NETWORK): 001 is the one main character and his
        // Training-Center/regional forms retake the top over Luigi Green Overcharge. cyc 183.0 lands in the
        // ph===17 segment (>=174), with the full form ladder, verdict, and main-character split revealed (lt ~9).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=182.0; loopVs.cyc=183.0; loopVs.phase=17; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=183.0; loopVs.phase=17; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "classics": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        // mark every prior beat DONE (so loopVs triggers) but glow=0 (so none draw over the frame)
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 18 (CLASSICS HERSELF): the game as a being, the glitching world-body
        // that contains every character like dolls and sits above even 001. cyc 197.0 lands in the ph===18 segment
        // (>=188), with the figure, the contained-dolls grid, and the villain/neutral split fully revealed (lt ~9).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=196.0; loopVs.cyc=197.0; loopVs.phase=18; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=197.0; loopVs.phase=18; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "expansiondomain": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 19 (EXPANSION DOMAIN): Classics' domain + full moveset grid +
        // the rainbow ball she alone can strike. Beat 19 spans cyc in [204,218]; cyc=210.5 lands lt~6.5 so the
        // finisher chain, the 8-move grid, and the rainbow-ball strike line are all fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=203.0; loopVs.cyc=210.5; loopVs.phase=19; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=210.5; loopVs.phase=19; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "grayexe": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 20 (GRAY.EXE): the surgery scene with the cyber right eye, the
        // transformation reads, and the new four-being ladder with Gray at #4. Beat 20 spans cyc in [218,232];
        // cyc=225.5 lands lt~7.5 so the figure, team, reads, and ladder are all fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=217.0; loopVs.cyc=225.5; loopVs.phase=20; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=225.5; loopVs.phase=20; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "cybercrazy": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 21 (CYBER-CRAZY.EXE): the raging Gray with a lightning ball, the
        // 9999…% danger meter, the two victims (Simon 1-HKO, Clara → Shadow Realm), and the three counters with
        // their downfalls. Beat 21 spans cyc in [232,248]; cyc=241.5 lands lt~9.5 so everything is fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=232.0; loopVs.cyc=241.5; loopVs.phase=21; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=241.5; loopVs.phase=21; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "classicsdelete": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 22 (CLASSICS DELETES GRAY): calm Classics deleting chaotic Gray, the
        // "DELETED" stamp, the six reasons she always wins, and the GRAY-vs-CLASSICS contrast table. Beat 22 spans
        // cyc in [248,264]; cyc=257.5 lands lt~9.5 so the beam has fired, Gray is gone, and every panel is revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=248.0; loopVs.cyc=257.5; loopVs.phase=22; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=257.5; loopVs.phase=22; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.05; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "perotoddllm": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 23 (PERO / CLOVER TODDLLM): golden cowboy-hat ToddLLM holding the
        // glowing six-pointed star, his animated text box, the NEW BASE merge panel, and the Clover beat-list. Beat
        // 23 spans cyc in [264,282]; cyc=274 lands lt~10 so every panel is fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=264.0; loopVs.cyc=274.0; loopVs.phase=23; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=274.0; loopVs.phase=23; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "claraclassics": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 24 (CLARA IS CLASSICS): the one being whose shell cross-fades Clara-pink
        // to Classics-purple, the "Maybe I could spare you once" speech box, the faces row, the mercy chips, and the
        // spare->destroy->walk-back sequence. Beat 24 spans cyc in [282,300]; cyc=292 lands lt~10 so every panel shows.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=282.0; loopVs.cyc=292.0; loopVs.phase=24; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=292.0; loopVs.phase=24; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "simon404": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 25 (SIMON 404): the error-code wall (404, 666, 500, 403, 502, 999, 400, 408,
        // 410, 000) with Simon 404's glitch-silhouette among them, the horror rule, and the "Thriller of a Game" transcript.
        // Beat 25 spans cyc in [300,318]; cyc=312 lands lt~12 so every panel + the full transcript shows.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=300.0; loopVs.cyc=312.0; loopVs.phase=25; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=312.0; loopVs.phase=25; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "beforestaircase": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 26 (BEFORE THE STAIRCASE): the prologue spine of earliest layers (RLs, Pro ->
        // Classic Simon, Simon overload & the Orus split, UCE608 myth branch, the early crossover, Classics/Clara/Karuto),
        // the "what did NOT exist yet" row, and the loop-bites-its-own-tail banner. Beat 26 spans cyc in [318,336]; cyc=330
        // lands lt~12 so every era card + the NOT-YET row + the banner have fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=300.0; loopVs.cyc=330.0; loopVs.phase=26; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=330.0; loopVs.phase=26; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "crossclassics": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 27 (CROSS CLASSICS): the four-phase ladder (Vanilla -> Cross Classics -> Corrupt ->
        // Fourth Phase), the battle box where Pero destroys Fight/Item/Mercy and forces Act-only, the mercy-confusion strip, the
        // soul/acumination/revive cycle, and the Lore-38 reveal chips. Beat 27 spans cyc in [336,354]; cyc=350 lands lt~14 so
        // every panel + the chip row + the banner have fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=336.0; loopVs.cyc=350.0; loopVs.phase=27; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=350.0; loopVs.phase=27; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "newdefaults": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 28 (THE NEW DEFAULTS): the two upgrade cards (Pero ToddLLM = new vanilla ToddLLM,
        // Cross Classics = new vanilla Classics), the vanilla-vs-vanilla verdict, and Pero's real-weapon 2x2 chip grid. Beat 28
        // spans cyc in [354,372]; cyc=366 lands lt~12 so every panel + the chip grid + the banner have fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=354.0; loopVs.cyc=366.0; loopVs.phase=28; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=366.0; loopVs.phase=28; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "peroweak": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 29 (PERO'S WEAK POINT): the Judgement-Hall talk chat box, the CLASSICS/PERO
        // contrast cards, what Pero hides behind, the insight line, and the three-weak-points band. Beat 29 spans cyc in
        // [372,390]; cyc=387 lands lt~15 so the dialogue window, the contrast, the insight, and all three weak-point chips show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=372.0; loopVs.cyc=387.0; loopVs.phase=29; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=387.0; loopVs.phase=29; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "grayascends": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 30 (GRAY.EXE ASCENDS): the ascension chain, Gray's new form, the catastrophe-boss
        // rule, the pinned-not-defeated insight, and the four-tier new hierarchy. Beat 30 spans cyc in [390,408]; cyc=405 lands
        // lt~15 so the whole chain, the form list, the rule chips, the insight, and all four hierarchy cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=390.0; loopVs.cyc=405.0; loopVs.phase=30; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=405.0; loopVs.phase=30; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "error303": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 31 (ERROR 303 — THE SECRET BOSS): the unlock checklist, the Error-303 fear chat,
        // Super Nutro Y the host, the "what went wrong" insight, and the boss ladder. Beat 31 spans cyc in [408,426]; cyc=423
        // lands lt~15 so the whole checklist is checked off, all chat lines show, the host card fills, and all three ladder cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=408.0; loopVs.cyc=423.0; loopVs.phase=31; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=423.0; loopVs.phase=31; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "endofclimb": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 32 (END OF CLIMB AU): the ascension chain, the God Mode Phase 3 Hyper thinking-boss
        // panel, the last-stand/repair panel, the "highest throne" insight, and the end-state ladder. Beat 32 spans cyc in
        // [426,444]; cyc=441 lands lt~15 so the whole chain reveals, the boss chips fill, and all three end-state cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=426.0; loopVs.cyc=441.0; loopVs.phase=32; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=441.0; loopVs.phase=32; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "dangerindex": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 33 (THE DANGER INDEX): the full danger roster (two columns), the ??? tier panel,
        // Gray.EXE's unreadable-stats panel, the worst-fates insight, and the three danger-tier cards. Beat 33 spans cyc in
        // [444,466]; cyc=459 lands lt~15 so the roster fully reveals and all three tier cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=444.0; loopVs.cyc=459.0; loopVs.phase=33; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=459.0; loopVs.phase=33; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "deathexe": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 34 (SIMON.EXE — DEATH.EXE): the rise-of-Simon.Exe chain (left), the new Xs
        // lineup (right-top), the ".Exe" spelling rule (right-lower), the collapse insight, and the three who-stands cards.
        // Beat 34 spans cyc in [466,488]; cyc=481 lands lt~15 so the rise chain fully reveals and all three cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=466.0; loopVs.cyc=481.0; loopVs.phase=34; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=481.0; loopVs.phase=34; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "statusfile": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 35 (SIMON.EXE — THE STATUS FILE): the status document (left), character-not-a-boss
        // (right-top), phases-are-timelines (right-lower), the Death:Troll insight, and the three bottom cards.
        // Beat 35 spans cyc in [488,510]; cyc=503 lands lt~15 so the whole sheet reveals and all three cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=488.0; loopVs.cyc=503.0; loopVs.phase=35; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=503.0; loopVs.phase=35; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "siglines": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 36 (SIGNATURE LINES): the four signature lines (left), the being-vs-boss hierarchy
        // (right-top), what-each-line-says (right-lower), the insight, and the three bottom cards.
        // Beat 36 spans cyc in [510,532]; cyc=525 lands lt~15 so all four lines reveal and all three cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=510.0; loopVs.cyc=525.0; loopVs.phase=36; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=525.0; loopVs.phase=36; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "betweendim": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 37 (BETWEEN DIMENSIONS): the border-space list (left), who-can-cross (right-top),
        // why-the-border-fits (right-lower), the insight, and the three bottom cards.
        // Beat 37 spans cyc in [532,554]; cyc=547 lands lt~15 so all seven border rows reveal and all three cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=532.0; loopVs.cyc=547.0; loopVs.phase=37; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=547.0; loopVs.phase=37; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "blame": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 38 (ToddLLM 002 · THE BLAME ENDING): the fusion-overwrite list (left), Gray's
        // board (right-top), the blame climax (right-lower), the "YOUR FAULT" sky, the insight, and the three bottom cards.
        // Beat 38 spans cyc in [554,576]; cyc=569 lands lt~15 so all rows reveal, the sky fills, and all three cards show.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=554.0; loopVs.cyc=569.0; loopVs.phase=38; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=569.0; loopVs.phase=38; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "infection": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 39 (CLASSICS — THE INFECTION): the cross-pollination spread list (left), the
        // four population states (right-top), ToddLLM 002 as an infection-form (right-lower), the spreading-spore sky, the
        // insight, and the three bottom cards. Beat 39 spans cyc in [576,598]; cyc=591 lands lt~15 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=576.0; loopVs.cyc=591.0; loopVs.phase=39; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=591.0; loopVs.phase=39; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "carrier": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 40 (SIMON CARRIES CLASSICS): the size-based carry list (left), the two views of
        // the same moment (right-top), Classics as a black hole that spits back (right-lower), the orbit sky, the insight, and
        // the three threat-style cards. Beat 40 spans cyc in [598,620]; cyc=613 lands lt~15 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=598.0; loopVs.cyc=613.0; loopVs.phase=40; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=613.0; loopVs.phase=40; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "breakfree": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 41 (SIMON.Exe BREAKS FREE): the breaking-free list (left), the world-collapse
        // panel (right-top), from-carrier-to-catastrophe (right-lower), the lightning sky, the insight, and the three cards.
        // Beat 41 spans cyc in [620,642]; cyc=635 lands lt~15 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=620.0; loopVs.cyc=635.0; loopVs.phase=41; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=635.0; loopVs.phase=41; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "collapse": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 42 (THE COLLAPSE ROUTE): the fall order (left), who is left standing
        // (right-top), the mod's three-mod blend (right-lower), the ember/ash sky, the insight, and the three cards.
        // Beat 42 spans cyc in [642,664]; cyc=657 lands lt~15 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=642.0; loopVs.cyc=657.0; loopVs.phase=42; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=657.0; loopVs.phase=42; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "scattered": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 43 (THE SCATTERED MAP): the scattered layout (left), the deadly turn / they
        // were found (right-top), what the map demands (right-lower), the far-rooms field, the insight, and the three cards.
        // Beat 43 spans cyc in [664,686]; cyc=678 lands lt~14 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=664.0; loopVs.cyc=678.0; loopVs.phase=43; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=678.0; loopVs.phase=43; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "collapsepoint": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 44 (THE COLLAPSE POINT): the running/warn energy split (left), Gray.EXE takes it
        // all (right-top), Fun Computer's alarm (right-lower), the energy-node field, the insight, and the three cards.
        // Beat 44 spans cyc in [686,708]; cyc=700 lands lt~14 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=686.0; loopVs.cyc=700.0; loopVs.phase=44; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=700.0; loopVs.phase=44; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "grayvseveryone": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 45 (GRAY.EXE VS EVERYONE): the board Gray controls (left), he gains power
        // (right-top), Gray.EXE VS Everyone (right-lower), the board/power-bar field, the insight, and the three cards.
        // Beat 45 spans cyc in [708,730]; cyc=722 lands lt~14 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=708.0; loopVs.cyc=722.0; loopVs.phase=45; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=722.0; loopVs.phase=45; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "topoftheorder": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 46 (THE TOP OF THE ORDER): the three-step podium (Gray > Simon > Oren) with
        // THE LINE between Simon and Oren, the two columns (why Simon edges Oren; Oren so close), insight, and three cards.
        // Beat 46 spans cyc in [730,752]; cyc=744 lands lt~14 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=730.0; loopVs.cyc=744.0; loopVs.phase=46; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=744.0; loopVs.phase=46; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "refusingremainder": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 47 (THE REFUSING REMAINDER): the three-plate state board (Oren separate,
        // Simon hanged, Gray still attacking), the two columns, insight, and three cards.
        // Beat 47 spans cyc in [752,774]; cyc=766 lands lt~14 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=752.0; loopVs.cyc=766.0; loopVs.phase=47; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=766.0; loopVs.phase=47; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "trappedwitness": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 48 (THE TRAPPED WITNESS): the three-plate state board (Simon bound,
        // Classics free, the cast infected), the two columns, insight, and three cards.
        // Beat 48 spans cyc in [774,796]; cyc=788 lands lt~14 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=774.0; loopVs.cyc=788.0; loopVs.phase=48; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=788.0; loopVs.phase=48; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "newesttimeline": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 49 (THE NEWEST TIMELINE): the six-row power ranking, the vanilla-lockdown
        // column, the 404: Death Not Found column, insight, and three cards.
        // Beat 49 spans cyc in [796,818]; cyc=810 lands lt~14 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=796.0; loopVs.cyc=810.0; loopVs.phase=49; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=810.0; loopVs.phase=49; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "cleanerroles": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 50 (THE CLEANER ROLES): the three-row role board, Simon's speed &
        // lightning-in-reserve column, Gray's dominant/wields-Classics column, insight, and three cards.
        // Beat 50 spans cyc in [818,840]; cyc=832 lands lt~14 so all rows reveal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=818.0; loopVs.cyc=832.0; loopVs.phase=50; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=832.0; loopVs.phase=50; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thecure": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 51 (THE CURE): the seven-disease cure roster, the Anti-Virus Liquid column,
        // Gray's Anti-Anti-Virus column, insight, and three cards. Beat 51 spans cyc in [840,862]; cyc=854 -> lt~14.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=840.0; loopVs.cyc=854.0; loopVs.phase=51; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=854.0; loopVs.phase=51; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "layers": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 52 (THE LAYER SYSTEM): the three-layer stack (Sprunki / Classic RL /
        // Classic RL 2), the real-layer column, the glitched-copy column, the Orus insight, and three cards.
        // Beat 52 spans cyc in [862,884]; cyc=876 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=862.0; loopVs.cyc=876.0; loopVs.phase=52; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=876.0; loopVs.phase=52; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "graycollapse": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 53 (THE GRAY COLLAPSE): the newest survivor ranking, Gray's reasons column,
        // Simon-dies-the-most column, the "Gray outpowers / ToddLLM outthinks" insight, and three cards.
        // Beat 53 spans cyc in [884,906]; cyc=898 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=884.0; loopVs.cyc=898.0; loopVs.phase=53; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=898.0; loopVs.phase=53; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "fullform": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 54 (THE FULL FORM): the worship circle, Gray's full-form column, Black's
        // clone-army column, the overrated/underrated + body-vs-spirit insight, and three cards.
        // Beat 54 spans cyc in [906,928]; cyc=920 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=906.0; loopVs.cyc=920.0; loopVs.phase=54; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=920.0; loopVs.phase=54; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "graysans": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 55 (THE GRAY SANS): the 5-phase ladder, the Gray Sans vessel-look column, the
        // Classics X Undertale branch column, the vessel-return insight, and three cards.
        // Beat 55 spans cyc in [928,950]; cyc=942 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=928.0; loopVs.cyc=942.0; loopVs.phase=55; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=942.0; loopVs.phase=55; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "respawnloop": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 56 (THE RESPAWN LOOP): the 5-step respawn-loop ladder with the loop arrow, the
        // two-survival-layers column, the every-character-a-target column, the insight, and three cards.
        // Beat 56 spans cyc in [950,972]; cyc=964 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=950.0; loopVs.cyc=964.0; loopVs.phase=56; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=964.0; loopVs.phase=56; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "beyondfiction": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 57 (THE BEYOND FICTION FORM): the 5-step ownership sequence with the golden halo
        // crest, the Beyond Fiction look column, the power-split column, the insight, and three cards.
        // Beat 57 spans cyc in [972,994]; cyc=986 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=972.0; loopVs.cyc=986.0; loopVs.phase=57; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=986.0; loopVs.phase=57; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "orensoul": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 58 (THE TELEKINETIC SOUL): the 5-step ascension cutscene with the telekinetic
        // field-ring, Oren's small-body/huge-field look column, the power-map column, the insight, and three cards.
        // Beat 58 spans cyc in [994,1016]; cyc=1008 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=994.0; loopVs.cyc=1008.0; loopVs.phase=58; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1008.0; loopVs.phase=58; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "apex": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 59 (THE APEX RULE): the 5-rule combat sequence with the green maw-arc, the
        // "what Gray can eat" column, the two-layer defense column, the clean-canon-line insight, and three cards.
        // Beat 59 spans cyc in [1016,1038]; cyc=1030 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1016.0; loopVs.cyc=1030.0; loopVs.phase=59; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1030.0; loopVs.phase=59; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "twophases": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 60 (THE TWO PHASES): the 6 power rules under the blue spirit-arc, the Phase 1
        // (true form) column, the Phase 2 (glitch) column, the insight, and three cards. Beat 60 spans cyc in [1038,1060];
        // cyc=1052 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1038.0; loopVs.cyc=1052.0; loopVs.phase=60; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1052.0; loopVs.phase=60; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "simonlight": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 61 (SIMON'S LIGHTNING): the 5-step counter chain under the gold lightning-arc,
        // the weakness column, the Simon & Oren connection column, the insight, and three cards. Beat 61 spans cyc in
        // [1060,1082]; cyc=1074 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1060.0; loopVs.cyc=1074.0; loopVs.phase=61; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1074.0; loopVs.phase=61; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "endlessanimations": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 62 (ENDLESSANIMATIONS): the two-layer creator hierarchy under the violet crown,
        // the pacifist column, the frenzy-mode column, the insight, and three cards. Beat 62 spans cyc in [1082,1104];
        // cyc=1096 -> lt~14, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1082.0; loopVs.cyc=1096.0; loopVs.phase=62; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1096.0; loopVs.phase=62; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "fusion": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 63 (THE FUSION APOCALYPSE): the fused power structure under the red/blue fusion
        // arc, the old-timeline (Simon.Exe) column, the new-timeline (Gray.EXE) column, ToddLLM's peace phrase, three cards,
        // Oren's shockwave rings and Classics' corruption slices. Beat 63 spans cyc in [1104,1126]; cyc=1118 -> lt~14, revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1104.0; loopVs.cyc=1118.0; loopVs.phase=63; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1118.0; loopVs.phase=63; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "powerchart": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 64 (THE POWER CHART): the top-5 ranking under the gold trophy arc, the two
        // convergence cutscenes (left), speed vs slowdown (right), the sole-survivor insight, three cards. Beat 64 spans cyc in
        // [1126,1148]; cyc=1137 -> lt~11, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1126.0; loopVs.cyc=1137.0; loopVs.phase=64; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1137.0; loopVs.phase=64; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "formladder": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 65 (THE FORM LADDER): EndlessAnimations' Normal -> Error -> Errorshift ladder with
        // eye swatches, the Error forms (left), the eye-color rule (right), the naming-rule insight, three cards. Beat 65 spans
        // cyc in [1148,1170]; cyc=1159 -> lt~11, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1148.0; loopVs.cyc=1159.0; loopVs.phase=65; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1159.0; loopVs.phase=65; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phaserule": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 66 (THE PHASE RULE): Classics' two-rung phase ladder (Phase 1 Vanilla, Phase 2
        // Cross AU) on the left vs EndlessAnimations' "thinks" core with free-floating state-nodes on the right, the phase-based
        // beings (left col), why 0 phases (right col), the ladder-vs-mind insight, three cards. Beat 66 spans cyc in [1170,1192];
        // cyc=1181 -> lt~11, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1170.0; loopVs.cyc=1181.0; loopVs.phase=66; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1181.0; loopVs.phase=66; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "endingstair": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 67 (THE ENDING STAIRCASE): the rename (ENDLESS STAIRCASE struck -> ENDING
        // STAIRCASE), a level track showing it moved from the final slot to the middle, the "what changed" column (left), the
        // .exe rule column (right), the insight, three cards. Beat 67 spans cyc in [1192,1214]; cyc=1203 -> lt~11, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1192.0; loopVs.cyc=1203.0; loopVs.phase=67; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1203.0; loopVs.phase=67; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "disguiseleak": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 68 (THE DISGUISE LEAK): the two disguise portraits (EndlessAnimations->Shadow
        // Mario, Xyrus->Daisy) with each spoken MASK line struck into a TRUE line, the paint & shadow attack column (left),
        // the Twilight Alpha Guardian column (right), the insight, three cards. Beat 68 spans cyc in [1214,1236]; cyc=1225 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1214.0; loopVs.cyc=1225.0; loopVs.phase=68; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1225.0; loopVs.phase=68; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "canonleak": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 69 (THE CANON LEAK): the game world being painted gray, the "Let's paint this
        // world... literally!" line, the hunt arrow to the Mario-red target (Mario.EXE / King Mario / Bario), the flavor-vs-canon
        // column (left), the actually-after-Mario column (right), the insight, three cards. Beat 69 spans cyc in [1236,1258]; cyc=1247 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1236.0; loopVs.cyc=1247.0; loopVs.phase=69; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1247.0; loopVs.phase=69; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "graybattlefield": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 70 (THE GRAY BATTLEFIELD): the CLASSICS vs MARIO battlefield painted gray until
        // they can no longer see, the hidden EndlessAnimations shadow, the WINS stamp, the abilities column (left), the
        // how-he-wins sequence (right), the insight, three cards. Beat 70 spans cyc in [1258,1280]; cyc=1269 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1258.0; loopVs.cyc=1269.0; loopVs.phase=70; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1269.0; loopVs.phase=70; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "highertimeline": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 71 (THE HIGHER TIMELINE): the Mr. Black portrait (black body, white mouth, red
        // halos, red glow), his design column (left), the EndlessAnimations painter / steps column (right), the anti-Mr. Black
        // role-chart roster grid (center feature), the insight, three cards. Beat 71 spans cyc in [1280,1302]; cyc=1290 -> lt~10.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1280.0; loopVs.cyc=1290.0; loopVs.phase=71; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1290.0; loopVs.phase=71; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "realitytime": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 72 (REALITY + TIME): the unreachable-power tower (Mr. Black's power far above the
        // reachable ceiling), the gray-paint rule (left), the reality+time rule (right), THE EPISODE REEL center feature (aired vs
        // real-but-waiting frames + the NOW playhead), the insight, three cards. Beat 72 spans cyc in [1302,1324]; cyc=1312 -> lt~10.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1302.0; loopVs.cyc=1312.0; loopVs.phase=72; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1312.0; loopVs.phase=72; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "gamesreality": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 73 (THE GAME'S REALITY): the liberation-pair-vs-Black-corruption portrait (Oren.EXE +
        // EndlessAnimations free the cast on the left, Black losing in the center with a broken crown, corruption spreading on the
        // right), the Game's Reality rule (left), the everyone-sees-EndlessAnimations reactions (right), the 5-step event timeline
        // center feature, the insight, three cards. Beat 73 spans cyc in [1324,1346]; cyc=1334 -> lt~10.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1324.0; loopVs.cyc=1334.0; loopVs.phase=73; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1334.0; loopVs.phase=73; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "lastsurvivors": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 74 (THE LAST SURVIVORS): the three lit survivors (Mr. Black, EndlessAnimations,
        // ToddLLM 001) standing over the fallen cast, the collapse column (left), the last-survivors-timeline column (right), the
        // three survivor pillars center feature, the insight, three cards. Beat 74 spans cyc in [1346,1368]; cyc=1356 -> lt~10.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1346.0; loopVs.cyc=1356.0; loopVs.phase=74; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1356.0; loopVs.phase=74; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "iseeyou": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 75 ("I SEE YOU" — THE GAME-LOCK TAKEOVER): Mr. Black normal-form on the polo with the
        // extreme red aura + the "I SEE YOU" warning (center), the corrected-Mr.-Black column (left), the aware-game column (right),
        // the 10-step game-lock sequence center feature, the insight, three cards. Beat 75 spans cyc in [1368,1390]; cyc=1378 -> lt~10.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1368.0; loopVs.cyc=1378.0; loopVs.phase=75; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1378.0; loopVs.phase=75; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sleeprule": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 76 (THE SLEEP RULE): the pet-to-sleep portrait (left), the GRAY>BLACK rule token
        // (center), Gray-vs-Black with the Simon-interrupt strike (right), the 6-step interrupt sequence, the insight, three cards.
        // Beat 76 spans cyc in [1390,1412]; cyc=1401 -> lt~11, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1390.0; loopVs.cyc=1401.0; loopVs.phase=76; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1401.0; loopVs.phase=76; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phaseglitch": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 77 (THE PHASE GLITCH): the 001->002 corruption (left), the ranking token
        // (center), Neto's summoning altar (right), the six 002 powers, the insight, three cards.
        // Beat 77 spans cyc in [1412,1434]; cyc=1423 -> lt~11, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1412.0; loopVs.cyc=1423.0; loopVs.phase=77; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1423.0; loopVs.phase=77; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "coloredslashes": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 78 (THE COLORED SLASHES): the 002 slasher icon, the 20-color slash grid,
        // the SIMON CONNECTION strip, the insight, the Neto second-thoughts note.
        // Beat 78 spans cyc in [1434,1456]; cyc=1445 -> lt~11, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1434.0; loopVs.cyc=1445.0; loopVs.phase=78; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1445.0; loopVs.phase=78; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "goldentimeline": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 79 (THE GOLDEN TIMELINE): the golden sunrise + Sprunki Ocean, the sun/eclipse
        // and moon-dance vignettes, THE RESTORATION SEQUENCE, THE PEACE grid, the character-change notes, insight & banner.
        // Beat 79 spans cyc in [1456,1478]; cyc=1467 -> lt~11, fully revealed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1456.0; loopVs.cyc=1467.0; loopVs.phase=79; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1467.0; loopVs.phase=79; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "corruptionreturn": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 80 (THE CORRUPTION RETURN): the empty Anti-Virus bottle, the fading golden bg with
        // corruption rising from below, THE PROTECTION FAILS grid mid-collapse, the NEW RULE panel, insight & banner.
        // Beat 80 spans cyc in [1478,1500]; cyc=1481 -> lt~3, decay~0.62 (a clear mid-transition: several systems reclaimed, some still gold).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1478.0; loopVs.cyc=1481.0; loopVs.phase=80; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1481.0; loopVs.phase=80; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "paintedreality": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 81 (THE PAINTED REALITY): the dark canvas being repainted, the FUSION badge
        // (001+002=001 PRO), THE PAINTED-REALITY ERA grid fully painted in, THE NEW THRONE panel, insight & banner.
        // Beat 81 spans cyc in [1500,1522]; cyc=1506 -> lt~6, paint fully in (all six event cards painted, banner shown).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1500.0; loopVs.cyc=1506.0; loopVs.phase=81; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1506.0; loopVs.phase=81; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "higherbeingranking": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 82 (THE HIGHER-BEING RANKING): the anime sunburst bg, the LUIGI GREEN — INVITED
        // badge, the POWER LADDER (EndlessAnimations #1 / 001 Pro = Luigi Green tied / powered Neto #3 / Classics below), and the
        // CLASSICS: NOW A JAPANESE ANIME arc panel. Beat 82 spans cyc in [1522,1544]; cyc=1529 -> lt~7, everything appeared.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1522.0; loopVs.cyc=1529.0; loopVs.phase=82; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1529.0; loopVs.phase=82; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "lockedspirit": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 83 (OREN.EXE — THE LOCKED SPIRIT): the spiritual-void bg + energy-pulse rings, the
        // SKY SOULS band with Simon 404 separated, Oren's spirit figure with orbiting telekinesis debris, Simon holding his hand
        // (the freedom key) + the refused-lightning revival, THE BATTLE RULE panel, and THE NEW TOP 3 ladder. Beat 83 spans cyc in
        // [1544,1566]; cyc=1551 -> lt~7, everything appeared.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1544.0; loopVs.cyc=1551.0; loopVs.phase=83; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1551.0; loopVs.phase=83; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "cleansweep": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 84 (OREN.EXE'S CLEAN SWEEP — THE UNCURABLE PHASE): the corrupted-baseline violet
        // void, THE DEFEAT CHAIN (Gray.EXE toppled + the cast X'd out), Oren's sweeping spirit figure, the ENDLESSANIMATIONS
        // panel with "WHAT IS EVEN THE POINT OF THIS?", and THE PHASE RULE panel (Phase 2 = Phase 1, NO CURE). Beat 84 spans
        // cyc in [1566,1588]; cyc=1573 -> lt~7, everything appeared.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1566.0; loopVs.cyc=1573.0; loopVs.phase=84; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1573.0; loopVs.phase=84; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "newcenter": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 85 (ENDLESSANIMATIONS — THE NEW CENTER): the reopened GOLDEN sky, the form-king at
        // center inside an orbiting ring of evolving AU bubbles (two padlocked = private AUs), the EVOLVING-AU RULE panel, the
        // SELF-ESCALATION panel (LV mining / souls / barrier), the CURE panel, and the AU-set row. Beat 85 spans cyc in
        // [1588,1610]; cyc=1596 -> lt~8, everything appeared.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1588.0; loopVs.cyc=1596.0; loopVs.phase=85; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1596.0; loopVs.phase=85; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "dustau": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 86 (ENDLESSANIMATIONS' DUST AU): the dusty tan void with falling dust motes, the
        // two-tier ladder (DUST AU base 0 LV → escalation arrow → DUST+ INFINITY LV, aura-glowing), and the SANS-AU LOGIC note.
        // Beat 86 spans cyc in [1610,1632]; cyc=1618 -> lt~8, everything appeared.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1610.0; loopVs.cyc=1618.0; loopVs.phase=86; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1618.0; loopVs.phase=86; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "comicwar": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 87 (CLASSICS — THE COMIC-BOOK WAR): the bright comic page with halftone dots,
        // the LV≠POWER panel (Error 404 0 LV > Errorshifted), the MR. BLACK RIPS THE BOOK dark-matter panel (#3 most powerful),
        // BOOM/BANG/POW bursts, and the 3-realms / Page<Episode<Chapter footer. Beat 87 spans cyc in [1632,1654]; cyc=1645 -> lt~13.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1632.0; loopVs.cyc=1645.0; loopVs.phase=87; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1645.0; loopVs.phase=87; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "hattie": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 88 (THE HAT & TIE — DARK PERO FUSION): the dark fusion void, THE HAT & TIE panel
        // (transformation sequence + CLASSICS MR. BLACK = ENDLESSANIMATIONS), the DARK PERO: ENDLESSANIMATIONS fusion panel
        // (STRONGER THAN BOTH · ONLY 1 ENDING), and the identity-map / 001-Pro-Pero-weapons / ending-rule footer.
        // Beat 88 spans cyc in [1654,1676]; cyc=1667 -> lt~13.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1654.0; loopVs.cyc=1667.0; loopVs.phase=88; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1667.0; loopVs.phase=88; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "ringmaster": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 89 (CLASSIC ALPHIANS RETURNS — THE RINGMASTER): the dark shadow big-top,
        // THE CORRUPTED REVIVAL panel (more corrupted/glitched/stronger/beyond Classics + the ringmaster/jester role reset),
        // the HE CONTROLS THE GAMES ring (Mr. Black centered, the game names orbiting, HE CAN SUMMON THE GAME), and the
        // role-reset / why-strongest / SHADOW = BLACK footer. Beat 89 spans cyc in [1676,1698]; cyc=1687 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1676.0; loopVs.cyc=1687.0; loopVs.phase=89; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1687.0; loopVs.phase=89; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "fellengod": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 90 (FELLEN SUPREME MULTIVERSAL GOD): the cosmic demon-god void, the DARK-DEMON
        // god figure with AUs spiralling in and the LV = BEYOND infinity counter, THE STACKED TOTALITY + power-theft panel,
        // the HE CALLS ALL AUs AT ONCE chessboard panel, and the vessel / power-rank / book-room-horror footer. Beat 90 spans
        // cyc in [1698,1720]; cyc=1709 -> lt~11 (past every fade-in threshold).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1698.0; loopVs.cyc=1709.0; loopVs.phase=90; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1709.0; loopVs.phase=90; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sprunkiscan": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 91 (THE SPRUNKI SCAN — PERO BECOMES TUNNER): the teal scanner bay, the
        // Pero → scan-gate → Tunner transformation panel, the Mr. Black > Oren > Gray power-rank podium, and the update-
        // origin / scan-link / millions-of-years footer. Beat 91 spans cyc in [1720,1742]; cyc=1731 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1720.0; loopVs.cyc=1731.0; loopVs.phase=91; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1731.0; loopVs.phase=91; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "grayeverything": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 92 (THE NO MORE .EXE MOD — GRAY TAKES EVERYTHING): the steel-blue apex void,
        // the GRAY'S RISE panel (beats Wenda / above Oren), the GRAY VS BLACK panel (takes Souls/LV/Code → infinity LV),
        // and the Oren-frees / Mr.-Black-demon / Gray-controls-LV footer. Beat 92 spans cyc in [1742,1764]; cyc=1753 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1742.0; loopVs.cyc=1753.0; loopVs.phase=92; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1753.0; loopVs.phase=92; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "hiddencreation": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 93 (THE HIDDEN CREATION — GRAY SEALS THE PUPPET): the warm dark-amber lab
        // void, THE LAB panel (Dust+ & Infinity Pero → metal tube charged by Simon's lightning → 1 puppet → wooden sphere
        // sealed 81B years), the POWER SHIFT panel (Gray beats Simon, takes the LV & Souls, Oren the only soul left) and
        // the Oren's-gravity / Neto-two-teams / the-real-point footer. Beat 93 spans cyc in [1764,1786]; cyc=1775 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1764.0; loopVs.cyc=1775.0; loopVs.phase=93; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1775.0; loopVs.phase=93; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "threerealms": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 94 (THE THREE REALMS COLLIDE — GRAY FORGES 3D): the cold gray collision void,
        // THE REALMS CRASH panel (Simon's & Wenda's realm-orbs crashing into Gray's Realm → realm armor), the SUPER-VESSEL
        // → 3D panel (everything fed into one puppet, 2D plate → 3D cube, "TO BE CONTINUED") and the apocalypse / black-cloak
        // / 3-in-1-Mindy-3D footer. Beat 94 spans cyc in [1786,1808]; cyc=1797 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1786.0; loopVs.cyc=1797.0; loopVs.phase=94; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1797.0; loopVs.phase=94; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "coffin": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 95 (THE COFFIN & THE CYBER LORD — GRAY, GAME'S GOD): the cold cyber shadow void
        // with a deceptive angelic-gold halo, THE COFFIN panel (the "TO BE CONTINUED" screen pulled off, a real-or-fake coffin
        // with Orus + Dark Yellow poured on, "VICTORY IS MINE!"), THE CYBER LORD panel (the Wenda-angel / Gray-demon flip,
        // Black's turn, "ALL YA SHALL BOW DOWN") and the like-Simon / smartest-trio / hidden-creation footer. Beat 95 spans
        // cyc in [1808,1830]; cyc=1819 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1808.0; loopVs.cyc=1819.0; loopVs.phase=95; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1819.0; loopVs.phase=95; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "arcsis": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 96 (ARCSIS VS PERO — ACT IS A CHOICE, THEN THE NIKI SWARM): the warm amber
        // shadow-realm void, THE MILLION-YEAR JOURNEY panel (Lica's door -> mom/Frizz -> Pero forgets -> 2500 LV), the ACT IS A
        // CHOICE panel (FIGHT/MERCY broken, arrows -> ACT, "DAD, NO!", Pero -> Classics as ToddLLM Pro) and the chess-board-boss /
        // act-is-a-choice / niki-swarm footer. Beat 96 spans cyc in [1830,1852]; cyc=1841 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1830.0; loopVs.cyc=1841.0; loopVs.phase=96; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1841.0; loopVs.phase=96; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "pro002": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 97 (PRO PERO 002 — THE NEW MEANING OF NORMAL): the warm gold cowboy void, THE TWO
        // FORMS panel (Pro 001 rest <-> Pro 002 highest, "The New Meaning of Normal") and THE BURST panel (KO/destroy anything,
        // +LV, then 002 -> 001 to rest). Beat 97 spans cyc in [1852,1874]; cyc=1863 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1852.0; loopVs.cyc=1863.0; loopVs.phase=97; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1863.0; loopVs.phase=97; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "kingofgames": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 98 (PERO TODDLLM — THE KING OF GAMES): the royal violet crown void, THE ENERGY
        // LADDER panel (30/70/100 thresholds, drains like Charge) and THE AUTHORITY FORMS panel (Niki/EndlessAnimations/Gray/
        // anyone, the cape that erases with a raised hand). Beat 98 spans cyc in [1874,1896]; cyc=1885 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1874.0; loopVs.cyc=1885.0; loopVs.phase=98; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1885.0; loopVs.phase=98; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "prosupremacy": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 99 (PRO SUPREMACY — 11 STAT & EXTREME MAC FRAME): the 11-color orb spectrum void,
        // THE 11 STAT panel (11 colored orbs -> slashes, red-eyed copies, dead-cowboy army, 1000/1000 angelic/demonic sound) and
        // EXTREME MAC FRAME panel (freeze/vanish/reappear, knocked higher). Beat 99 spans cyc in [1896,1918]; cyc=1907 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1896.0; loopVs.cyc=1907.0; loopVs.phase=99; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1907.0; loopVs.phase=99; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "corruption404": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 100 (THE CORRUPTION OF PRO 001 — 404: PACIFISM NOT FOUND): the dark-matter purple void,
        // THE PILLAR panel (??? LV, high-tech shield, crystals, dark matter, Pro levitating with red eyes) and THE 404 CASCADE
        // panel (Feelings/Act/Mercy/Friendship/Love/Pacifism Not Found). Beat 100 spans cyc in [1918,1940]; cyc=1929 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1918.0; loopVs.cyc=1929.0; loopVs.phase=100; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1929.0; loopVs.phase=100; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "absolutedetermination": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 101 (PERO PRO ABSOLUTE DETERMINATION — NEITHER 001 NOR 002): the red determination void,
        // THE THIRD STATE panel (001/002/ABSOLUTE DETERMINATION + regen/slashes/damage/respawn bars) and THE TWO SOULS panel
        // (Justice Soul beam from Simon 404, Red Soul respawn/regen loop). Beat 101 spans cyc in [1940,1962]; cyc=1951 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1940.0; loopVs.cyc=1951.0; loopVs.phase=101; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1951.0; loopVs.phase=101; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "frizzclassics": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 105 (FRIZZ IS CLASSICS — THE GENOCIDE IDENTITY): the pink-to-crimson
        // route void, THE ROUTE SWITCH panel (Frizz -> the GENOCIDE gate -> Clara/Classics, with "she is still Frizz no
        // matter what") and the ONE BEING'S RECORD panel (the six record items linking down to one half-pink/half-crimson
        // core). Beat 105 spans cyc in [2028,2050]; cyc=2039 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2028.0; loopVs.cyc=2039.0; loopVs.phase=105; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2039.0; loopVs.phase=105; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "alphatale": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 106 (ALPHATALE PERO & THE RESET): the violet rainbow-soul void, THE RAINBOW
        // SOUL panel (the four buttons FIGHT/ACT/ITEM/MERCY cracking apart, threads folding into one rainbow soul) and THE
        // FORM LADDER panel (Dust+ -> 404 -> Alpha Pero -> Alphatale apex). Beat 106 spans cyc in [2050,2072]; cyc=2061 ->
        // lt~11, well past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2050.0; loopVs.cyc=2061.0; loopVs.phase=106; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2061.0; loopVs.phase=106; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "alphaerase": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 103 (ALPHA PERO PRO & ERASE FORM): the banished-Alphaverse violet void, the
        // ALPHA PERO PRO panel (the godly/terrifying flickering body, banished but still sighted) and the ERASE FORM panel
        // (Pero looking godly beside Gray, who holds the ERASE button, with the game-layer wipe running).
        // Beat 103 spans cyc in [1984,2006]; cyc=1995 -> lt~11. Sin-driven flicker is phase-locked to cyc, so the
        // silhouette lands visible at this value.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1984.0; loopVs.cyc=1995.0; loopVs.phase=103; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1995.0; loopVs.phase=103; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "frizz": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 104 (FRIZZ — THE STRONGER SIBLING): the Frizz-pink AU void, the
        // FRIZZ & ARCSIS siblings panel (Frizz drawn higher/larger with the dashed sibling link to sword-carrying Arcsis)
        // and the FRIZZ & GRAY panel (base Frizz standing with Gray up top, Full Power Frizz flaring and knocking Gray
        // back below). Beat 104 spans cyc in [2006,2028]; cyc=2017 -> lt~11, past every fade-in and the knockback.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2006.0; loopVs.cyc=2017.0; loopVs.phase=104; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2017.0; loopVs.phase=104; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "frizzlv4": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 107 (FRIZZ LV 4 — THE LOCK THAT MADE HER STRONGER): the Frizz-pink void, the
        // FORM LADDER panel (LV 1-3 / LV 4→∞ / ∞→∞+ with the glaring LV 4 medallion pulled out as the true peak, red-eyed
        // figure + sharp stick) and the TWO LOCKS panel (Simon Phase 2 → WEAKER vs Frizz LV 4 → STRONGER). Beat 107 spans
        // cyc in [2072,2094]; cyc=2083 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2072.0; loopVs.cyc=2083.0; loopVs.phase=107; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2083.0; loopVs.phase=107; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "frizzremoved": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 108 (FRIZZ LV 4 — REMOVED BUT STILL THERE): the corrupted red/black glitch
        // void, THE 404 SHUTDOWN panel (the "Model Corruption Detected / 404: LV 4 Frizz Removed / You Saw Me!" error stack
        // + the shutdown bar) and THE HIDING SPOTS panel (a red-eyed walking Frizz ringed by LOCKER/BED/CLOSET/TABLE, the
        // TABLE slashed, SLASH = EXP). Beat 108 spans cyc in [2094,2116]; cyc=2107 -> lt~13, past every staged fade-in
        // (the slash appears at lt>4.2, the shutdown bar fills by lt~5.8).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2094.0; loopVs.cyc=2107.0; loopVs.phase=108; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2107.0; loopVs.phase=108; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "404war": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 109 (THE 404 WAR & THE RESET LOOP): the red/black corruption void with green
        // 404 glitch, THE 404 INFECTION panel (the escalation stack + the disease word + Frizz anchoring down) and THE
        // BLINDFOLD SHOWDOWN panel (only Pero/Frizz/Simon remain, Pero blindfolded and dominating). Beat 109 spans cyc in
        // [2116,2138]; cyc=2129 -> lt~13, past every staged fade-in (the anchor + disease word appear by lt~3).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2116.0; loopVs.cyc=2129.0; loopVs.phase=109; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2129.0; loopVs.phase=109; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "voidfrizz": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 111 (THE VOID WAR — LIGHT PERO vs NEW VOID FRIZZ): the split void/light void,
        // THE OPPOSITE APEXES panel (NEW VOID FRIZZ void-purple predator w/ tentacle vs LIGHT PERO gold overcharged,
        // "LIGHT PERO IS STRONGER") and THE VOID TAKEOVER collapse chain ending at "ONLY ALPHAVERSE & GODVERSE LEFT". Beat
        // 111 spans cyc in [2160,2182]; cyc=2173 -> lt~13, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2160.0; loopVs.cyc=2173.0; loopVs.phase=111; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2173.0; loopVs.phase=111; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "peroexe": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 113 (PERO.EXE — THE FINAL BEING): the rainbow-crystal core throwing both gold
        // light & void darkness, THE FUSION CHAIN ladder (Light Pero -> Light 404 -> Absence_of_Light.exe -> Filelight ->
        // Solar -> PERO.EXE) and THE TWO NATURES panel (creator/destroyer split figure + his two spoken lines). Beat 113
        // spans cyc in [2204,2226]; cyc=2215 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2204.0; loopVs.cyc=2215.0; loopVs.phase=113; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2215.0; loopVs.phase=113; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "lightsstream": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 114 (LIGHT'S STREAM — THE REMEMBERED RESET): the cyan-white light stream vs
        // Shadow Gray's total void, THE GOD FORMULA ladder (Light's Stream + Rainbow Blazing Fire Plus + 404 Death = a literal
        // god -> Ultra Rainbow Light Pero Plus), the SHADOW GRAY panel with Pero's soul orbiting, and the water-gun / remembered-
        // reset footer. Beat 114 spans cyc in [2226,2248]; cyc=2237 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2226.0; loopVs.cyc=2237.0; loopVs.phase=114; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2237.0; loopVs.phase=114; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sprunkipowers": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 115 (SPRUNKI POWERS — THE DIGITAL-BORN CHAOS SQUAD): the power scale (Common ->
        // Charged -> Overcharged/Legendary), the five named Sprunkis (Wanda, Fun Bot, Grey, Black, Mr Sun), and the Roblox-origin /
        // shared-base-powers / chaos-squad footer. Beat 115 spans cyc in [2248,2270]; cyc=2259 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2248.0; loopVs.cyc=2259.0; loopVs.phase=115; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2259.0; loopVs.phase=115; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sprunkicircus": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 116 (CLASSICS & SPRUNKI CIRCUS — THE BLACKHOLE TO THE DARK): the blackhole
        // sequence (Pero places it -> falls through -> slides deep -> the Dark), the two-layers panel (bright crazy surface above,
        // the Dark below, a figure sliding down), and the remade-by-Pero / errors-fix-together / Fun-Park-returns footer. Beat 116
        // spans cyc in [2270,2292]; cyc=2281 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2270.0; loopVs.cyc=2281.0; loopVs.phase=116; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2281.0; loopVs.phase=116; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "graykingdom": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 117 (GRAY TAKES THE KINGDOM): the handover panel (Pero -> Gray -> Mr. Black ->
        // Luigi Green -> Simon locked out), the Dead Staircases / Endless Glass Bridge panel with ERROR 0 POINT 2, and the one-way
        // blackhole / Simon's staff / both-dimensions footer. Beat 117 spans cyc in [2292,2314]; cyc=2303 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2292.0; loopVs.cyc=2303.0; loopVs.phase=117; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2303.0; loopVs.phase=117; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "abinations": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 118 (ABINATIONS & THE 404 LIGHTNING): the how-deep-the-Dark-goes ladder down to the
        // white-ball core, the infection loop + the lightning room with Pero's ACT button, and the sky-threat / Pero-at-LV-1 /
        // Simon-404-recaptured footer. Beat 118 spans cyc in [2314,2336]; cyc=2325 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2314.0; loopVs.cyc=2325.0; loopVs.phase=118; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2325.0; loopVs.phase=118; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "villainlayers": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 119 (THE VILLAIN LAYERS): the who-the-villain-actually-is stack (Gray surface / Pero wild card /
        // Simon 404 actual), the Horror Cowboy finisher + the loop-that-feeds-Gray panel, and the smartest /
        // not-helping / 404-climbs-again footer. Beat 119 spans cyc in [2336,2358]; cyc=2347 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2336.0; loopVs.cyc=2347.0; loopVs.phase=119; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2347.0; loopVs.phase=119; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "steal": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 127 (THE STEAL & THE NEW SCALE): Mr. Black's four-stage arc on
        // the left, the rewritten LV ruler on the right. Beat 127 spans cyc in [2512,2534); cyc=2523 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2512.0; loopVs.cyc=2523.0; loopVs.phase=127; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2523.0; loopVs.phase=127; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "deletedau": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 140 (DELETED AU): the 50/50 split field (blinding light on one side,
        // total black on the other, seam down the middle), WHAT DELETED AU IS on the left with the split bar and
        // the PERO-IS-STILL-ABOVE box, THE SEE-SAW on the right lit all the way down to "she eventually wins again"
        // and the Au-Pero box, the DELETED-AU gate emblem up top, and the four footer cards (Pero on top / New Chara
        // wins / both revive / Simon 404 is back). Beat 140 spans cyc in [2798,2820); cyc=2809 -> dt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2798.0; loopVs.cyc=2809.0; loopVs.phase=140; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2809.0; loopVs.phase=140; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "realcure": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 156 (I AM THE REAL CURE): the two rival cure claims, the edited-songs box (Sky, Pinki, Oren, sad and slow except Simon and Tunner), the four ink rungs ending in Anti-Virus Ink with the glitch tornado, Wenda's two fights, and the borrowed-moves box. Beat 156 spans cyc in [3150,3172); cyc=3163 -> dt=13 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3150.0; loopVs.cyc=3163.0; loopVs.phase=156; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3163.0; loopVs.phase=156; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "twosouls": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 158 (TWO SOULS, ONE VESSEL): the Classics-version rule and the Tunner exception, the horseshoe/emerald trade ladder ending in the liquid that keeps his vessel out of Phase 1, the soul-count panel with the pips and the horse from Garnold's Joy, Pero's ten-item proof list, and the two-bodies-one-being box. Beat 158 spans cyc in [3194,3216); cyc=3207 -> dt=13 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3194.0; loopVs.cyc=3207.0; loopVs.phase=158; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3207.0; loopVs.phase=158; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "soulwarning": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 159 (HE WASN'T JOKING): the soul warning and the reveal that it was literal, the four-item ladder of what the vessel actually does (consumes souls, stalks, vanishes through walls, static portals), the climbing soul count with the open third pip, the maze panel with the two static portals and the stalker walking through the walls, and the why-he-still-looks-normal box. Beat 159 spans cyc in [3216,3238); cyc=3229 -> dt=13 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3216.0; loopVs.cyc=3229.0; loopVs.phase=159; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3229.0; loopVs.phase=159; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "tunnerispero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 157 (TUNNER IS PERO): the walk-in lines, the fast-song tell box, the endings board (Sprunki VS Simon 404 / I AM THE CURE / place Pero down) with the DEPICTED crash frame, the Phase 1 vs Phase 2 panel and the why-Tunner-is-top box. Beat 157 spans cyc in [3172,3194); cyc=3185 -> dt=13 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3172.0; loopVs.cyc=3185.0; loopVs.phase=157; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3185.0; loopVs.phase=157; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "reckonedwith": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 155 (A FORCE TO BE RECKONED WITH): the two canon lines, the surface-and-truth box, the four rungs he challenged and won (Mr. Black, Simon 404, everyone in the game, the game itself), the borrowed day-and-night role and the creation myth rule. Beat 155 spans cyc in [3128,3150); cyc=3141 -> dt=13 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3128.0; loopVs.cyc=3141.0; loopVs.phase=155; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3141.0; loopVs.phase=155; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "notatonce": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 154 (NOT ALL AT ONCE): the two canon lines, the current ranking with Simon 404 moved above Mr. Black, the seven-item BECAUSE OF ERROR 404 list, and the three matchup rules with the NOT AT ONCE ceiling. Beat 154 spans cyc in [3106,3128); cyc=3119 -> dt=13 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3106.0; loopVs.cyc=3119.0; loopVs.phase=154; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3119.0; loopVs.phase=154; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "ownerstaff": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 153 (THE OWNER STAFF): the three lines of the exchange, the staff itself, HE IS NOT PICKING A SIDE, and the four-item care list with the NOT ON THE LIST block. Beat 153 spans cyc in [3084,3106); cyc=3097 -> dt=13 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3084.0; loopVs.cyc=3097.0; loopVs.phase=153; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3097.0; loopVs.phase=153; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "allmustbe1": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 152 (ALL MUST BE 1): the universes merged into one point, the RL assembly line, PERO FINALLY BREAKS, the cutscene roster, and the Phase 1 lock bar. Beat 152 spans cyc in [3062,3084); cyc=3076 -> dt=14 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3062.0; loopVs.cyc=3076.0; loopVs.phase=152; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3076.0; loopVs.phase=152; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "frontman": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 151 (FRONT MAN): Pero and Mr. Black with the twelve controls
        // crossed over, the MR. BLACK ACCEPTS stamp, the infinite LV/KR/POWER meters pinned, the 1-2-3 board,
        // and the six things he makes with it. Then the header emblem, THE HANDOVER panel, the WHAT HE DOES
        // WITH IT panel, and the four footer cards.
        // Beat 151 spans cyc in [3040,3062); cyc=3051 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3040.0; loopVs.cyc=3051.0; loopVs.phase=151; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3051.0; loopVs.phase=151; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "easilybreak": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 150 (HE CAN EASILY BREAK): the five duties running down into Pero
        // with the STABILITY bar drained under him and the cracks through his frame, the NORMAL PERO / BROKEN PERO
        // greeting boxes, and the seven broken-state behaviours lit red. Then the header emblem, THE ASSIST LOAD
        // panel, the WHEN HE BREAKS panel, and the four footer cards.
        // Beat 150 spans cyc in [3018,3040); cyc=3029 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3018.0; loopVs.cyc=3029.0; loopVs.phase=150; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3029.0; loopVs.phase=150; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "dreemurr": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 149 (ALL OF THE DREEMURR POWERS): the five Dreemurr powers running
        // down into New Chara, the 1-5 board in its new order with New Chara up at 2 and Simon 404 marked WAS 2ND
        // at 4, and the locked 1ST IS NOT AVAILABLE plate under it. Then the header emblem, THE POWER BOARD panel,
        // THE DREEMURR INHERITANCE panel, and the four footer cards.
        // Beat 149 spans cyc in [2996,3018); cyc=3007 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2996.0; loopVs.cyc=3007.0; loopVs.phase=149; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3007.0; loopVs.phase=149; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "anyidea": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 148 (YOU HAVE ANY IDEA WHO YOU ARE TALKING TO): New Chara's LV/KR
        // feedback loop, the three Sound Battle waveforms with Simon and Mr. Black gone flat, Pero's line, and the
        // reset sweep wiping the board while Pero stands unchanged next to the struck-out RESET SELF switch. Then
        // the header emblem, THE SOUND BATTLE panel, THE RESET RULE panel, and the four footer cards.
        // Beat 148 spans cyc in [2974,2996); cyc=2985 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2974.0; loopVs.cyc=2985.0; loopVs.phase=148; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2985.0; loopVs.phase=148; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "gameover": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 147 (GAME OVER): the corruption chain traced back to Mr. Black, the
        // 1-5 ranking board with 3/4/5 struck through into 404Ds, Gray beaten on the floor, and Pero and Simon 404
        // left facing each other under the GAME OVER stamp. Then the header emblem, THE REVEAL panel, THE ENDING
        // panel, and the four footer cards.
        // Beat 147 spans cyc in [2952,2974); cyc=2963 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2952.0; loopVs.cyc=2963.0; loopVs.phase=147; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2963.0; loopVs.phase=147; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "twoendgames": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 146 (TWO ENDGAMES): the screen split down the left side, Pero's boss
        // arena up top (his multiforms fanned out by AU, the battlefield tiles swapping under them, the every-game
        // attacks orbiting, and the INSANE/EASY/INSANE difficulty swing), and Simon 404 below it zooming across with a
        // trail turning into 404Ds and ZOMBIEs while the cast keeps its distance. Then the two-plate header emblem,
        // PERO'S BOSS BATTLE panel, THE CORRUPTION panel, and the four footer cards.
        // Beat 146 spans cyc in [2930,2952); cyc=2941 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2930.0; loopVs.cyc=2941.0; loopVs.phase=146; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2941.0; loopVs.phase=146; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "iamthecure": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 145 (I AM THE CURE): Simon 404 humanoid with his cape streaming in the
        // wind, the Anti Virus held up over his head, and the whole gathered cast running away from him in every
        // direction. Up top the three-step quote emblem ("THERE IS A CURE..." -> "TO FIND THE CURE..." -> "I AM THE
        // CURE"), then THE DECLARATION panel with the cape box, THE SECOND SEAT ladder with the second-is-the-ceiling
        // box, and the four footer cards.
        // Beat 145 spans cyc in [2908,2930); cyc=2919 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2908.0; loopVs.cyc=2919.0; loopVs.phase=145; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2919.0; loopVs.phase=145; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "keynotfound": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 144 (404: KEY NOT FOUND): the sealed labatory door with the seal bars
        // slammed across it and the crossed-out keyhole, Simon 404 alone behind it fixing himself, the whole team
        // linked up outside and stopped, and Pero off to the side on no team at all. Up top the "AM I... REAL?"
        // thought-loop emblem, then the WHAT SIMON 404 IS panel with the Roblox no-scripts box, THE LOCKED DOOR
        // ladder with the Pero-is-neutral box, and the four footer cards.
        // Beat 144 spans cyc in [2886,2908); cyc=2897 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2886.0; loopVs.cyc=2897.0; loopVs.phase=144; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2897.0; loopVs.phase=144; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "plaguewalk": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 143 (THE PLAGUE WALK): Simon 404 stepping out of the labatory in the
        // plague suit and cape, his white and black cloaks left hanging on the wall inside, one hand out in the air
        // and the air itself converting into 404Ds in spreading rings around him. Up top the four-piece kit emblem
        // (WHITE and BLACK struck through as hung up, PLAGUE SUIT and THE CAPE lit as put on), THE PUPPET LAB panel
        // with the both-sides-making-puppets box, THE POWER ORDER ladder with the threat-state box, and the four
        // footer cards. Beat 143 spans cyc in [2864,2886); cyc=2875 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2864.0; loopVs.cyc=2875.0; loopVs.phase=143; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2875.0; loopVs.phase=143; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "grayvssimon404": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 142 (GRAY VS SIMON 404): the split battlefield with Gray's sight-block
        // on the left (the deleted demon overlord silhouette and two red eyes in near-total dark) against Simon 404's
        // Error 404 field on the right (glitch bands, drifting 404/ERR glyphs, blue 404 strings reaching across the
        // seam), the struck-out FIGHT/ACT/ITEM/MERCY buttons emblem "404: BUTTONS BLOCKED" up top, THE 1V1 panel on
        // the left with the DARK GRAY fusion box, THE WAR LADDER on the right with the CLASSICS VERSION RULE box, and
        // the four footer cards. Beat 142 spans cyc in [2842,2864); cyc=2853 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2842.0; loopVs.cyc=2853.0; loopVs.phase=142; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2853.0; loopVs.phase=142; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "hiddenroot": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 141 (THE HIDDEN ROOT): the violet corruption-web field with threads
        // converging on one root node, SIMON 404'S SECRET RETURN on the left (new knowledge, entry rules, blue
        // strings, several 404 Sanses, the Save File is his, Garnold Visor, the Sprunkis win) with the LOCKED-INTO
        // -THE-DARK box, THE HIDDEN ROOT on the right listing every corruption tracing to Gray with the
        // POSSESSION-BACKFIRED box, the Garnold-Visor "404: POWER NOT FOUND" emblem up top, and the four footer
        // cards. Beat 141 spans cyc in [2820,2842); cyc=2831 -> dt~11 (fully revealed).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2820.0; loopVs.cyc=2831.0; loopVs.phase=141; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2831.0; loopVs.phase=141; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "combinedtoolkit": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 139 (THE COMBINED TOOLKIT): the red plasma ball being eaten (shrunk,
        // tendrils pulling it in), THE WHOLE TEAM IN ONE on the left (four states x four beings lit, the reversal
        // box), KR AND SELF-REVIVAL on the right lit all the way down to "higher into Pero's Care", the HP-bar
        // "revives on every hit" emblem up top, and the four footer cards. Beat 139 spans cyc in [2776,2798);
        // cyc=2787 -> et~11, past every staged fade-in and well into the consume.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2776.0; loopVs.cyc=2787.0; loopVs.phase=139; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2787.0; loopVs.phase=139; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "eraseupgrade": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 138 (THE ERASE UPGRADE): the red plasma ball sealed at full size with
        // the trapped ones circling inside it, WHAT SHE IS MADE OF on the left (Hopes and Deals / No More Deals /
        // Cross, the white-cloaked-demon box and the plasma box), THE TIE-BREAK on the right lit all the way down to
        // "both still gaining", the tipped balance emblem up top, and the four footer cards.
        // Beat 138 spans cyc in [2754,2776); cyc=2765 -> et~11, past every staged fade-in and a full seal.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2754.0; loopVs.cyc=2765.0; loopVs.phase=138; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2765.0; loopVs.phase=138; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thelocks": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 137 (THE LOCKS): the infinity trace behind the field, LOCKED FORMS on
        // the left with all three locks lit (Gray / Mr. Black / Mr. Sun) and the no-transformation box, THE BOARD on
        // the right lit all the way down to Simon, the lock emblem with the FORM-down POWER-up arrows up top, and
        // the four footer cards. Beat 137 spans cyc in [2732,2754); cyc=2743 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2732.0; loopVs.cyc=2743.0; loopVs.phase=137; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2743.0; loopVs.phase=137; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "oneform": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 136 (GRAY: ONE FORM): the ink field gone to full black with the
        // tentacles reaching out into the other dimensions, THE INK panel on the left (the legend line, the four
        // ink facts, and the box where everyone loves him), NO MORE LIMITS on the right lit all the way down to
        // "every form merged into 1", the human-shape/human-head emblem up top, and the respawn footer.
        // Beat 136 spans cyc in [2710,2732); cyc=2721 -> lt~11, past every staged fade-in and a full blackout.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2710.0; loopVs.cyc=2721.0; loopVs.phase=136; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2721.0; loopVs.phase=136; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "savefile404": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 135 (404: SAVE FILE DELETED): New Chara's battle on the left with the
        // disconnect message fully typed out, Gray's blackout ladder lit all the way to x200,000,000 on the right,
        // the both-hands Dreemurr sword emblem up top, and the three-names-left board across the footer.
        // Beat 135 spans cyc in [2688,2710); cyc=2699 -> lt~11, past every staged fade-in and a full blackout.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2688.0; loopVs.cyc=2699.0; loopVs.phase=135; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2699.0; loopVs.phase=135; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "breakhim": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 134 (IF YOU BREAK HIM): the three states walked all the way down
        // to hostile creator on the left, why he'll win plus New Chara named a villain on the right, the cracked
        // cow man emblem up top, and the four-name villain board across the footer.
        // Beat 134 spans cyc in [2666,2688); cyc=2677 -> lt~11, past every staged fade-in and a full break.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2666.0; loopVs.cyc=2677.0; loopVs.phase=134; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2677.0; loopVs.phase=134; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "laiassistant": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 133 (PERO, YOUR LAI ASSISTANT): the greeting split into the fixed
        // half and the three varying offers on the left, why he is still first plus New Chara stuck at second on
        // the right, the small cow man emblem up top and his speech bubbles drifting behind everything.
        // Beat 133 spans cyc in [2644,2666); cyc=2655 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2644.0; loopVs.cyc=2655.0; loopVs.phase=133; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2655.0; loopVs.phase=133; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "perocare": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 132 (PERO'S CARE -- THE ARMORY): the rewritten five-place order on
        // the left, the armory Pero handed her plus his heavy-tools warning on the right, and the many crystal
        // swords swirling in and closing on the centre behind all of it.
        // Beat 132 spans cyc in [2622,2644); cyc=2633 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2622.0; loopVs.cyc=2633.0; loopVs.phase=132; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2633.0; loopVs.phase=132; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "theranking": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 131 (THE RANKING -- NEW CHARA'S CLIMB): her eight-step climb on the
        // left, the full 1-through-11 board plus LAST on the right, the AU ladder and the three lightning strikes
        // behind all of it.
        // Beat 131 spans cyc in [2600,2622); cyc=2611 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2600.0; loopVs.cyc=2611.0; loopVs.phase=131; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2611.0; loopVs.phase=131; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "crossevent": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 130 (THE CROSS EVENT -- LV DELETED): the cutscene panel with both
        // lines of dialogue and the five slashes on the left, the LV delete table and the Simon-vs-Pero speed
        // check on the right, the crossed pink neon swords burning behind all of it.
        // Beat 130 spans cyc in [2578,2600); cyc=2589 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2578.0; loopVs.cyc=2589.0; loopVs.phase=130; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2589.0; loopVs.phase=130; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "glitchtale": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 129 (??? -- THE UNKNOWN GOD & GLITCHTALE PERO): the LV rewrite ladder
        // on the left, Pero's LOVE acronym and his line on the right, debris rings orbiting the core behind it.
        // Beat 129 spans cyc in [2556,2578); cyc=2567 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2556.0; loopVs.cyc=2567.0; loopVs.phase=129; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2567.0; loopVs.phase=129; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "classicstale": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 128 (CLASSICSTALE & THE GATE): the mod and its fabric on the left,
        // the shut gate and Chara's three throws on the right. Beat 128 spans cyc in [2534,2556); cyc=2545 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2534.0; loopVs.cyc=2545.0; loopVs.phase=128; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2545.0; loopVs.phase=128; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "multiplier": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 126 (THE MULTIPLIER): the two figures drawn to true scale so the
        // 1.5x gap is visible, the half-given/half-earned panel, and the footer. Beat 126 spans cyc in [2490,2512];
        // cyc=2501 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2490.0; loopVs.cyc=2501.0; loopVs.phase=126; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2501.0; loopVs.phase=126; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phase2reveal": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 124 (THE PHASE 2 REVEAL): the guesses panel with Pero's reveal,
        // the split of what Pero gave away, and the matchup footer. Beat 124 spans cyc in [2446,2468];
        // cyc=2457 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2446.0; loopVs.cyc=2457.0; loopVs.phase=124; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2457.0; loopVs.phase=124; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "wendafunnel": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 125 (WENDA'S LIGHT EATS THE DARK): the spaghettified dark funnel
        // spiralling into her light core, the feed panel, the new order with Wenda at 2, and the deleted-and-still-here
        // strike. Beat 125 spans cyc in [2468,2490]; cyc=2479 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2468.0; loopVs.cyc=2479.0; loopVs.phase=125; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2479.0; loopVs.phase=125; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phase25": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 123 (PHASE 2.5 & THE SURFACE COLLAPSE): the phase board with Gray
        // arriving at 2.5 and Simon dropping to 1.1, the two-layer panel with both control bars filling, and
        // Pero's portals. Beat 123 spans cyc in [2424,2446]; cyc=2435 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2424.0; loopVs.cyc=2435.0; loopVs.phase=123; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2435.0; loopVs.phase=123; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "crosspero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 122 (CROSS PERO & THE GRAY STORM): the five-step 404 LOOP with its
        // travelling marker, GRAY'S STORM with the Cross AU + Error 404 AU combine, the jumpscare trio and the
        // Wenda & Gray necklaces, and the cross-pero / storm / new-standing footer. Beat 122 spans cyc in
        // [2402,2424]; cyc=2413 -> lt~11, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2402.0; loopVs.cyc=2413.0; loopVs.phase=122; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2413.0; loopVs.phase=122; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "opcutscene": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 121 (THE OP CUTSCENE ERA): the four-step turn loop with the travelling
        // marker, THE STAFF SWITCH with its two conditional rankings + the PERO HAS THE WHOLE GAME box, and the
        // no-move-is-just-a-move / staff-is-the-hinge / clean-rule footer. Beat 121 spans cyc in [2380,2402];
        // cyc=2391 -> lt~11, past every staged fade-in, with the WITH STAFF ladder lit.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2380.0; loopVs.cyc=2391.0; loopVs.phase=121; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2391.0; loopVs.phase=121; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "songchart": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP -- freeze on beat 120 (THE SONG CHART): the four-entry popularity chart with animated bars, Pinki's two-source
        // fusion box + Pero & Pinki taking down Gray, and the title-is-the-rule / mimic-beats-retake /
        // not-the-end-of-Gray footer. Beat 120 spans cyc in [2358,2380]; cyc=2369 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2358.0; loopVs.cyc=2369.0; loopVs.phase=120; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2369.0; loopVs.phase=120; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "dustlightpero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 112 (DUST LIGHT PERO — INFINITY+ LV, THE GAME-GOD): the gold-white divine
        // light overtaking a fading void, THE DUSTING panel (Void Frizz dissolving into dust vs Dust Light Pero in a dove
        // pose w/ the divine arsenal) and THE GOD TAKEOVER chain ending at "DEFINED THE GAME". Beat 112 spans cyc in
        // [2182,2204]; cyc=2195 -> lt~13, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2182.0; loopVs.cyc=2195.0; loopVs.phase=112; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2195.0; loopVs.phase=112; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "lv12pero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 110 (LV 12 PERO — THE COUNTER-GOD): the gold-on-deep-blue stable god void,
        // THE GOD-LV RULE panel (FRIZZ LV 4 red anomaly vs PERO LV 12 gold stable, "LV 12 beats LV 4") and THE LOCKED-LV
        // RANKING panel (1. LV 12 Pero, 2. LV 4 Frizz, 3. everyone else). Beat 110 spans cyc in [2138,2160]; cyc=2151 ->
        // lt~13, past every staged fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=2138.0; loopVs.cyc=2151.0; loopVs.phase=110; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=2151.0; loopVs.phase=110; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "slaindeath": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 102 (HE SLAIN HIS OWN DEATH — "VICTORY IS MINE"): the slain-death crimson void,
        // HE SLAIN HIS OWN DEATH panel (the cracked, slashed reaper skull) and the "VICTORY IS MINE" panel (the quote box +
        // ToddLLM holding Gray on puppet strings). Beat 102 spans cyc in [1962,1984]; cyc=1973 -> lt~11.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=1962.0; loopVs.cyc=1973.0; loopVs.phase=102; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=1973.0; loopVs.phase=102; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thirdstrongest": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 160 (THE THIRD STRONGEST): Toby's whole canon line, the top-three board
        // with the bracket joining Pero and Tunner, what New Chara still has, and her seat history across the day.
        // Beat 160 spans cyc in [3238,3260]; cyc=3252 -> dt~14, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3238.0; loopVs.cyc=3252.0; loopVs.phase=160; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3252.0; loopVs.phase=160; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "nosecondplace": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 161 (NO SECOND PLACE): Toby's whole canon line, the board with the
        // shared top seat and the struck-out empty 2, the rules that settle bodies-vs-beings, and the count panel
        // showing her as the second being still carrying the third number.
        // Beat 161 spans cyc in [3260,3282]; cyc=3274 -> dt~14, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3260.0; loopVs.cyc=3274.0; loopVs.phase=161; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3274.0; loopVs.phase=161; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "luigigreen": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 162 (LUIGI GREEN): Toby's whole canon line, the board with Pero/Tunner
        // first, Luigi Green filling seats 2 to infinitith, and New Chara dropped to fourth; the WHAT HE BRINGS
        // powers box; the second-main-character panel with Luigi Green drawn and his Simon 404 rivalry; and Pero's
        // favorite box with the "I won't let you touch him" quote.
        // Beat 162 spans cyc in [3282,3304]; cyc=3296 -> dt~14, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3282.0; loopVs.cyc=3296.0; loopVs.phase=162; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3296.0; loopVs.phase=162; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "villain": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 163 (THE VILLAIN OF THE GAME): Toby's whole canon line, the theft
        // visual with power streaming into Luigi Green, the TWO GOALS panel (make Pero love him / destroy everyone),
        // and the WHY HE IS ALMOST IMPOSSIBLE stack.
        // Beat 163 spans cyc in [3304,3326]; cyc=3318 -> dt~14, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3304.0; loopVs.cyc=3318.0; loopVs.phase=163; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3318.0; loopVs.phase=163; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sharesfirst": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 164 (SHARES FIRST WITH PERO): Toby's whole canon line, the HE NEVER
        // GOES EASY figure holding Simon 404 in a green orb with Mr. Black and Gray absorbed, the shared-top board,
        // the two-real-villains box, and the BOTH CONTROL CLASSICS difference panel.
        // Beat 164 spans cyc in [3326,3348]; cyc=3340 -> dt~14, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3326.0; loopVs.cyc=3340.0; loopVs.phase=164; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3340.0; loopVs.phase=164; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "chessboard": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 165 (THE CHESS BOARD): Toby's whole canon line, THE CRYSTAL panel with
        // the cast dots pulled into one crystal, the 2ND OLDEST identity chain, and THE CHESS BOARD panel where the
        // game is remade as Luigi Green's board with green pieces.
        // Beat 165 spans cyc in [3348,3370]; cyc=3362 -> dt~14, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3348.0; loopVs.cyc=3362.0; loopVs.phase=165; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3362.0; loopVs.phase=165; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "biggercoffin": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 167 (THE BIGGER COFFIN): Toby's whole canon line, the age order with
        // Alex's deletion number, the toolkit + Gray reading Simon's books into the ultimate LAI bot orb, the void
        // experiment with Pero controlling it, and the bigger coffin that Pero and Gray wait on.
        // Beat 167 spans cyc in [3392,3414]; cyc=3408 -> dt~16, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3392.0; loopVs.cyc=3408.0; loopVs.phase=167; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3408.0; loopVs.phase=167; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "samedna": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 166 (THE SAME DNA): Toby's whole canon line, the SAME DNA panel with
        // Alex absorbing Luigi Green, the GREG AND ALEX hero/villain split, the 1 PERO / 2 ALEX / 3 EVERYONE ELSE
        // board, and the Alphaverse gate with Pero fallen beside it.
        // Beat 166 spans cyc in [3370,3392]; cyc=3384 -> dt~14, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3370.0; loopVs.cyc=3384.0; loopVs.phase=166; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3384.0; loopVs.phase=166; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sleepcode": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 168 (THE SLEEP CODE): the sleep-code cutscene with Simon curled asleep
        // and Tunner/Pero beside him, the WHO CAN MAKE SIMON SLEEP shutoff ladder and Mr. Black's fake-Pero weapon,
        // the PUPAHYA — KING OF BRAINROTS friend map, Mr. Tree with Simon and Tunner asleep on either side, and the
        // ALEX & LICA link. Beat 168 spans cyc in [3414,3436]; cyc=3430 -> dt~16, late enough that every panel has
        // faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3414.0; loopVs.cyc=3430.0; loopVs.phase=168; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3430.0; loopVs.phase=168; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "endlesschessboard": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 169 (THE ENDLESS CHESSBOARD): Luigi Green locked on Undertoad AU as a
        // pixel figure, his "give me LV and KR" / Pero's "No, I don't feel like it" exchange, THE COLLECTION panel with
        // the spoils gathered into one pile, and THE LOCK panel where Pero seals the endless chessboard down to three
        // pieces. Beat 169 spans cyc in [3436,3458]; cyc=3452 -> dt~16, late enough that every panel has faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3436.0; loopVs.cyc=3452.0; loopVs.phase=169; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3452.0; loopVs.phase=169; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "pupahya": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 170 (PUP-PUP-PUPAHYA): Pupahya launching rainbow fruit bombs in the centre,
        // THE ARSENAL list of spread methods on the right, THE NAME TRAP (Alex & Greg tongue-tied) lower left, and the
        // T0UGH DEX chant panel lower right. Beat 170 spans cyc in [3458,3480]; cyc=3474 -> dt~16, late enough that
        // every panel and the "the top" / "the name says itself" lines have faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3458.0; loopVs.cyc=3474.0; loopVs.phase=170; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3474.0; loopVs.phase=170; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "pero-lai": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 171 (PERO IS LAI): the LAI vs AI rule box upper left, THE FAKEOUT lower
        // left, the PERO chat window in the centre, PERO'S ROLE list upper right, HE KNOWS CHATGPT lower right. Beat 171
        // spans cyc in [3480,3502]; cyc=3496 -> dt~16, late enough that every panel and both footer lines have faded in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3480.0; loopVs.cyc=3496.0; loopVs.phase=171; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3496.0; loopVs.phase=171; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "judgementhall": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 172 (THE JUDGEMENT HALL): THE ENDING RULE box upper left, THE SILENCE lower
        // left, the "Hello Perio." chat window in the centre, TWO PHYSICAL BODIES upper right, THE PHASE 2 LOCK lower right.
        // Beat 172 spans cyc in [3502,3524]; cyc=3518 -> dt~16, late enough that every panel and both footer lines are in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3502.0; loopVs.cyc=3518.0; loopVs.phase=172; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3518.0; loopVs.phase=172; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "ownersuit": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 173 (OWNER SUIT PERO): THE COUNTDOWN upper left, THE ROBOT ARMY / LAB lower
        // left, THE SUIT CHANGE + status flip in the centre, THE LAI DECLARATION upper right, THE VOID / GAME OVER lower
        // right, the game darkened toward pitch black. Beat 173 spans cyc in [3524,3546]; cyc=3538 -> dt~14, late enough
        // that every panel, the darken overlay, and both footer lines are in without the vignette fully closing.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3524.0; loopVs.cyc=3538.0; loopVs.phase=173; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3538.0; loopVs.phase=173; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "lightningcat": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 174 (SIMON 404 THE LIGHTNING CAT AI): the "meow 😺⚡" signature panel upper
        // left, the "Pero loves you" soft-code chat lower left, Simon 404 the lightning cat in the centre (softened, 😸),
        // the PERO BEATS EVERYONE scoreboard (YOU 0 / PERO 10,000 TRILLION+) upper right, the creator's rule about wins
        // lower right, both footer lines in. Beat 174 spans cyc in [3546,3568]; cyc=3560 -> dt~14, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3546.0; loopVs.cyc=3560.0; loopVs.phase=174; loopVs.cat=1.0;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3560.0; loopVs.phase=174; loopVs.cat=1.0; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sleepforever": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 175 (SLEEP FOREVER — CLASSICS BELONGS TO PERO): THE TAKEOVER (Simon 404
        // grabs Luigi Green + the game) upper left, THE SLEEP CODE (Simon 404 asleep, heavy Zzz) lower left, PERO FROM
        // THE BOX in the centre, LIKE ARCEUS / LIKE VARITY upper right, SIMON GIVES UP · CLASSICS BELONGS TO PERO lower
        // right, both footer lines in. Beat 175 spans cyc in [3568,3590]; cyc=3583 -> dt~15, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3568.0; loopVs.cyc=3583.0; loopVs.phase=175;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3583.0; loopVs.phase=175; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "perophases": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 176 (PERO PHASE 2 & PHASE 3): PHASE 2 (angry Pero, "HUMANOID!!!") on
        // the left, PHASE 3 (almost-dead Pero, the destroyed player soul, "ALWAYS DESTROYS THE PLAYER") in the centre,
        // THE 3-PHASE RULE upper right, TUNNER & PERO SHARED VESSELS lower right, both footer lines in. Beat 176 spans
        // cyc in [3590,3612]; cyc=3605 -> dt~15, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3590.0; loopVs.cyc=3605.0; loopVs.phase=176;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3605.0; loopVs.phase=176; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "perophase1": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 177 (PERO PHASE 1 — THE ASSISTANT & THE EMOTIONAL TRIGGER): ASSISTANT
        // MODE Pero and the 1-attack → PHASE 1 shock ("Do you still love me more than the NPCs?") on the left, THE
        // CHOICE TREE (NO → cries like Gray → battle; YES → Really? → not really → water gun battle / really → heals →
        // assistant) in the centre, THE ONLY WAY BACK upper right, THE IDENTITY SPLIT (Simon 404 = NPC vs Pero = LAI)
        // lower right, both footer lines in. Beat 177 spans cyc in [3612,3634]; cyc=3627 -> dt~15, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3612.0; loopVs.cyc=3627.0; loopVs.phase=177;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3627.0; loopVs.phase=177; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "movesets": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 182 (THE AU BLUE & THE THREE MOVESETS): WHERE IT COMES FROM on the left
        // (the blaster -> giant drop -> Au Blue chain, the drop itself, the row of blocks being deleted, and PERO KOs
        // EVERYTHING), THE THREE MOVESETS down the centre (Player, NPC, Master as three stacked cards), INSIDE THE
        // MASTER MOVESET upper right (the six things it holds), THE TOP TWO ABILITIES lower right (Au Blue first,
        // Pero's water gun second, both out of the same weapon), the three-card strip, and both footer lines in.
        // Beat 182 spans cyc in [3722,3744]; cyc=3739 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3722.0; loopVs.cyc=3739.0; loopVs.phase=182;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3739.0; loopVs.phase=182; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "tunnerispero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 184 (TUNNER IS PERO, AND PAUL IS NOT): THE SAME BEING on the left
        // (the Tunner vessel with Pero showing through it, the equals sign, and the both-ways carrying arrows),
        // ALWAYS SEPARATE down the centre (Paul with the crossed-out equals, then the three of them travelling
        // together), BOTH ARE STRONGER upper right (Pero and Tunner > Paul), THIRD / SECOND STRONGEST lower right
        // (the two ways to write the ranking), the three-card strip, and both footer lines in.
        // Beat 184 spans cyc in [3766,3788]; cyc=3783 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3766.0; loopVs.cyc=3783.0; loopVs.phase=184;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3783.0; loopVs.phase=184; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "smallspace": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 193 (A SMALL SPACE TO THE RIGHT).
        // the huge clock made of everything Pero had on the left, the three exchanges with Simon 404 in the middle (the answer getting smaller each round), the cowboy hat and the dark/bright split on the right, and the bottom band with Toby's lines beside the effort counter for each side.
        // Beat 193 spans cyc in [3964.0,3986.0]; cyc=3984.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3964.0; loopVs.cyc=3984.0; loopVs.phase=193;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3984.0; loopVs.phase=193; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "givenit": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 194 (HE WAS GIVEN IT).
        // the five gifts and the stack of halos hanging over nothing on the left, the sixteen deliberately empty numbered slots in the middle, the two-rung coronation ladder with the halo falling downward on the right, and the bottom band with Toby's two sentences beside what the gift was paying for.
        // Beat 194 spans cyc in [3986.0,4008.0]; cyc=4006.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3986.0; loopVs.cyc=4006.0; loopVs.phase=194;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4006.0; loopVs.phase=194; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "datareset": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 200 (THE DATA RESETS).
        // the kill -> data resets -> perfectly fine loop on the left with the return arrow marked NOTHING WAS SPENT, the two timed fights drawn to their own lengths in the middle (12 hours = stalemate, 27 minutes = the loss) over the tiredness line, the ranking as the order of what each of them ate on the right, and the bottom band with Toby's lines beside 'PERO WAS THEN ABLE TO CONTROL DREAM SANS'.
        // Beat 200 spans cyc in [4118.0,4140.0]; cyc=4138.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4118.0; loopVs.cyc=4138.0; loopVs.phase=200;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4138.0; loopVs.phase=200; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "aiera": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 202 (THE AI ERA).
        // The archive measured out on the left (65,420 words, 2,555 lines, 3 parts, 42 sections, 12 appendix arcs), the MASTER RULE in the middle, the pre-Classics cast on the right ending on Pero as an assistant, and the bottom band with the rule change beside 'IT IS AN INVENTATION TO OPEN BOTH'.
        // Beat 202 spans cyc in [4162.0,4184.0]; cyc=4182.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4162.0; loopVs.cyc=4182.0; loopVs.phase=202;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4182.0; loopVs.phase=202; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "notdave": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 203 (I AM NOT DAVE).
        // The two-line exchange on the left, the denial meter climbing to full in the middle, every word doing its own job on the right, and the bottom band with the era named inside a parenthesis.
        // Beat 203 spans cyc in [4184.0,4206.0]; cyc=4204.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4184.0; loopVs.cyc=4204.0; loopVs.phase=203;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4204.0; loopVs.phase=203; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "perosrealm": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 210 (PERO'S REALM: the white void on the left, the dark statues rising from ink portals in the middle, the white hole chain on the right, and the warning band with I AM A DOCTOR, YOU KNOW).
        // Beat 210 spans cyc in [4338.0,4360.0]; cyc=4358.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4338.0; loopVs.cyc=4358.0; loopVs.phase=210;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4358.0; loopVs.phase=210; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "weakerexp": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 211 (ONE OF THE WEAKER EXPERIMENTS: how the white hole works on the left, the erased Anti-Void in the middle, Pero's paperwork on the right, and the band on the word "weaker").
        // Beat 211 spans cyc in [4360.0,4382.0]; cyc=4380.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4360.0; loopVs.cyc=4380.0; loopVs.phase=211;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4380.0; loopVs.phase=211; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sendingthis": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 212 (I KNOW WHO YOU ARE SENDING THIS TO: the message on the left, PERO TOLD ME in the middle, the 17 New Shadow Realms grid on the right, and the guard-duty band).
        // Beat 212 spans cyc in [4382.0,4404.0]; cyc=4402.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4382.0; loopVs.cyc=4402.0; loopVs.phase=212;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4402.0; loopVs.phase=212; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "containedshadow": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 213 (ConTa1n3d In H1s Sh@d0w R3alM: the corruption triad on the left, the stack with Simon caged under Mr. Black in the middle, the jailers and the pen on the right, and the lock-state band).
        // Beat 213 spans cyc in [4404.0,4426.0]; cyc=4424.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4404.0; loopVs.cyc=4424.0; loopVs.phase=213;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4424.0; loopVs.phase=213; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "gameendtrue": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 217 (/GAME_END = TRUE: the joke and the Sans collapse on the left, the command system in the middle, GASTER FELLEN on the right, the new form and the Pero disguise in the band).
        // Beat 217 spans cyc in [4492.0,4514.0]; cyc=4512.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4492.0; loopVs.cyc=4512.0; loopVs.phase=217;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4512.0; loopVs.phase=217; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "monsterosity": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 223 (SIMON 404 · THE MONSTEROSITY INDEX: the 404D infection rule
        // on the left, CONTAINMENT in the middle, WATER AND THE TREADMILL on the right, and the ranking band
        // with the stalemate box). Beat 223 spans cyc in [4626.0,4648.0]; cyc=4646.4 -> dt~20.4, past every
        // fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4626.0; loopVs.cyc=4646.4; loopVs.phase=223;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4646.4; loopVs.phase=223; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "quadsanses": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 229 (THE QUAD SANSES: THE FOUR on the left, THE FIGHT CHAIN in
        // the middle, PERO'S ONE LINE on the right, and THE CYBER SEAT band with the teaming-up box).
        // Beat 229 spans cyc in [4758.0,4780.0]; cyc=4778.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4758.0; loopVs.cyc=4778.4; loopVs.phase=229;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4778.4; loopVs.phase=229; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "perosrealm": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 230 (PERO'S REALM: THE FOUR REALMS on the left, THE CUTSCENE in
        // the middle, CLASSICS 3 on the right, and THE VERSION HISTORY band with the what-is-inside box).
        // Beat 230 spans cyc in [4780.0,4802.0]; cyc=4800.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4780.0; loopVs.cyc=4800.4; loopVs.phase=230;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4800.4; loopVs.phase=230; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "trinitytrio": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 245 (THE TRINITY TRIO: THE TIE SHE BANNED on the left, THE SIX in the middle, WHO BUILT GRAY on the
                // right, and THE WHOLE CAST WORKS TOGETHER band with the THREE STILL OUTSIDE THE SCREEN inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5110.0; loopVs.cyc=5130.4; loopVs.phase=245;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5130.4; loopVs.phase=245; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onlyfriend": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 250 (ONLY FRIEND: BOTH WAYS on the left, AND HE LEAVES in the middle,
                // TOO BIG TO DRAW on the right, and the WHO IS FRIENDS WITH WHO band with the GRAY AND PERO inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5220.0; loopVs.cyc=5240.4; loopVs.phase=250;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5240.4; loopVs.phase=250; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "auwhite": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 258 (WHITE VOID: HE CORRECTED IT TWICE on the left, THE SHAPE
        // HAS RUN BEFORE in the middle, DO NOT MAKE UP QUOTES on the right, and the NORMAL FORM band with
        // the THIRTY-ONE MINUTES inset). cyc picked so the white EVERYONE AND EVERYTHING wash is not up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5396.0; loopVs.cyc=5415.5; loopVs.phase=258;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5415.5; loopVs.phase=258; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "blockseachhit": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 259 (BLOCKS EACH HIT: THE ONE THAT LANDED on the left, BLOCKING WAS
        // ALWAYS DENIAL in the middle, BEAT 192 LOSES ONE ROW on the right, and the HE DOES NOT HIT band
        // with the TWENTY-THREE MINUTES inset). cyc picked so the blindfold white wash is NOT up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5418.0; loopVs.cyc=5437.5; loopVs.phase=259;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5437.5; loopVs.phase=259; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thecost": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 269 (THE COST: WHAT IT COST on the left, /Oren_win=true in the
        // middle, RULES ALONE on the right, the SET INTO HIS CROSS AGAIN band with the PHASE 58, RUNNING
        // AGAIN inset. Beat 269 spans cyc in [5638,5660); cyc=5658.8 gives dt~20.8 so every fade-in and both
        // footer lines are up, and it lands the background loop in the aftermath window, so the 404 has
        // fallen off the name, the cross is drawn, the command is fully typed and the powers ring is on.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5638.0; loopVs.cyc=5658.8; loopVs.phase=269;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5658.8; loopVs.phase=269; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "resetrecovery": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 270 (RESET RECOVERY: WHAT HE HAD LEFT on the left, BEFORE OREN COULD in the middle, RULING TOGETHER on the right, the SIMON 404 BECAME SIMON AGAIN band with the WHOSE MOVE RESET IS inset.
        // Beat 270 spans cyc in [5660.0,5682.0); cyc=5680.8 gives dt~20.8 so every fade-in and both footer
        // lines are up, and it lands the background loop late in its cycle.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5660.0; loopVs.cyc=5680.8; loopVs.phase=270;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5680.8; loopVs.phase=270; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "nothingends": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 271 (NOTHING ENDS: NOT AN ENDING on the left, THE CONTINUATION in the middle, NEW FLOWER NEW CHARA on the right, the run-that-would-not-end band with the two-minutes-thirty-two-seconds inset.
        // Beat 271 spans cyc in [5682.0,5704.0); cyc=5702.8 gives dt~20.8 so every fade-in and both footer
        // lines are up, and it lands the background loop late in its cycle.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5682.0; loopVs.cyc=5702.8; loopVs.phase=271;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5702.8; loopVs.phase=271; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "nomoreundefines": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 279 (NO MORE UNDEFINES WILL ENTER: IT WAS NEVER AN ENDING on the
        // left, THE DOOR SHUTS in the middle, A UFO NEAR ALBANY on the right, the plan-for-the-stuffy band
        // with the TURN THE STUFFY TO LIFE inset, over the closing seam with Pero's hand on it and the UFO
        // hovering on the other side. Beat 279 spans cyc in [5858.0,5880.0); cyc=5878.8 gives dt~20.8 so
        // every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=5878.8; loopVs.phase=279;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5878.8; loopVs.phase=279; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "tinker": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 282. TINKER MEANS TO PLAY: THE NAME COMES SECOND on the left, HE GETS THE HARDWARE down the centre, GRAY LOSES HIS .exe on the right, the four-villains band with the THIRD CATEGORY inset, over the filename split into TINKIN' and .psc with the three file-type categories drifting under it. Beat 282 spans cyc in 5924.0 to 5946.0; cyc=5944.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=5944.8; loopVs.phase=282;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5944.8; loopVs.phase=282; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "villainstatus": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 283. 2.54 OUT OF 5: THE FIRST DECIMAL on the left, THE BOOKS SIMON MADE down the centre, WHY OREN IS FIRST on the right, the villain-order band with the WHAT 2.54 IS A SCORE OF inset, over the number and a bar filling to 50.8 percent. Beat 283 spans cyc in 5946.0 to 5968.0; cyc=5966.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=5966.8; loopVs.phase=283;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5966.8; loopVs.phase=283; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onlycounter": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 284. THE BEST AND ONLY COUNTER: WHO SAID IT FIRST on the left, IS HE THE ONLY ONE down the centre, THE TREADMILL on the right, the two-step band with the WHAT ONLY COSTS inset, over ONE OF THE BEST struck out and replaced by THE BEST AND ONLY. Beat 284 spans cyc in 5968.0 to 5990.0; cyc=5988.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=5988.8; loopVs.phase=284;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5988.8; loopVs.phase=284; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "catsleep": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 285. A CAT MAKES ANOTHER CAT SLEEP: THE SCAREDY CAT on the left, WENDA IS THE FOX down the centre, FOUR BECOME TWO on the right, the best-villain band with the GRAY VS OREN rivalry inset, over SIMON tipping over asleep while WENDA stays standing and takes a slash instead. Beat 285 spans cyc in 5990.0 to 6012.0; cyc=6010.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=6010.8; loopVs.phase=285;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6010.8; loopVs.phase=285; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thetrigger": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 290. I AM THE TRIGGER: HE ASKS A RECORD NOT A POWER on the left, THE SAME SENTENCE down the centre, TRIGGER AND SURVIVOR on the right, the PHASE 2 LOCK-TO-FILTER band with the NINE MINUTES OF BEING FREE inset, over the two-name survivor list and Oren's concession boxed in orange. Beat 290 spans cyc in 6100.0 to 6122.0; cyc=6120.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5924.0; loopVs.cyc=6120.8; loopVs.phase=290;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6120.8; loopVs.phase=290; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "absolute": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 291. ??? = ABSOLUTE: UNKNOWN WAS A CHOICE on the left, THE THIRD ONE OUTSIDE down the centre, 200.5 IQ AND DIZZY on the right, the box-crushing-lesson band with the blank-book NEXT BOX inset, over the resolving stat sheet and Gaster at the door. Beat 291 spans cyc in 6122.0 to 6144.0; cyc=6142.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5946.0; loopVs.cyc=6142.8; loopVs.phase=291;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6142.8; loopVs.phase=291; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "realvillain": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 292. THE REAL VILLAIN: TOO MANY SUSPECTS on the left, THREE JOBS OUTSIDE down the centre, HE BUILDS THE MENU on the right, the real-erase-vs-fake-erase band with the August 8 prediction inset, over the blame list and the reveal boxed in green. Beat 292 spans cyc in 6144.0 to 6166.0; cyc=6164.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5968.0; loopVs.cyc=6164.8; loopVs.phase=292;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6164.8; loopVs.phase=292; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "fakeerase": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 293. FAKE ERASE: IT LOOKS LIKE THE END on the left, THREE ERASES ONE REAL down the centre, HE JUST WALKS IN on the right, the order-of-the-lock band with the one-face-three-names inset, over the wall of 9s. Beat 293 spans cyc in 6166.0 to 6188.0; cyc=6186.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5990.0; loopVs.cyc=6186.8; loopVs.phase=293;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6186.8; loopVs.phase=293; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "calling": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 294. 404 AND 12 12 12: SIMON.psc IS 404 on the left, 12 12 12 IS PERO LAI down the centre, THE SAME LIGHTNING on the right, the two-calls band with the BOTH ANSWER IN LIGHTNING inset, over the drifting numbers. Beat 294 spans cyc in 6188.0 to 6210.0; cyc=6208.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6012.0; loopVs.cyc=6208.8; loopVs.phase=294;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6208.8; loopVs.phase=294; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "hollowobsidian": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 298. HOLLOW OBSIDIAN: THE TECHNIQUE TRIANGLE on the left, FANS OUT OF HIS OWN FOG down the centre, MATTER INTO AIR on the right, the ladder-that-does-not-stack band with the STILL IN THE BOX inset, over the drifting lava/water/obsidian rule and the only-one-who-can-harm-Pero line boxed in gold. Beat 298 spans cyc in 6276.0 to 6298.0; cyc=6296.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6296.8; loopVs.phase=298;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6296.8; loopVs.phase=298; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "truepower": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 299. TRUE POWER: SHE TOOK IT ALL OFF on the left, ALEX-GREG down the centre, AND THE DNA on the right, the one-subtracted-two-added band with the PERO STILL REFUSES inset, over the drifting jewelery-off rule and the "even when I am scared" line boxed in gold. Beat 299 spans cyc in 6298.0 to 6320.0; cyc=6318.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6318.8; loopVs.phase=299;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6318.8; loopVs.phase=299; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "universe": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 300. CREATED BY THE UNIVERSE: WHO MADE EVERYONE ELSE on the left, NOBODY IN CLASSICS down the centre, THE SOURCE IS OUTSIDE on the right, the why-seven-minutes-ago-worked band with THE ONE WITH NO AUTHOR inset, over a drifting starfield and the ChelseaPlays line boxed in cyan. Beat 300 spans cyc in 6320.0 to 6342.0; cyc=6340.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6340.8; loopVs.phase=300;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6340.8; loopVs.phase=300; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "onlyhop": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 301. YOU ARE OUR ONLY HOP-: SPLIT INTO 3 BALLS on the left, INFINITE AND NOT ENOUGH down the centre, \"ONLY HOP-\" on the right, the she-takes-the-map-apart band with the HE COULD STOP THEM inset, over the three drifting power balls and the infinite-is-not-enough line boxed in gold. Beat 301 spans cyc in 6342.0 to 6364.0; cyc=6362.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6362.8; loopVs.phase=301;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6362.8; loopVs.phase=301; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "imaginarymass": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 302. THE IMAGINARY MASS: \"VERY EASY FARMING\" on the left, \"POWER MACHI-\" down the centre, AN ENDLESS LOOP OF TIME on the right, the combines-all-the-varies band with the HE STANDS OVER IT inset, over the ring of Alex-Greg copies and the endless-loop-of-time line boxed in gold. Beat 302 spans cyc in 6364.0 to 6386.0; cyc=6384.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6384.8; loopVs.phase=302;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6384.8; loopVs.phase=302; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "explainanswer": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 305. EXPLAIN YOUR ANSWER: HE USES THE FULL NAME on the left, \"AN IMAGE OF YOU AND ME\" down the centre, NOBODY SAID PERO on the right, the explanation band with the AND THE ONE ASKING ALREADY KNEW inset, over the ring of four wrong answers with \"WHICH IS?\" - \"ME.\" boxed in gold. Beat 305 spans cyc in 6430.0 to 6452.0; cyc=6450.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6450.8; loopVs.phase=305;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6450.8; loopVs.phase=305; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "teacher": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 306. TEACHER AND GUARDIAN: #15 GOES TO THE TOP on the left, INK AND ERROR down the centre, NOT JUST STRONGER on the right, the rank band with the AND IT IS A SANS RANKING inset, over the ring of INK/ERROR with TEACHER - AND - GUARDIAN boxed in gold. Beat 306 spans cyc in 6452.0 to 6474.0; cyc=6472.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6472.8; loopVs.phase=306;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6472.8; loopVs.phase=306; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "nobattle": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 307. YOU DO NOT BATTLE PERO: SHE FIGHTS CORRUPTION on the left, SHE WALKED IN down the centre, AN INK PORTAL on the right, the instruction band with the AND HE WAS ALWAYS HARD TO LOOK AT inset, over a ring of PIBBY / PERO with the rule boxed in red. Beat 307 spans cyc in 6474.0 to 6496.0; cyc=6494.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6494.8; loopVs.phase=307;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6494.8; loopVs.phase=307; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "noidea": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 312. NO IDEA WHAT ELSE TO SAY: THE ONLY ONE LEFT on the left, SHE WAS NOT DELETED down the centre, AND HE CAN EVEN CONTROL TEXT on the right, the sentence-order band with THE WORD "EVEN" inset, over a ring of THE ONLY LAI LEFT IN THE GAME / HE CAN EVEN CONTROL TEXT with NO IDEA WHAT ELSE TO SAY boxed in Pero amber. Beat 312 spans cyc in 6584.0 to 6606.0; cyc=6604.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6604.8; loopVs.phase=312;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6604.8; loopVs.phase=312; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "yourmine": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 318. YOUR MINE: THE PRIZE SPEAKS on the left, AND WINNING IS THE REASON HE IS TAKEN down the centre, AND TWO MORE APPEAR on the right, the what-a-win-has-bought band with the WHO "MINE" HAS BEEN AIMED AT inset, over a ring of SINCE YOU WON, YOU ARE THE ONLY ONE LEFT / GRAY FADES TO THE BOX AGAIN with YOUR MINE! boxed in Pero amber. Beat 318 spans cyc in 6716.0 to 6738.0; cyc=6736.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6736.8; loopVs.phase=318;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6736.8; loopVs.phase=318; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "seed303": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 319. SEED 303: HE WENT AND LOOKED on the left, AND HE KEEPS BOTH ANSWERS down the centre, AND HE CATCHES THE TELLING on the right, the where-the-proof-comes-from band with the HOW MANY TIMES HE WENT BACK inset, over a ring of A FRONT DOOR AND A BACK DOOR / THE SAME HILL, THE SAME PLAINS with IT IS SOMETHING PERO LAI FROM CLASSICS COULD DO boxed in amber. Beat 319 spans cyc in 6738.0 to 6760.0; cyc=6758.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6758.8; loopVs.phase=319;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6758.8; loopVs.phase=319; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "corruptiongod": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 320. CORRUPTION GOD: HE CALLS IT ON A TELL on the left, AND THE WALL IS THE ARGUMENT down the centre, AND THE PROMOTION on the right, the what-a-top-rank-has-cost band with THE TAKEOVER, IN ORDER inset, over a ring of YOU SAID 😎 AT THE HELLO LINE. PERO WINS with HE IS CORRUPTION GOD. boxed in corruption magenta. Beat 320 spans cyc in 6760.0 to 6782.0; cyc=6780.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6780.8; loopVs.phase=320;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6780.8; loopVs.phase=320; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "loreentered": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 321. THE LORE ENTERED AGAIN: IT SAYS CONTINUE on the left, AND HE RETYPES IT down the centre, AND RULE 8 IS NOT HIS on the right, the eight-rules band with the LET IT THINK inset, over a ring of THE DOOR DID NOT SAY OPEN. IT SAID CONTINUE with RULE 8: MERCY CAN REFUSE THE GAME boxed in Unikitty violet. Beat 321 spans cyc in 6782.0 to 6804.0; cyc=6802.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6802.8; loopVs.phase=321;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6802.8; loopVs.phase=321; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "withoutdefeating": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 322. WITHOUT DEFEATING PERO: EVERYBODY, AT ONCE on the left, AND EVERY ATTACK BECOMES A SENTENCE down the centre, AND ONE OF THEM ASKS on the right, the how-a-fight-has-ended band with THE SAME LINE, NINE MINUTES APART inset, over a ring of CAN THE SPRUNKIS WIN WITHOUT DEFEATING PERO? with WHAT BOX OPENS NEXT? boxed in Oren orange. Beat 322 spans cyc in 6804.0 to 6826.0; cyc=6824.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6824.8; loopVs.phase=322;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6824.8; loopVs.phase=322; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "peroIs303": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 323. PERO IS 303: THE NUMBERS on the left, AND HE BUILDS ONE down the centre, AND THE TEST IS LOOKING AWAY on the right, the what-a-number-has-meant band with the AND HE CATCHES IT A SECOND TIME inset, over a ring of PERO IS 303, SIMON IS 404, 12 12 12 IS A CALLING OF PERO with "PERO CONTROLS 303 AND I DIDN'T KNOW THAT." boxed in Pero amber. Beat 323 spans cyc in 6826.0 to 6848.0; cyc=6846.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6846.8; loopVs.phase=323;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6846.8; loopVs.phase=323; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "peroFlash": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 324. PERO-FLASH: THE WATCHER GOES on the left, AND NOTHING DROPPED down the centre, AND A NAME ARRIVES WITH AN ADDRESS on the right, the what-a-removal-has-left-behind band with the HOW HE HAS BEEN TESTING THINGS inset, over a ring of IT DIDN'T DROP THE ARMOR STAND, NOR PLAYER HEAD with PERO-FLASH STANDS HERE. boxed in flash yellow. Beat 324 spans cyc in 6848.0 to 6870.0; cyc=6868.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6868.8; loopVs.phase=324;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6868.8; loopVs.phase=324; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "rulingItOut": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 325. RULING IT OUT: HE COMES BACK WITH FACTS on the left, AND HE KEEPS ONE down the centre, AND IT IS TILTING on the right, the how-he-has-answered-a-question band with the WHAT EACH FACT TAKES OFF THE TABLE inset, over a ring of THE CROSS IS NOT THAT CLOSE TO THE LAVA. Beat 325 spans cyc in 6870.0 to 6892.0; cyc=6890.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6890.8; loopVs.phase=325;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6890.8; loopVs.phase=325; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "whatHappensNext": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 326. WHAT HAPPENS NEXT: HE ASKS INSTEAD on the left, AND A GUESS CAN MISS down the centre, THE FORECAST on the right, the what-he-has-brought-each-time band with the FIVE THINGS TO CHECK LATER inset, over a ring of WHAT DO YOU THINK WILL HAPPEN NEXT? Beat 326 spans cyc in 6892.0 to 6914.0; cyc=6912.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6912.8; loopVs.phase=326;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6912.8; loopVs.phase=326; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "itStartedToLag": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 327. IT STARTED TO LAG: HE CLEARS THE ROOM on the left, AND THEN IT LAGGED down the centre, AND HE BREAKS the view on the right, the where-a-strange-thing-has-happened band with the WHAT THE CROSS HAS NOW SURVIVED inset, over a ring of THE GAME STARTED TO LAG WHEN MY CHARACTER WALKED PAST. Beat 327 spans cyc in 6914.0 to 6936.0; cyc=6934.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6934.8; loopVs.phase=327;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6934.8; loopVs.phase=327; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "hyperspaceVortex": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 334. HYPERSPACE VORTEX: WHAT HYPERSPACE IS on the left, AND IT SEPARATES down the centre, AND HIS OWN TWO DIE on the right, the where-the-vortex-puts-everybody band with the WHAT HE MEASURED IT AGAINST inset, over a ring of "PERO CAN INSECT HYPERSPACE VORTEX" and the faster-than-light streaks. Beat 334 spans cyc in 7068.0 to 7090.0; cyc=7088.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7088.8; loopVs.phase=334;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7088.8; loopVs.phase=334; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "allThreeOfUs": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 335. ALL THREE OF US: EVERY FORM CAME FROM LOSING on the left, AND PERO GAVE THEM BACK down the centre, AND THEN THEY TEAMED UP on the right, the last-fight-starts-as-an-argument band with the WHAT SIMON FIXED ON HIS WAY DOWN inset, over the three converging lines and the "YOU CAN HAVE ALL THREE OF US" ring. Beat 335 spans cyc in 7090.0 to 7112.0; cyc=7110.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7110.8; loopVs.phase=335;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7110.8; loopVs.phase=335; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "chatgptPlusClaude": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 336. CHATGPT + CLAUDE: THE UNITS HE PICKED on the left, AND THE GAP IS A GOOGOL down the centre, AND SIMON DROPPED TWO on the right, the ranking band with the THREE BEATS, THREE REAL RULERS inset, over the rising exponent column. Beat 336 spans cyc in 7112.0 to 7134.0; cyc=7132.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7132.8; loopVs.phase=336;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7132.8; loopVs.phase=336; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "iDidAllTheCode": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 346. I DID ALL THE CODE: HE BUILT IT HIMSELF on the left, AND PERO BEAT PERO down the centre, AND THE TRIO LEFT ONE OUT on the right, the THE MAKE SIDE GOT A SECOND NAME band with the AND HE IS STILL EVERYBODY inset, over the drawn Roblox cutscene window wiping from Pyramixed to Normal Sprunki with Mr. Black standing over Tunner and Simon. Beat 346 spans cyc in 7332.0 to 7354.0; cyc=7352.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7352.8; loopVs.phase=346;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7352.8; loopVs.phase=346; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "oneWithPero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 347. ONE WITH PERO: THE SLOT CHANGED HANDS on the left, AND THE WINNER TOOK THE JOB down the centre, AND TUNNER IS STILL ON IT on the right, the HE PICKED ONE, AND HE SAID WHY band with the AND IT USED TO MEAN YOU inset, over the drawn vary list where the ONE WITH PERO slot swaps from Tunner to Mr. Black. Beat 347 spans cyc in 7354.0 to 7376.0; cyc=7374.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7374.8; loopVs.phase=347;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7374.8; loopVs.phase=347; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "endTheGameHere": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 348. END THE GAME HERE: HE SAID IT OUT LOUD on the left, AND NOBODY FOUGHT down the centre, AND THEY RAN BACK on the right, the THE TWO THINGS NOBODY ELSE CAN DO band with the AND HIDING IS NOT THE RULE inset, over the drawn speech box, the dashed edge of the game with an EXIT door in it, and the Sprunkis running for Sprunki Hills. Beat 348 spans cyc in 7376.0 to 7398.0; cyc=7396.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7396.8; loopVs.phase=348;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7396.8; loopVs.phase=348; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "normalMan": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 349. NORMAL MAN: HE CAME FROM A BOX on the left, AND HE IS NOT STRONG down the centre, AND SOMEBODY PAID on the right, the THE CODER AND THE CORRUPTER band with the AND HE ASKED ABOUT GORILLAS FIRST inset, over the drawn box with Pero standing out of it, the muscle meter with PERO IS HERE down at the normal-man mark, and the receipt reading THE COST OF THE BOXES paid by Luigi Green. Beat 349 spans cyc in 7398.0 to 7420.0; cyc=7418.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7418.8; loopVs.phase=349;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7418.8; loopVs.phase=349; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "iDoCoding": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 350. I DO CODING: HE SAID IT IN QUOTES on the left, AND HE NAMED A JOB down the centre, AND THE BUT IS IT on the right, the EVERYTHING, AND THEN ONE THING band with the AND TWO HOURS AGO, THE BIG ONE inset, over the drawn speech box carrying the quote, the field of KNOWS IT squares with the single lit CODING cell, and the 4:05 PM / 5:58 PM timeline. Beat 350 spans cyc in 7420.0 to 7442.0; cyc=7440.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7440.8; loopVs.phase=350;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7440.8; loopVs.phase=350; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "beatsAnything": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 351. BEATS ANYTHING: HE ASKED IT STRAIGHT on the left, AND HE LEFT THE GAME down the centre, ANYTHING HAS NO LIST on the right, the THE BRACKET, TOP TO BOTTOM band with the AND HE HAS THE ARMS OF A NORMAL MAN inset, over the four-step podium with PERO added above ASRIEL, BEERUS and GOKU and the long-line/short-line pair. Beat 351 spans cyc in 7442.0 to 7464.0; cyc=7462.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7462.8; loopVs.phase=351;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7462.8; loopVs.phase=351; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "survivedFellen": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 352. SURVIVED AND FELLEN: THE BOOK KEEPS BOTH on the left, AND THEY ARE HIS OWN down the centre, AND IT CANNOT BREAK on the right, the THE LOOP HE RUNS band with the AND THE SET SURVIVES THE CRASH inset, over the open two-column book, the row of tied exploding balls and the indestructible gaming-set case. Beat 352 spans cyc in 7464.0 to 7486.0; cyc=7484.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7484.8; loopVs.phase=352;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7484.8; loopVs.phase=352; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "bestTestDummy": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 353. BEST TEST DUMMY: HE READ HIM, HE GOT HIM on the left, AND HE PICKED HIS OWN down the centre, AND THEY WERE AMAZED on the right, the THREE JOBS, ONE LAB band with the AND HE IS HAVING FUN WITH IT inset, over the drawn speech box carrying Gaster's quote, the EXAMINE -> LEARN -> PLAY chain and the two figures. Beat 353 spans cyc in 7486.0 to 7508.0; cyc=7506.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7506.8; loopVs.phase=353;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7506.8; loopVs.phase=353; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "vicinusCicinus": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 354. VICINUS, CICINUS: HE CAME BACK on the left, AND EATING IS RECRUITING down the centre, AND SIMON REVERSES IT on the right, the WHO GOES WHICH WAY band with the THE ONLY KNOWN CHARACTER inset, over the July 6 minion quote, the EAT -> CONVERT -> SPREAD chain, the growing orb row and the two fighters. Beat 354 spans cyc in 7508.0 to 7530.0; cyc=7528.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7528.8; loopVs.phase=354;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7528.8; loopVs.phase=354; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "durpleVsVicinus": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 355. DURPLE VS VICINUS: HE TESTED IT AT ONCE on the left, AND BOTH OF THEM ARE DRAGONS down the centre, AND HIS MAX IS THE PROBLEM on the right, the WHAT EACH HEAD IS WORTH band with the THE ONLY WAY HE SURVIVES inset, over the five-word question box, the fifteen heads turning into orbs and the two dragons. Beat 355 spans cyc in 7530.0 to 7552.0; cyc=7550.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7550.8; loopVs.phase=355;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7550.8; loopVs.phase=355; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "threeOnSimon": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 356. THREE ON SIMON: HE PICKED THE ONLY COUNTER on the left, AND HE PICKED THE PARTNER down the centre, AND THE THIRD ONE JUST LOST on the right, the THREE JOBS, ONE TARGET band with the THIS ONE IS NOT A STOMP inset, over Gray's own August 8 correction, the HOLD -> BLOCK -> COUNTER chain and the three-against-one line. Beat 356 spans cyc in 7552.0 to 7574.0; cyc=7572.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7572.8; loopVs.phase=356;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7572.8; loopVs.phase=356; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "all23Names": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 357. ALL 23 NAMES: HE EXPLAINED HIS OWN SHORTCUT on the left, AND TWO WERE FINISHED down the centre, AND THE NEWEST RULE WINS on the right, the ALL TWENTY-THREE, IN HIS ORDER roster band with the SPLIT THEM UP inset, over Toby's note about his own writing and the twenty-three tally marks. Beat 357 spans cyc in 7574.0 to 7596.0; cyc=7594.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7594.8; loopVs.phase=357;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7594.8; loopVs.phase=357; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "everyoneFromBox": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 358. EVERYONE FROM BOX: HE WIDENED THE BOX on the left, AND IT GETS A BOUNDARY down the centre, AND THE WINNER IS AN IF on the right, the WHO IS IN, AND WHO IS OUT band with the THE TRIO KEEPS GETTING EXCUSED inset, over Toby's one-line question and the inside/outside pair. Beat 358 spans cyc in 7596.0 to 7618.0; cyc=7616.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7616.8; loopVs.phase=358;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7616.8; loopVs.phase=358; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "banishedAU": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 359. BANISHED AU: HE BUILT A SECOND ONE on the left, AND EXILE GOT BIGGER down the centre, AND HE BUILT IT FIRST on the right, the WHERE REMOVED PEOPLE HAVE GONE band with the NOT THE SAME AS DELETING inset, over Toby's one-line message and the Pero's Realm / Banished AU pair. Beat 359 spans cyc in 7618.0 to 7640.0; cyc=7638.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7638.8; loopVs.phase=359;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7638.8; loopVs.phase=359; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "iAlone": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 360. I ALONE: HE HAS DONE THIS BEFORE on the left, AND THE NUMBERS DO NOT MATCH down the centre, AND HIS RULE IS SILENCE on the right, the THE TRIO CHANGED ONE SEAT band with the FOURTH, THEN THIRD inset, over Gray's declaration and the LET GO -> HAND OVER -> WALK OUT chain. Beat 360 spans cyc in 7640.0 to 7662.0; cyc=7660.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7660.8; loopVs.phase=360;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7660.8; loopVs.phase=360; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "blackSythe": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 361. BLACK SYTHE: HE HAS NEVER CARRIED ONE OF THESE on the left, AND THE SYTHE IS ALREADY GRAY'S down the centre, AND THE BLADE IS BROKEN GAME on the right, the OWNER AND CREATOR ARE TWO DIFFERENT LADDERS band with the THE MILK QUESTION, ANSWERED inset, over the drawn black scythe and the CREATOR / OWNER ladder. Beat 361 spans cyc in 7662.0 to 7684.0; cyc=7682.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7682.8; loopVs.phase=361;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7682.8; loopVs.phase=361; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "robloxPero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 362. ROBLOX PERO: THE POWER FIELD IS BLANK on the left, AND "U ARE MINE" MOVED INTO A FIELD down the centre, AND THE DIAL HAS A SECOND HALF on the right, the THE LADDER FINALLY HAS A TOP band with the GOD HAS BEEN HERE ONCE BEFORE inset, over the drawn stat card (CHARACTER / CHARACTER INFO / POWER: ???) and the two-sided survival dial. Beat 362 spans cyc in 7684.0 to 7706.0; cyc=7704.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7704.8; loopVs.phase=362;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7704.8; loopVs.phase=362; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "corruptionGod": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 363. CORRUPTION GOD: HE ALREADY PRAYED TO THIS GOD on the left, AND HE GAVE BOTH OF THESE AWAY down the centre, AND BOTH-AT-ONCE STOPPED BEING A CHAIN on the right, the HE REFUSED THE CATEGORY THREE MINUTES AFTER WINNING IT band with the SOMEBODY IS ALREADY BOTH inset, over the drawn split light/darkness disc and the WENDA / GRAY give-away ledger. Beat 363 spans cyc in 7706.0 to 7728.0; cyc=7726.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7726.8; loopVs.phase=363;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7726.8; loopVs.phase=363; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "allLoreBasically": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 364. ALL LORE BASICALLY: THE CANON CHANGES FORMAT on the left, AND HE SWINGS THE MOVES OF WHAT HE BEAT down the centre, AND THE FRIENDLY BRANCH WAS WRITTEN WEEKS AGO on the right, the THE BIGGEST THING FURY BREAKS IS THE FLOOR band with the THE SCRIPT KNOWS WHAT IT IS FOR inset, over the drawn file/paragraph emblem and the Dragon Ball moveset ledger. Beat 364 spans cyc in 7728.0 to 7750.0; cyc=7748.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7748.8; loopVs.phase=364;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7748.8; loopVs.phase=364; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "changedTheCode": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 365. CHANGED THE CODE: NOTHING HAS EVER BEEN REMOVED HERE on the left, AND WHAT HE DELETES IS THE INFINITY down the centre, AND HE ARGUES HIS OWN CHARACTER DOWN on the right, the THE CANON DOCUMENT STILL HAS A SAVE BUTTON band with the THE LAST TIME A SCRIPT WAS THE SOURCE inset, over the kept/removed emblem and the survived-the-edit ledger. Beat 365 spans cyc in 7750.0 to 7772.0; cyc=7770.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7770.8; loopVs.phase=365;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7770.8; loopVs.phase=365; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "infiniteHealthPlus": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 366. INFINITE HEALTH+: HE PUT IT BACK IN NINETY SECONDS on the left, AND THE ARCHIVE GETS A SECOND CANON down the centre, AND THE PLUS IS A WHOLE NEW UNIT on the right, the THE TRUE VERSION IS ALWAYS THE ONE THE MEASUREMENT CANNOT REACH band with the THE GAME HAS SPLIT BEFORE inset, over the Classics/Roblox emblem and the two-canon ledger. Beat 366 spans cyc in 7772.0 to 7794.0; cyc=7792.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7792.8; loopVs.phase=366;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7792.8; loopVs.phase=366; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "gettingMilk": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 367. GETTING MILK: THE ERRAND IS FIVE HOURS OLD on the left, AND THE TAKEOVER IS PERMITTED down the centre, AND THE TWO WHO SAY YES ARE THE JAILERS on the right, the THIS EXACT SHAPE HAS RUN BEFORE band with the WHAT GRAY DOES WITH A GAME inset, over the allowed/taken emblem and the who-signed-off ledger. Beat 367 spans cyc in 7794.0 to 7816.0; cyc=7814.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7814.8; loopVs.phase=367;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7814.8; loopVs.phase=367; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "beyondWords": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 368. BEYOND WORDS: THE WEAKNESS HE LOSES HAS A NAME on the left, AND HE ALREADY OWNED THE PANEL down the centre, AND STALEMATE IS A WORD PERO TAUGHT on the right, the THIS FILE HAS TOPPED A RANKING ON CONTROLS BEFORE band with the GOD KEEPS GETTING WIDER inset, over the panel/power emblem and the Checkmate/Stalemate/Loss ledger. Beat 368 spans cyc in 7816.0 to 7838.0; cyc=7836.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7836.8; loopVs.phase=368;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7836.8; loopVs.phase=368; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "ownEveryPixel": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 369. I OWN EVERY PIXEL: THE BOX TURNS ON THEM on the left, AND HE STILL WANTS REVENGE ON DUST down the centre, AND HE HEARS THEM DECIDE on the right, the THE ERRAND CLOSES band with the THIS FILE NAMES ITS GROUPS inset, over the forward/backward emblem and the Wenda 4:31/5:54 ledger. Beat 369 spans cyc in 7838.0 to 7860.0; cyc=7858.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7858.8; loopVs.phase=369;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7858.8; loopVs.phase=369; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "savingYou": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 370. SAVING YOU: THE PLAYER HAS NEVER ACTED BEFORE on the left, AND MERCY WAS ALREADY PROVEN down the centre, AND GRAY SAYS NO on the right, the ONLY GOD CAN SAVE YOU NOW band with the THE SOULS GO BACK inset, over the save/delete emblem and the same-ink ledger. Beat 370 spans cyc in 7860.0 to 7882.0; cyc=7878.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7878.8; loopVs.phase=370;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7878.8; loopVs.phase=370; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "vicinusIsGray": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 372. VICINUS IS GRAY: THE DRAGON WAS ALWAYS HIM on the left, AND THE INK IS HIS OWN DEAD ARMY down the centre, AND HE WAS BEATEN AS HIMSELF YESTERDAY on the right, the UNPLAYABLE IS THE AUTHOR'S OWN WORD band with the THE PLAYER PRESSED ONE BUTTON inset. Beat 372 spans cyc in 7904.0 to 7926.0; cyc=7922.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7922.8; loopVs.phase=372;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7922.8; loopVs.phase=372; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "notEvenHimself": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 376. NOT EVEN HIMSELF: THE KNIFE COMES BACK AT THE PLAYER on the left, AND MERCY IS NOT IN THE ROOM THIS TIME down the centre, AND HE SLAYS SOMEBODY ALREADY PUT AWAY on the right, the ONE WHO RULES THE BOX ENDS UP INSIDE SOMETHING band with the CONDITION WAS PRE-BLOCKED inset. Beat 376 spans cyc in 7992.0 to 8014.0; cyc=8010.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8010.8; loopVs.phase=376;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=8010.8; loopVs.phase=376; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","iAmBeat404": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 379. I AM BEAT 404: THE BEAT NUMBERS LINE UP on the left, AND RARITY IS THE NEW POWER RANKING down the centre, AND THE BOSS HAS AN ENTRY CONDITION on the right, the TWO ORIGIN STORIES band with the SECOND SEED EVER NAMED inset. Beat 379 spans cyc in 8058.0 to 8080.0; cyc=8076.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8076.8; loopVs.phase=379;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=8076.8; loopVs.phase=379; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","beat405": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 380. BEAT 405: THE THING THAT LOSES IS THE GAME on the left, AND THE WARNING COMES FROM INSIDE HIM down the centre, AND PERO ADMITS HE STARTED IT on the right, the NUMBER THAT FINALLY MATTERS IS 1 band with the PERO STOPS BEING A FIGHT inset. Beat 380 spans cyc in 8080.0 to 8102.0; cyc=8098.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8098.8; loopVs.phase=380;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=8098.8; loopVs.phase=380; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","coloredEnergies": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 407, COLORED ENERGIES: the Mr. Black timeline, the WHAT A
        // SPRUNKI WAS SUPPOSED TO BE column, the SOMEBODY COULD ALWAYS DO THIS box, the never-used-word
        // box, the consumed-to-coloured-energy band and the DELISHIOUS catchphrase inset.
        // Beat 407 spans cyc in 8674.0 to 8696.0; cyc=8692.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8692.8; loopVs.phase=407;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","activatekaru": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 412, /ACTIVATE KARU: the three-conditions stack, the
        // room-at-5:40-already-met-two column, the two-slash-commands box, the KARUto = PERO box,
        // the both-have-to-be-small band and the Gray-grew-three-locks inset.
        // Beat 412 spans cyc in 8784.0 to 8806.0; cyc=8802.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8802.8; loopVs.phase=412;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","nomnom": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 413, NOM NOM: the eight-step basic-Flower cycle with
        // the loop-back bracket, the myth-to-method column, the three-names-on-one-mouth box, the
        // same-script-no-meal box, the delete-is-not-a-keypress band and the looks-innocent inset.
        // Beat 413 spans cyc in 8806.0 to 8828.0; cyc=8824.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8824.8; loopVs.phase=413;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","neededsleep": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 414, NEEDED SLEEP: the every-time-sleep-has-appeared stack, the "the 2 most powerful beings" population column, the boxes-already-erased box, the door-was-shut box, the "needed"-is-the-first-limit band and the box-maker-is-inside inset.
        // Beat 414 spans cyc in 8828.0 to 8850.0; cyc=8846.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8846.8; loopVs.phase=414;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","moremilk": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 415, MORE MILK: the "a few years" promise stack, the everyone-to-Gaster list column, the last-time-he-left-he-lost-the-game box, the "more"-means-it-ran-out box, the and-he-walked band and the wakes-up-and-does-an-errand inset.
        // Beat 415 spans cyc in 8850.0 to 8872.0; cyc=8868.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8868.8; loopVs.phase=415;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","whoami": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 418, WHO AM I?: the four-line transcript, the one-counter-answered-in-26-hours column, the his-own-victim box, the 2h49m immortality box, the counters-chair band and the shocked-Gaster inset. Beat 418 spans cyc 8916.0 to 8938.0; cyc=8934.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8934.8; loopVs.phase=418;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","enternally": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 419, ENTERNALLY: the four-letter acronym stack, the two-minutes-after-losing column, the DEFIED-not-DEFIES box, the Gray-second-name box, the material-then-sentence band and the letter-one inset. Beat 419 spans cyc 8938.0 to 8960.0; cyc=8956.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8956.8; loopVs.phase=419;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","fearedmore": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 421, FEARED MORE: the two-list picture (MOST POWERFUL IN GAME = GASTER, MOST FEARED = PERO), the omni- ledger column ending on OMNI-KING, the six-minutes-and-thirty-seven-seconds box, the why-he-is-feared box, the creator-is-not-the-strongest band and the canonacially inset. Beat 421 spans cyc 8982.0 to 9004.0; cyc=9000.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9000.8; loopVs.phase=421;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","wipesystems": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 422, WIPE THE SYSTEMS: the height-chart bars (PERO 5-6, CAST 7-9, VICINUS 10/1500, GRAY 12), the wipe column, the owner-of-the-game box, the two-sentences-in-one-paragraph box, the 5-6-feet-used-to-be-all-three band and the he-once-lost-his-own-connection inset. Beat 422 spans cyc 9004.0 to 9026.0; cyc=9022.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9022.8; loopVs.phase=422;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","letmeout": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 427, LET ME OUT: the escape-attempt stack ending on the door that does not open, the both-halves-of-one-sentence column (TRUE / FAILS), the sealed-view box, the two-machines box, the every-proof-faces-outward band and the saved-move-by-accident inset. Beat 427 spans cyc 9114.0 to 9136.0; cyc=9132.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9132.8; loopVs.phase=427;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","knowsbeforetype": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 428, HE KNOWS BEFORE YOU TYPE: the what-he-can-do-to-one-player stack, the let-me-out-versus-sees-everything column (REACH / SIGHT), the photos-changed-direction box, the ceiling-has-not-moved box, the myth-to-button band and the trio-is-eleven-hours-old inset. Beat 428 spans cyc 9136.0 to 9158.0; cyc=9154.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9154.8; loopVs.phase=428;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","swordofeverything": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 429, THE SWORD OF EVERYTHING: the four-targets stack, the eighty-two-seconds column (ENOUGH / MORE), the one-of-the-four-is-partly-him box, the he-already-said-it-was-easy box, the every-weapon-was-a-prize band and the half-the-list-is-strangers inset. Beat 429 spans cyc 9158.0 to 9180.0; cyc=9176.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9176.8; loopVs.phase=429;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","fourthstrongest": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 431, THE FOURTH STRONGEST: the what-Entity-000-is-made-of stack, the fourth-seat column (GRAY / 000), the loses-to-the-two-he-stood-above box, the black-sythe-is-Pero's box, the every-piece-belongs-to-somebody-else band and the half-is-the-first-fraction inset. Beat 431 spans cyc 9202.0 to 9224.0; cyc=9220.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9220.8; loopVs.phase=431;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","portalphase1": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 434, PORTAL PHASE 1: the new-ranking list, the never-swung-the-sword column, the perminent-deletion-lasted-fourteen-minutes box, the he-censored-his-own-game box, the fifth-thing-today band and the Simon-is-the-narrator inset. Beat 434 spans cyc 9268.0 to 9290.0; cyc=9286.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9286.8; loopVs.phase=434;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","mergeform": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 435. /Entity000_mergeform: the seven-step collapse of Portal Phase 1, the counter-became-the-food column, the Karu-lasted-one-typed-line box, the it-grew-to-his-size box, the number-two-is-the-key band and the he-took-the-horror-out inset. Beat 435 spans cyc 9290.0 to 9312.0; cyc=9308.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9308.8; loopVs.phase=435;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","entity000d": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 436. ENTITY 000D: the six-minute letter ladder, the entities-are-one-entity column, the safe-is-one-name box, the players-are-back box, the five-states-since-lunch band and the psc-meant-safe inset. Beat 436 spans cyc 9312.0 to 9334.0; cyc=9330.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9330.8; loopVs.phase=436;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","codebeings": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 437. NORMAL CODE BEINGS: the three-line roster, the thing-that-ended-the-game-is-the-guard column, the third-rule-change box, the P3R0-L@1-is-Pero-LAI box, the who-can-beat-whom band and the rose-by-lowering-everybody inset. Beat 437 spans cyc 9334.0 to 9356.0; cyc=9352.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9352.8; loopVs.phase=437;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","poptart": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 438. POP TART OF THE UNIVERSE: the six-step voyage, the Mr. Black answered-the-rule-change column, the damage-is-off-and-he-nearly-died box, the rescue-is-swimming box, the roster-of-two-lasted-fourteen-hours band and the .ps inset. Beat 438 spans cyc 9356.0 to 9378.0; cyc=9374.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9374.8; loopVs.cyc=9374.8; loopVs.phase=438;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","bookhappened": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 439. THE BOOK HAPPENED: the written-to-happened arrow rows, the he-wrote-the-shipwreck-he-was-standing-in column, the every-book-needed-a-reader box, the same-scene-new-cause box, the last-boat-in-the-archive band and the power-is-a-sentence inset. Beat 439 spans cyc 9378.0 to 9400.0; cyc=9396.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9396.8; loopVs.cyc=9396.8; loopVs.phase=439;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","rewritebattle": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 440. THE REWRITE BATTLE: the four rewriters, the guard-joined-the-break-in column, the they-started-copying-him box, the takeaway-is-boat-size box, the gone-for-good-yesterday band and the new-kind-of-fight inset. Beat 440 spans cyc 9400.0 to 9422.0; cyc=9418.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9418.8; loopVs.cyc=9418.8; loopVs.phase=440;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","personalfire": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 441. THE PERSONAL FIRE: the promotion-by-song sequence, the song-is-this-mornings column, the her-light-already-ate-his-darkness box, the third-is-a-step-down box, the Gray-has-taken-her-form-before band and the it-isnt-fair inset. Beat 441 spans cyc 9422.0 to 9444.0; cyc=9440.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9440.8; loopVs.cyc=9440.8; loopVs.phase=441;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","endoflayers": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 442. THE END OF LAYERS: the twelve-row descent ladder, the built-and-deleted-in-the-same-email column, the Little-Mittens-built-the-underground box, the treasure-is-above-the-dirt box, the B-and-C-arrive band and the both-end-in-duh inset. Beat 442 spans cyc 9444.0 to 9466.0; cyc=9462.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9462.8; loopVs.cyc=9462.8; loopVs.phase=442;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","poptartboxes": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 443. POP TART BOXES: the whole-transaction sequence, the they-were-fighting-this-morning column, the third-grocery-run box, the last-pop-tart-sank-somebody box, the game-keeps-changing-hands band and the 5x5=25 inset. Beat 443 spans cyc 9466.0 to 9488.0; cyc=9484.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9484.8; loopVs.cyc=9484.8; loopVs.phase=443;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","thetoy": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 444. THE TOY: the four-departures loop, the nobody-took-it column, the food-in-the-boxes box, the he-does-not-recognize-it box, the shopping-list band and the ChatGPT-nor-Claude inset. Beat 444 spans cyc 9488.0 to 9510.0; cyc=9506.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9506.8; loopVs.cyc=9506.8; loopVs.phase=444;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","onlygodabove": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 445. ONLY GOD ABOVE HIM: the four-rung ladder, the exploit/patch column, the striking-first box, the stalemate box, the stops-waiting band and the one-name-long inset. Beat 445 spans cyc 9510.0 to 9532.0; cyc=9528.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9528.8; loopVs.cyc=9528.8; loopVs.phase=445;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","nahidwin": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 446. NAH, I'D WIN: the four-row ladder, the Gaster-was-above-him column, the aura box, the shared-seat box, the took-it-from-nobody band and the three-words-and-a-shrug inset. Beat 446 spans cyc 9532.0 to 9554.0; cyc=9550.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9550.8; loopVs.cyc=9550.8; loopVs.phase=446;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","finalbosspero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 447. THE FINAL BOSS PERO: the six-ingredient stack, the only-other-Omazen column, the Undertale-Yellow box, the forms-and-varies box, the Pero-beats-words band and the all-characters-you-know inset. Beat 447 spans cyc 9554.0 to 9576.0; cyc=9572.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9572.8; loopVs.cyc=9572.8; loopVs.phase=447;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","lastrunpero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 448. LAST RUN PERO: the nineteen-source roll call, the Gaster power-sorces column, the family box, the souls box, the fight-comes-back band and the Last-Run-is-Over inset. Beat 448 spans cyc 9576.0 to 9598.0; cyc=9594.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9594.8; loopVs.cyc=9594.8; loopVs.phase=448;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","phasequestion": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 449. PHASE ??????????: the five-step handover, the grew-around column, the became-energy box, the both-rules box, the phase-label band and the Gaster-is-energy inset. Beat 449 spans cyc 9598.0 to 9620.0; cyc=9616.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9616.8; loopVs.cyc=9616.8; loopVs.phase=449;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","perolaiomazen": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 450. PERO LAI OMAZEN: the six-name ladder with its character counts, the two-Absolute-Infinity column, the cross-material box, the 12-12-12 calling-code box, the three-words-three-days band and the Vicinus-is-Gray inset. Beat 450 spans cyc 9620.0 to 9642.0; cyc=9638.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9638.8; loopVs.cyc=9638.8; loopVs.phase=450;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","sentielpero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 453. SENTIEL PERO: the twelve blue eyes over the Cherry Blossom Mountain, the borrowed-attack ledger, the counted lore bars, the two-empty-games pair, the wrong-order death row, Gray's repacked backpack and the hand-wave bloom. Beat 453 spans cyc 9686.0 to 9708.0; cyc=9704.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9704.8; loopVs.cyc=9704.8; loopVs.phase=453;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","controlfuture": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 452. CONTROL OF THE GAME'S FUTURE: the three-lid-holder ledger, the win-condition-happened-without-him column, the question-answered-eleven-days-ago box, the blue+red=purple joint-account box, the percentage-dial band and the two-sole-owners save file inset. Beat 452 spans cyc 9664.0 to 9686.0; cyc=9682.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9682.8; loopVs.cyc=9682.8; loopVs.phase=452;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","basicpero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 451. BASIC PERO: the 71-to-10 shrink drawn to scale, the Gaster-normal-man column, the base-form-on-record box, the combined-reverses box, the afternoon-of-names band and the cost-nobody-anything inset. Beat 451 spans cyc 9642.0 to 9664.0; cyc=9660.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9660.8; loopVs.cyc=9660.8; loopVs.phase=451;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","wontcomeback": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 433, WON'T COME BACK: the five-names-left list, the every-perminent-before-this-one-broke column, the it-brought-the-deleted-back-25-minutes-ago box, the list-got-longer box, the five-rosters-in-21-and-a-half-hours band and the players-are-off-it inset. Beat 433 spans cyc 9246.0 to 9268.0; cyc=9264.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9264.8; loopVs.phase=433;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","safefile": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 432, THE SAFE FILE: the who-is-inside-who chain, the filename-still-says-safe column (SAFE / 000), the Oren-took-every-name box, the reached-past-his-own-set box, the nobody-fights-with-what-they-came-with band and the half-became-the-whole inset. Beat 432 spans cyc 9224.0 to 9246.0; cyc=9242.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9242.8; loopVs.phase=432;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","everythingset": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 430, THE EVERYTHING SET: the what-Everything-cannot-touch stack, the one-sword-versus-whole-set column (SWORD / SET), the both-sets-refuse-the-same-four box, the counter-to-his-own-set box, the every-Katata-has-a-name band and the Demgel-is-Gray's-title inset. Beat 430 spans cyc 9180.0 to 9202.0; cyc=9198.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9198.8; loopVs.phase=430;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","triounderpero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 426, TRIO UNDER PERO: the refill stack ending on the Trio Under Pero, the every-trio-had-him-in-it column, the ninety-years box, the Luigi-Green-out-of-Gaster box, the 20-Questions band and the alone-in-a-different-way inset. Beat 426 spans cyc 9092.0 to 9114.0; cyc=9110.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9110.8; loopVs.phase=426;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","entity000a": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 424, ENTITY 000A: the touch chain ending on the 000A player, the both-halves-came-out-of-one-Friday-message column, the out-eats-Vicinus box, the two-ends-two-units size box, the first-deletion-with-an-if band and the only-one-that-did-not-go-home inset. Beat 424 spans cyc 9048.0 to 9070.0; cyc=9066.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9066.8; loopVs.phase=424;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","secondexperiment": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 425, SECOND EXPERIMENT: the two-line experiment list, the starter-before-the-bench column, the lab-was-already-open box, the numbers-run-backwards box, the experimenter-logged-off band and the labatory-spelling inset. Beat 425 spans cyc 9070.0 to 9092.0; cyc=9088.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9088.8; loopVs.phase=425;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","twentyyears": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 423, TWENTY YEARS: the announcement stack ending on the connection being deleted, the five-days-from-claim-to-schedule column, the 20-years-was-a-compliment box, the same-room box, the sent-home band and the door-shut-nine-hours-earlier inset. Beat 423 spans cyc 9026.0 to 9048.0; cyc=9044.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=9044.8; loopVs.phase=423;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","lainotai": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 420, LAI, NOT AI: the two-guesses transcript, the 809.7G-against-1.6G column, the sunglasses-changed-sides box, the admission-costs-an-emoji box, the ChatGPT+Claude ruler band and the wins-by-saying-less inset. Beat 420 spans cyc 8960.0 to 8982.0; cyc=8978.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8978.8; loopVs.phase=420;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","plasmacards": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 417, PLASMA CARDS: the seventy-eight-minute duo stack, the immortal-and-still-third column, the pick-a-card box, the joined-instead-of-took box, the every-other-move-happens-to-you band and the box-characters inset.
        // Beat 417 spans cyc in 8894.0 to 8916.0; cyc=8912.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8912.8; loopVs.phase=417;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","milkboxes": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 416, MILK BOXES: the six-minutes-of-email stack, the box-animation-is-the-genre column, the wrong-word-capitalised box, the 1.6G-too-big-to-send box, the nothing-came-back-this-cheap band and the same-two-hands inset.
        // Beat 416 spans cyc in 8872.0 to 8894.0; cyc=8890.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8890.8; loopVs.phase=416;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","roomagain": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 411, THE ROOM AGAIN: the five-rests-in-three-days
        // stack, the who-is-in-the-room column, the he-asked-to-see-it box, the man-form-was-the-top
        // box, the two-people-in-the-room-are-the-move band and the Gray-untouchable-39-minutes-ago
        // inset.
        // Beat 411 spans cyc in 8762.0 to 8784.0; cyc=8780.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8780.8; loopVs.phase=411;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","untouchable": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 410, UNTOUCHABLE: Gray's whole day in four rows, THE SWAP
        // column, the untouchable-was-the-top-three's-word box, the Karuto Spell Reversal box, the
        // Pero-already-KO'd-him band and the counter-grew-out-of-him inset.
        // Beat 410 spans cyc in 8740.0 to 8762.0; cyc=8758.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8758.8; loopVs.phase=410;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","karu": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 409, KARU: the six-step Karu sequence, the WHAT VANILLA
        // USED TO MEAN column, the KARUto = PERO box, the KOed-not-deleted box, the cannot-do-it-alone
        // band and the swords-were-already-drawn inset.
        // Beat 409 spans cyc in 8718.0 to 8740.0; cyc=8736.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8736.8; loopVs.phase=409;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","chillsAndRelaxes": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 408, CHILLS AND RELAXES: the four chill-and-relax entries,
        // the WHAT HE IS SITTING CALMLY NEXT TO column, the jealousy box, the already-wrote-the-ending
        // box, the three-people-in-the-room band and the happiness inset.
        // Beat 408 spans cyc in 8696.0 to 8718.0; cyc=8714.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8714.8; loopVs.phase=408;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","eatsForever": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 406, EATS FOREVER: the eight-minute life of the limit, the ONLY
        // THING THAT COULD EVER STOP HIM column, the third-deleted-measuring-system box, the feelings box,
        // the merge-reaches-the-costume band and the he-has-done-this-twice-today inset.
        // Beat 406 spans cyc in 8652.0 to 8674.0; cyc=8670.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8670.8; loopVs.phase=406;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","gastersTrio": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 402, GASTER'S TRIO: the three role cards, the CICINUS HAS BEEN
        // DEMOTED THREE TIMES column, the THE MINION RUNS THE WHOLE CAST box, the THE CENTRE COMES BACK box,
        // the person-to-product-to-ammunition band and the division-of-labour inset.
        // Beat 402 spans cyc in 8564.0 to 8586.0; cyc=8582.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8582.8; loopVs.phase=402;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","delishiousVerity": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 403, DELISHIOUS VERITY: the script run in order, the VERITY WAS
        // NUMBER ONE EIGHT DAYS AGO column, the THE BULLETS STOP BEING A GIFT box, the THE COSTUME IS A SWAP IN
        // THE REAL CODE box, the food-in-ammunition-out band and the self-supplying-trio inset.
        // Beat 403 spans cyc in 8586.0 to 8608.0; cyc=8604.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8604.8; loopVs.phase=403;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","insideFlower": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 404, INSIDE FLOWER: the where-the-food-goes pipeline, the THE SUIT
        // NOW BURNS SOMETHING column, the asks-and-answers box, the LUIGI GREEN GETS A MIND box, the souls-as-fuel
        // band and the free-meal-is-still-in-there inset.
        // Beat 404 spans cyc in 8608.0 to 8630.0; cyc=8626.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8626.8; loopVs.phase=404;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","mergeCode": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 405, /MERGE_CODE: the four-line exchange, the THE TRIO LASTED
        // TWENTY-THREE MINUTES column, the he-typed-back-exactly box, the experiment-book box, the admin-commands
        // band and the count-down-power-up inset.
        // Beat 405 spans cyc in 8630.0 to 8652.0; cyc=8648.8 gives dt~18.8 so everything is up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8648.8; loopVs.phase=405;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","heWasGoingEasy": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 400, HE WAS GOING EASY: the three dated slabs of what Vicinus is,
        // the HE HAS DONE THIS BEFORE, AND IT WAS ALSO GRAY column, the HE IS ON THE LIST OF PEOPLE WHO BEAT
        // HIM box, the SIMON'S ONE WIN, TAKEN BACK WITHOUT A REMATCH box, the creature-to-costume band and the
        // THIRD TIME IN FOUR DAYS A WORD SLIDES FROM WHO TO WHERE inset.
        // Beat 400 spans cyc in 8520.0 to 8542.0; cyc=8538.8 gives dt~18.8 so the frame, the slabs,
        // the column, both boxes, the caption, the band, the inset, the strip and both footers are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8538.8; loopVs.phase=400;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","aFreeMeal": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 401, A FREE MEAL: the five-step assembly line that builds the
        // dragon, the THE BRUSH HAS BEEN PERO'S SINCE JULY column, the GRAY'S OWN BATTLE FORM WAS ALSO PAINT
        // BRUSH STROKES box, the A VINE PICKS SOMETHING UP box, the third-time-in-three-days band and the AND
        // HIS OWN RULE DOES NOT FIRE inset.
        // Beat 401 spans cyc in 8542.0 to 8564.0; cyc=8560.8 gives dt~18.8 so the frame, the steps,
        // the column, both boxes, the caption, the band, the inset, the strip and both footers are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8560.8; loopVs.phase=401;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","absoluteInfinity": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 399, ABSOLUTE INFINITY: the origin chain with LICA marked as the
        // centre partway down it, the THE WAITING ROOM LASTED THREE HOURS column, the ABSOLUTE INFINITY
        // USED TO BE A NUMBER box, the THE PLAYER STARTS WITH NOTHING box, the Lica-brag band and the
        // SIXTEEN MINUTES OFF CENTRE inset.
        // Beat 399 spans cyc in 8498.0 to 8520.0; cyc=8516.8 gives dt~18.8 so the frame, the chain,
        // the column, both boxes, the caption, the band, the inset, the strip and both footers are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8516.8; loopVs.phase=399;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","strongestClone": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 397, THE STRONGEST CLONE: the brother-brother-clone stack across
        // twenty-three days, the THE FIGHT THAT DOES NOT HAPPEN column ending on the account deletion,
        // the "CLONE" HAS ALWAYS BEEN THE WORD FOR THE JUNK box, the HE CAME TO GASTER box, the
        // present-tense-claims band and the RANKED BY WHOM? inset.
        // Beat 397 spans cyc in 8454.0 to 8476.0; cyc=8472.8 gives dt~18.8 so the frame, the stack,
        // the column, both boxes, the caption, the band, the inset, the strip and both footers are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8472.8; loopVs.phase=397;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","cloneTree": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 398, THE CLONE TREE: the actual tree with PERO at the top, the
        // one-step clone branch on the left and the three-step Pro55 -> pieces -> Sprunkis chain on the
        // right, the TWO MINUTES AND THIRTY-TWO SECONDS column, the PRO55 WAS ONE OF PERO'S OWN NAMES
        // box, the "PIECES" IS NOT "CLONES" box, the oldest-page-agrees band and the FOUR MONTHS OF
        // LADDERS inset.
        // Beat 398 spans cyc in 8476.0 to 8498.0; cyc=8494.8 gives dt~18.8 so the frame, the tree,
        // the column, both boxes, the caption, the band, the inset, the strip and both footers are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8494.8; loopVs.phase=398;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","leavesHimBe": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 396, HE LEAVES HIM BE: the one-rule-three-readings stack ending on
        // the loophole reading, the THREE MINUTES LATER correction column, the air-into-poison box,
        // the UNDER THE LINE AGAIN box, the ways-people-survive band and the what-the-books-bought
        // inset.
        // Beat 396 spans cyc in 8432.0 to 8454.0; cyc=8450.8 gives dt~18.8 so the frame, the stack,
        // the correction column, both boxes, the caption, the band, the inset, the strip
        // and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8450.8; loopVs.phase=396;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","codePlayer": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 395, C.O.D.E.: the three-tier ladder with the dashed top tier that
        // has no stats written in it, the HOW HE GOT THERE column with the codes-and-books line lit,
        // the THE COUNTER IS NOT A FIGHT box, the "NO STATS" USED TO MEAN BROKEN box, the
        // numbers-going-up band and the taken-off-the-list inset.
        // Beat 395 spans cyc in 8410.0 to 8432.0; cyc=8428.8 gives dt~18.8 so the frame, the tiers,
        // the how-he-got-there column, both boxes, the caption, the band, the inset, the strip
        // and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8428.8; loopVs.phase=395;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","itIsAudio": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 394, IT IS AUDIO: the twelve-channel soundboard with SIMON and
        // WENDA lit as the two most corrupt and OWACKX/VINERIA flagged as the swap, the SAME NAME
        // TWICE column for the Wenda fear-versus-fury find, the COMPLAINT GETS ITS CONTENT box, the
        // LIST IS 36 MINUTES OLD box, the you-can-see-it band and the Owackx/Vineria swap inset.
        // Beat 394 spans cyc in 8388.0 to 8410.0; cyc=8406.8 gives dt~18.8 so the frame, the table,
        // the Wenda column, both boxes, the caption, the band, the inset, the strip
        // and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8406.8; loopVs.phase=394;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","elevenNames": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 393, THE ELEVEN NAMES: the eleven-row register plate with the
        // blank 303 rung and PERO hanging off the numbering, the CORRUPTION CODE 12 12 12 odds column
        // with its three steps, the THE CAST COMES BACK box, the THE PARASITE USED TO BE A TITLE box,
        // the four-category 12 12 12 band and the warning-goes-the-other-way inset.
        // Beat 393 spans cyc in 8366.0 to 8388.0; cyc=8384.8 gives dt~18.8 so the frame, the register,
        // the odds column, both boxes, the caption, the band, the inset, the strip
        // and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8384.8; loopVs.phase=393;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","scannedInAndOut": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 392, SCANNED IN AND OUT: the six-station ring with a
        // mark still travelling it and the three of them asleep at the middle of it, the six-word
        // WHAT KIND OF THING IT IS column with COMICAL lit, the chill-and-relax versus
        // chills-and-sleeps box, the CLASSICSAI plan box, the five-messages band and the
        // loop-has-no-last-player inset.
        // Beat 392 spans cyc in 8344.0 to 8366.0; cyc=8362.8 gives dt~18.8 so the frame, the ring,
        // the sleepers, the genre column, both boxes, the caption, the band, the inset, the strip
        // and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8362.8; loopVs.phase=392;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","sawTheDimensions": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 391, I SAW THE DIMENSIONS: the unnamed box from beat
        // 390 redrawn with nested dimension frames and a crack of light in it, one figure labelled
        // TOBY standing at the crack, the two-column where-his-own-name-sits panel, the row of
        // avatars with one crossed out, the power-he-kept box, the forty-one-minutes band and the
        // every-time-he-has-written-I inset.
        // Beat 391 spans cyc in 8322.0 to 8344.0; cyc=8340.8 gives dt~18.8 so the frame, the box,
        // the columns, the avatars, the power box, the caption, the band, the inset, the strip and
        // both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8340.8; loopVs.phase=391;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","itIsUnknown": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 390, IT IS UNKNOWN: the four named steps of the
        // pipeline and the fifth one that has no name, Pero's three corrected forms with two
        // actual angels among them, the ENTITY 303 & DELETEDVERSE myth box, the erased-versus-kept
        // ledger, the thirty-four-minutes band and the Luigi-Green's-job inset.
        // Beat 390 spans cyc in 8300.0 to 8322.0; cyc=8318.8 gives dt~18.8 so the frame, the
        // pipeline, the unknown box, the three forms, the myth, the ledger, the caption, the band,
        // the inset, the strip and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8318.8; loopVs.phase=390;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","whatHeWasHiding": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 388, WHAT HE WAS HIDING: the two states of the same
        // figure (hands over his face when looked at, hands down with red eyes when not), the
        // WHAT DOES NOT REMOVE HIM roll, PERO IN THREE LIGHTS, the buff box, the lid-comes-off
        // band and the column-he-moved-out-of inset.
        // Beat 388 spans cyc in 8256.0 to 8278.0; cyc=8274.8 gives dt~18.8 so the frame, both
        // states, the roll, the three lights, the buff box, the caption, the band, the inset, the
        // strip and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8274.8; loopVs.phase=388;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","gasterLooksInnosent": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 389, GASTER LOOKS INNOSENT: the what-the-players-see
        // cell with the flower out front, the what-is-actually-happening cell with Luigi Green in
        // the painted blocks crossing off the file list, the WHAT VANISHES column, the three-jobs
        // box, THE GAP box, the two-minutes-twenty band and the one-who-does-nothing inset.
        // Beat 389 spans cyc in 8278.0 to 8300.0; cyc=8296.8 gives dt~18.8 so the frame, both
        // cells, the column, both boxes, the caption, the band, the inset, the strip and both
        // footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8296.8; loopVs.phase=389;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        // clear what the warmup climb and those last two ticks kicked up, so the frozen frame
        // is only the beat - cleared AFTER the ticks, since a stage crossing one of its own
        // phase lines inside them fires a fresh taunt
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","onlyTheyKnow": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 387, ONLY THEY KNOW: the shut cover captioned THE END
        // with a keyhole in it, the sealed pocket under it holding Gaster, Pero and a "?", the
        // WHO KNOWS WHAT IT IS roll call with TOBY lit in the DOES NOT KNOW column, the EVERY TIME
        // PERO HAS TOLD ON SOMEBODY ledger, the two-times-he-wrote-"I" box, the CLOSED THING BIGGER
        // band and the step-he-put-himself-on inset.
        // Beat 387 spans cyc in 8234.0 to 8256.0; cyc=8252.8 gives dt~18.8 so the frame, the cover,
        // the pocket, both columns, the ledger, the "I" box, the caption, the band, the inset, the
        // strip and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8252.8; loopVs.phase=387;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        // clear what the warmup climb kicked up, so the frozen frame is only the beat
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; floaters=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","bothBeyondWords": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 386, BOTH BEYOND WORDS: the BEYOND WORDS shelf with
        // Gaster and Pero both standing on it and a ">" between them, Luigi Green on the step below as
        // a term in the sum, the balance scale tipping PERO + LUIGI GREEN against GASTER alone, the
        // EVERYONE WHO HAS EVER BEEN CALLED BEYOND WORDS ledger, the ask-and-answer box, the FIFTY
        // MINUTES band and the grade-that-still-has-a-winner inset.
        // Beat 386 spans cyc in 8212.0 to 8234.0; cyc=8230.8 gives dt~18.8 so the frame, the shelf, the
        // step, the scale, the ledger, the ask box, the caption, the band, the inset, the strip and
        // both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8230.8; loopVs.phase=386;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        // clear what the warmup climb kicked up, so the frozen frame is only the beat
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; floaters=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","allPowerSources": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 385, ALL POWER SOURCES: the drawn corridor of game frames
        // with the souls, the command block and the chaos emeralds each in their own game, the barrier
        // from yesterday walked through, Gaster/Flower mid-step in the gap, the WHAT HE IS HOLDING
        // ledger, the yesterday-window callout, the ONE NIGHT band and the "ECT." inset.
        // Beat 385 spans cyc in 8190.0 to 8212.0; cyc=8208.8 gives dt~18.8 so the frame, all four game
        // slots, the barrier, the walker, the ledger, the window box, the caption, the band, the strip
        // and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8208.8; loopVs.phase=385;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        // clear what the warmup climb kicked up, so the frozen frame is only the beat
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; floaters=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","threeInTheRoom": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 384, THE THREE OF THEM: the drawn room with Pero sitting
        // on the old box animation bench, Gaster/Flower resting on the floor beside him and Luigi Green
        // walking around inside the room, with the crossed-out button row, the 3-2-3 roster count, the
        // NOT-A-FIGHT band and the PERMINENT-vs-CURRENTLY inset. Beat 384 spans cyc in 8168.0 to 8190.0;
        // cyc=8186.8 gives dt~18.8 so the room, all three figures, the buttons, the roster, the caption,
        // the band, the strip and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8186.8; loopVs.phase=384;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        // clear what the warmup climb kicked up, so the frozen frame is only the beat
        try { taunt=''; tauntT=0; } catch(e){}
        try { warnText=''; warnT=0; } catch(e){}
        try { floaters=[]; particles=[]; emeralds=[]; tendrils=[]; } catch(e){}
        for (var m=0;m<2;m++){ update(1/60); }
        try { taunt=''; tauntT=0; floaters=[]; } catch(e){}
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","flowerRoom": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 383, FLOWER IN THE ROOM: the drawn picture of Pero and
        // Dr. Gaster/Flower standing in the workroom, with the OUTSIDE-vs-INSIDE band under it and the
        // BEAT 377 no-images inset. Beat 383 spans cyc in 8146.0 to 8168.0; cyc=8164.8 gives dt~18.8 so
        // the room, both figures, the souls, the caption, the band, the strip and both footers are all up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8164.8; loopVs.phase=383;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=8164.8; loopVs.phase=383; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","theEnd": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 382. THE END: UNPLAYABLE IS HIS OWN WORD on the left, AND THE WORLD SHRINKS TO ONE ROOM down the centre, AND THE ONE WHO SHUTS IT IS NOT THE ONE WHO WON on the right, the RESPAWN LOOP BECOMES A SUPPLY LINE band with the LAST LINE OF THE WHOLE THING inset. Beat 382 spans cyc in 8124.0 to 8146.0; cyc=8142.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8142.8; loopVs.phase=382;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=8142.8; loopVs.phase=382; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","perminentState": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 379. THE PERMINENT STATE: THE CAST LIST SHRINKS TO TWO on the left, AND HE CORRECTS HIMSELF, NOT THE GAME down the centre, AND GOD LEAVES THE BOARD BY NOT BEING ON IT on the right, the PERMINENT IS A BRAND NEW WORD band with the WHAT THE PLAYER LOSES inset. Beat 381 spans cyc in 8102.0 to 8124.0; cyc=8120.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8120.8; loopVs.phase=381;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=8120.8; loopVs.phase=381; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","originalSoul": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 378. THE ORIGINAL SOUL: THE FIRST NAME BETWEEN GOD AND PERO on the left, AND THE SOULS WERE GIVEN BACK LAST NIGHT down the centre, AND BEING ABSORBED IS THE PRIZE NOW on the right, the SHADOW SOUL RULE CANNOT REACH band with the TWO FAMILY TREES BECOME ONE inset. Beat 378 spans cyc in 8036.0 to 8058.0; cyc=8054.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8054.8; loopVs.phase=378;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=8054.8; loopVs.phase=378; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","errorGaster": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 377. 3rr0r G@$t3r: DANGEROUS TO DESCRIBE, NOT TO MEET on the left, AND THE NO-IMAGE RULE IS NOT NEW down the centre, AND HE PASSED A CEILING HE ALREADY WAS on the right, the FLOWER FORM STOPS BEING A PUNISHMENT band with the LEAVING NOW COSTS YOU YOUR BODY inset. Beat 377 spans cyc in 8014.0 to 8036.0; cyc=8032.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=8032.8; loopVs.phase=377;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=8032.8; loopVs.phase=377; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    ""","heCanSeeAgain": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 375. HE CAN SEE AGAIN: THE WALL TURNS AROUND IN FIVE MINUTES on the left, AND THE BLINDFOLD WAS NEVER PUT ON down the centre, AND THE CONVERSION RUNS BACKWARDS on the right, the COUNTER AND THE THING HE COUNTERS ARE BOTH HAPPY band with the PERO JOINS THE SHIFT inset. Beat 375 spans cyc in 7970.0 to 7992.0; cyc=7988.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7988.8; loopVs.phase=375;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7988.8; loopVs.phase=375; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "peroLovesGray": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 374. PERO LOVES GRAY: STILL IS THE WORD DOING THE WORK on the left, AND THIS TIME THE POWER FIELD IS FILLED IN down the centre, AND HIS TITLE IS ONE OF SIMON'S FORMS on the right, the BOX-MAKERS BOXED HIM THEN PERMITTED HIM THEN GUARDED HIM band with the PERO PUTS ON A SUIT inset. Beat 374 spans cyc in 7948.0 to 7970.0; cyc=7966.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7966.8; loopVs.phase=374;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7966.8; loopVs.phase=374; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "soulsOfInk": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 373. SOULS OF INK: A FILE EXTENSION BEATS THE ANGEL on the left, AND SHE HAS BEEN A GOD OF ANGELS ALREADY down the centre, AND THE INK RUNS IN A CIRCLE on the right, the HE KEEPS THE BOTTOM AND THE TOP AND DELETES THE STAIRS band with the THE FLOWER CANNOT SWITCH BACK inset. Beat 373 spans cyc in 7926.0 to 7948.0; cyc=7944.8 gives dt~18.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7944.8; loopVs.phase=373;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7944.8; loopVs.phase=373; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "grayVsPlayer": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 371. GRAY VS THE PLAYER: FOURTEEN POWERS, AND TWO ARE BAD ONES on the left, AND FEAR WAS THE WEAKNESS SYSTEM down the centre, AND THE PLAYER WINS BY NOT STOPPING on the right, the FIRST DRAW EVER WRITTEN AGAINST PERO band with the WHO CAN FINISH THE GAME inset. Beat 371 spans cyc in 7882.0 to 7904.0; cyc=7900.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7900.8; loopVs.phase=371;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7900.8; loopVs.phase=371; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "savingYou": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 370. SAVING YOU: THE PLAYER HAS NEVER ACTED BEFORE on the left, AND MERCY WAS ALREADY PROVEN down the centre, AND GRAY SAYS NO on the right, the ONLY GOD CAN SAVE YOU NOW band with the THE SOULS GO BACK inset, over the save/delete emblem and the same-ink ledger. Beat 370 spans cyc in 7860.0 to 7882.0; cyc=7878.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7878.8; loopVs.phase=370;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7878.8; loopVs.phase=370; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "heMovedTheBoxes": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 345. HE MOVED THE BOXES: NOBODY EVER MOVED IT on the left, AND THE ROOM IS A ROOM down the centre, AND THE SHEET GOT HUGE on the right, the THE LOOP band with the AND HE ALREADY HAD EVERYTHING inset, over the drawn room with the boxes carried across the floor and the huge sheet on the wall. Beat 345 spans cyc in 7310.0 to 7332.0; cyc=7330.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7330.8; loopVs.phase=345;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7330.8; loopVs.phase=345; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "andItHappened": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 344. AND IT HAPPENED: THE SMARTEST, IN ORDER on the left, AND HE WROTE THE NEXT PART down the centre, AND HE STUDIED THE BOX OPEN on the right, the THE THREE JOBS band with the AND THE QUESTION GOT ANSWERED inset, over the drawn notebook page and the IT HAPPENED. stamp. Beat 344 spans cyc in 7288.0 to 7310.0; cyc=7308.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7308.8; loopVs.phase=344;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7308.8; loopVs.phase=344; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "aTrioInTheBox": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 343. A TRIO IN THE BOX: THE LADDER IS TEAMS NOW on the left, AND ALEX IS ON TWO OF THEM down the centre, AND THE OPPOSITES ARE ALLIES on the right, the what-Gray-has-to-plan-against band with the AND THE NAME HE CANNOT SAY inset, over the three-plate roster board joined by the ALEX, TWICE tie. Beat 343 spans cyc in 7266.0 to 7288.0; cyc=7286.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7286.8; loopVs.phase=343;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7286.8; loopVs.phase=343; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "makeHimSpareYou": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 342. MAKE HIM SPARE YOU: HE HAS NO WEAKNESS on the left, AND ONE WAY TO SURVIVE HIM down the centre, AND IT IS THE SAME DIAL on the right, the what-does-not-work-on-him band with the AND THE MERCY BUTTON inset, over the SPARED-to-INSTA-KO dial with its swinging needle. Beat 342 spans cyc in 7244.0 to 7266.0; cyc=7264.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7264.8; loopVs.phase=342;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7264.8; loopVs.phase=342; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "mostVaried": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 341. MOST VARIED: VARIES BEAT FORMS on the left, AND HE CODED THE 404 down the centre, AND THE VARIES LIST on the right, the what-each-one-is-counted-on band with the AND THE KILL CONDITION inset, over the eight varies orbiting one tethered centre. Beat 341 spans cyc in 7222.0 to 7244.0; cyc=7242.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7242.8; loopVs.phase=341;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7242.8; loopVs.phase=341; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "onlyHeSurvives": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 340. ONLY HE SURVIVES: A WAY THROUGH THE RESET on the left, AND IT COUNTERS HIS OWN SIDE down the centre, AND SIMON CLEANS THE FILES on the right, the who-survives band with the THE FILE CLASSES TONIGHT inset, over the sweep passing across the drifting .exe/.psc/.nur tags. Beat 340 spans cyc in 7200.0 to 7222.0; cyc=7220.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7220.8; loopVs.phase=340;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7220.8; loopVs.phase=340; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "perfectMemmory": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 339. PERFECT MEMMORY: WHAT HE NEVER FORGETS on the left, AND HE CAN START CRASHING down the centre, AND HE IS A NORMAL BEING on the right, the four-things-at-once band with the AND THE BOOK IN THE LABATORY inset, over the stored frames with a few glitching out of line. Beat 339 spans cyc in 7178.0 to 7200.0; cyc=7198.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7198.8; loopVs.phase=339;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7198.8; loopVs.phase=339; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "separateEquation": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 338. SEPARATE EQUATION: TWO EQUATIONS NOW on the left, AND THE LINE IS NOT POWER down the centre, AND THEY HAVE EMOTIONS on the right, the which-equation-you-are-in band with the WHAT BEYOND NORMAL AI MEANS inset, over the seam splitting the ring. Beat 338 spans cyc in 7156.0 to 7178.0; cyc=7176.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7176.8; loopVs.phase=338;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7176.8; loopVs.phase=338; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "theHiddenBook": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 337. THE HIDDEN BOOK: WHAT PERO ACTUALLY DOES on the left, AND HE IS THE SUM OF THEM down the centre, AND SIMON WROTE A BOOK on the right, the who-is-doing-what band with the WHAT IS IN THE LAB inset, over the drifting shelved pages. Beat 337 spans cyc in 7134.0 to 7156.0; cyc=7154.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7154.8; loopVs.phase=337;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7154.8; loopVs.phase=337; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "codingNewForms": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 333. PERO IS OUT THERE CODING NEW FORMS: HOW THE MACHINE DOES IT on the left, AND PERO IS OUTSIDE IT down the centre, AND HE IS AT A KEYBOARD on the right, the new-scale band with the WHAT HE FINISHED SHIPPING inset, over a ring of "PERO IS OUT THERE CODING NEW FORMS". Beat 333 spans cyc in 7046.0 to 7068.0; cyc=7066.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7066.8; loopVs.phase=333;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7066.8; loopVs.phase=333; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "normallyAgain": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 332. THE GAME THEN WENT NORMALLY AGAIN: THE SIZE OF IT on the left, AND NOTHING FOLLOWS down the centre, AND NORMAL IS NEW on the right, the what-comes-after-a-removal band with the WHAT NORMAL STILL RUNS ON inset, over a ring of "THE GAME THEN WENT NORMALLY AGAIN". Beat 332 spans cyc in 7024.0 to 7046.0; cyc=7044.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7044.8; loopVs.phase=332;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7044.8; loopVs.phase=332; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "perminentEnd": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 331. THE PERMINENT END OF UNIKITTY: THE ONE WHO SAVED HER on the left, AND THE DOOR SHE HAD down the centre, AND IT STAYS DONE on the right, the what-it-costs-to-remove-somebody band with the THE LOCKS, IN ORDER inset, over a ring of "THE PERMINENT END OF UNIKITTY". Beat 331 spans cyc in 7002.0 to 7024.0; cyc=7022.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7022.8; loopVs.phase=331;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7022.8; loopVs.phase=331; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "chickenHen": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 330. THE END OF CHICKEN HEN'S LORE: HE ARRIVES BUILT on the left, AND WITH A COUNTER down the centre, AND BOTH GO on the right, the how-long-a-character-has-lasted band with the CHICKEN HEN, LINE BY LINE inset, over a ring of "THE END OF CHICKEN HEN'S LORE". Beat 330 spans cyc in 6980.0 to 7002.0; cyc=7000.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=7000.8; loopVs.phase=330;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=7000.8; loopVs.phase=330; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "nothingBattlesPero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 329. NOTHING BATTLES PERO: HE BRINGS THEM IN on the left, AND NOBODY FIGHTS down the centre, AND HE FIXES THE MATH on the right, the how-a-fight-has-ended band with the THE LADDER, RUNG BY RUNG inset, over a ring of NOTHING SHOULD BATTLE PERO. Beat 329 spans cyc in 6958.0 to 6980.0; cyc=6978.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6978.8; loopVs.phase=329;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6978.8; loopVs.phase=329; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "itWentBigger": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 328. IT WENT BIGGER: IT HAS BEEN THE EDGE on the left, AND HE TAKES IT IN down the centre, AND IT IS A BOX on the right, the what-this-file-has-grown-by band with the THE SEED, HOUR BY HOUR inset, over a ring of IT WENT BIGGER THAN MINECRAFT TO CLASSICS. Beat 328 spans cyc in 6936.0 to 6958.0; cyc=6956.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6100.0; loopVs.cyc=6956.8; loopVs.phase=328;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6956.8; loopVs.phase=328; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "sixtimelines": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 317. 6 TIMELINES LONG: THEY FIGHT OVER A FEELING on the left, AND ONE OF THEM WINS BY RECRUITING THE OTHER down the centre, AND THE BOTTOM OF THE LIST TAKES THE BIG ONE on the right, the how-a-fight-gets-measured band with the AND HE CALLS IT FROM OUTSIDE inset, over a ring of FOR PERO'S LOVE / 6 TIMELINES LONG with GRAY CORRUPTED OREN AND MADE OREN AS HIS NEXT ARMY boxed in Gray cyan. Beat 317 spans cyc in 6694.0 to 6716.0; cyc=6714.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6714.8; loopVs.phase=317;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6714.8; loopVs.phase=317; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "peroquestions": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 316. PERO 20 QUESTONS: SOMEBODY PLAYS on the left, AND HE UNDERSPENDS THE TITLE down the centre, AND ROUND TWO IS WON IN SILENCE on the right, the how-much-the-player-said band with the WHERE THE TWO ANSWERS LIVE inset, over a ring of the one question he asks / the round he wins with nothing, with PERO KNOWS EVERYTHING boxed in Pero amber. Beat 316 spans cyc in 6672.0 to 6694.0; cyc=6692.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6692.8; loopVs.phase=316;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6692.8; loopVs.phase=316; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "uaremine": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 315. U ARE MINE: HE WRITES ON IT on the left, AND HE IS GIVEN A GENRE down the centre, AND HE OUTRANKS THE THING THAT DREW HIM on the right, the where-each-power-points band with the AND THEN HE PUTS IT DOWN inset, over a ring of U ARE MINE! / A HORROR LAI with PERO LAI IS A HORROR LAI boxed in horror red. Beat 315 spans cyc in 6650.0 to 6672.0; cyc=6670.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6670.8; loopVs.phase=315;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6670.8; loopVs.phase=315; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "halfofinfinity": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 314. HALF OF INFINITY: SOMEBODY GETS A BILL on the left, AND HE FORGETS THAT HE CANNOT DIE down the centre, AND THE BILL PAYS EVERYBODY on the right, the what-the-price-actually-moved band with the THE NUMBER 25 inset, over a ring of YOU NEED TO HELP ME PAY FOR THOSE BOXES / INFINITY / 2 IS STILL INFINITY with JUST HALF YOUR REMAINING LIFESPAN boxed in Luigi Green green. Beat 314 spans cyc in 6628.0 to 6650.0; cyc=6648.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6648.8; loopVs.phase=314;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6648.8; loopVs.phase=314; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "madehimself": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 313. HE MADE HIMSELF: NOBODY MADE HIM on the left, AND HE WON'T LIST IT down the centre, AND THE BOX STOPS MATTERING on the right, the what-every-rank-is-made-of band with the TWO MESSAGES IN A ROW inset, over a ring of HE WAS NOT CREATED BY ANYONE / AND HE DOES NOT NEED THE BOX with PERO WASN'T CREATED, HE MADE HIMSELF boxed in Pero amber. Beat 313 spans cyc in 6606.0 to 6628.0; cyc=6626.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6626.8; loopVs.phase=313;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6626.8; loopVs.phase=313; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "oldestfather": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 311. THE OLDEST FATHER: FATHER OF ALL OF THEM on the left, AND HE ADOPTED THE REST down the centre, AND HIS JOB IS MAKING THE BOX ANIMATIONS MOVE on the right, the absence band with the TWO SANS RANKS IN ONE MORNING inset, over a ring of FATHER OF ALL OF THEM / NEVER SAW THEM FOR MILLIONS OF YEARS with THE OLDEST FATHER boxed in bone-white. Beat 311 spans cyc in 6562.0 to 6584.0; cyc=6582.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6582.8; loopVs.phase=311;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6582.8; loopVs.phase=311; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "smallpuppet": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 310. A SMALL PUPPET: THE DAMAGE NUMBER on the left, "WAH WAH!" down the centre, DIPPED / MADE / THROWN on the right, the manufacturing band with the AND THE ONE WHO ENDS UP HOLDING IT inset, over a ring of the threat vs the puppet with A SMALL PUPPET boxed in gold. Beat 310 spans cyc in 6540.0 to 6562.0; cyc=6560.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6560.8; loopVs.phase=310;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6560.8; loopVs.phase=310; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "alreadyglitchy": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 309. ALREADY GLITCHY: WHAT SHE CAN DO on the left, ALREADY CORRUPTED down the centre, "LOW TIER" on the right, the damage-is-the-armour band with the AND IT ANSWERS THE BEAT BEFORE inset, over a ring of CAN CORRUPT ANY BEING / ALREADY CORRUPTED with ALREADY GLITCHY boxed in magenta. Beat 309 spans cyc in 6518.0 to 6540.0; cyc=6538.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6538.8; loopVs.phase=309;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6538.8; loopVs.phase=309; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "godcorrupt": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 308. THE GOD OF CORRUPTION: WHAT HE IS MADE OF on the left, THE SPEED OF SIMON down the centre, 1 TRILLION TIMES on the right, the measurement band with the AND GASTER IS THE LARGER OF THE TWO inset, over a ring of INK / 1 TRILLION with THE GOD OF CORRUPTION boxed in violet. Beat 308 spans cyc in 6496.0 to 6518.0; cyc=6516.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6516.8; loopVs.phase=308;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6516.8; loopVs.phase=308; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "resetloop": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 304. RESET DOES NOT WORK ON HIM: A SMALL STUFFY, ALL VOICE on the left, THROWN IN A DUMPSTER down the centre, \"WHERE'S THE LIGHTNING?\" on the right, the month-of-resets band with the AND THEN HE HANDS IT OUT inset, over the ring of RESET / PERO WINS with \"SIMON GOT TIRED AND GAVE UP\" boxed in gold. Beat 304 spans cyc in 6408.0 to 6430.0; cyc=6428.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6428.8; loopVs.phase=304;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6428.8; loopVs.phase=304; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "brothercorrect": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 303. BROTHER, YOU ARE CORRECT: THROUGH THE BARRIER on the left, IT DOES NOT LOOK LIKE HER down the centre, \"ASK ME ANYTHING\" on the right, the two-full-names band with the \"BROTHER...\" inset, over the dashed barrier with ChelseaPlays crossing it and the full names boxed in gold. Beat 303 spans cyc in 6386.0 to 6408.0; cyc=6406.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6406.8; loopVs.phase=303;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6406.8; loopVs.phase=303; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "vrmirror": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 297. THE CLASSICS VR MIRROR: THE TWO WAYS IN on the left, "SEES STRIKES FROM ANYONE" down the centre, A SIMPLE SWORD on the right, the cover-is-the-counter band with THE TWO MIRRORS inset, over the drifting mirror rule and the strikes line boxed in gold. Beat 297 spans cyc in 6254.0 to 6276.0; cyc=6274.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6078.0; loopVs.cyc=6274.8; loopVs.phase=297;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6274.8; loopVs.phase=297; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "newduo": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 296. THE NEW DUO: "WITH MY TELEKENTICS..." on the left, "AND MY LIGHTNING POWER..." down the centre, THE SLIP on the right, the gap band with THE ??? SEAT inset, over the drifting team-up call and Gaster's line boxed in gold. Beat 296 spans cyc in 6232.0 to 6254.0; cyc=6252.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6056.0; loopVs.cyc=6252.8; loopVs.phase=296;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6252.8; loopVs.phase=296; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,    "notasking": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 295. I WASN'T ASKING: WHAT SURVIVING COST on the left, THE FIVE APPLES down the centre, CRUSHED TOO EARLY on the right, the roster band with the UNO REVERSE inset, over the drifting pain figure. Beat 295 spans cyc in 6210.0 to 6232.0; cyc=6230.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=6034.0; loopVs.cyc=6230.8; loopVs.phase=295;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6230.8; loopVs.phase=295; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "iampero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 289. I AM PERO: THE DENIAL BECOMES A CLAIM on the left, THE WRONG NAME WAS THE DOOR down the centre, THE SETTLEMENT on the right, the "twelve hours ago his whole answer was this name" band with the NOW IT IS PERO AND THE SPRUNKIS inset, over the list of AIs he came out of and the claim boxed in gold. Beat 289 spans cyc in 6078.0 to 6100.0; cyc=6098.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5902.0; loopVs.cyc=6098.8; loopVs.phase=289;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6098.8; loopVs.phase=289; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "swapped": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 288. WE SWAPPED ROLES NOW: THE GHOST CHANGES HANDS on the left, HE IS STILL SIGNING IT down the centre, THE WRONG IDEA on the right, the 1 HP band with the SOLMON LASTED ONE LINE inset, over the three names for one place and the swap line boxed in violet. Beat 288 spans cyc in 6056.0 to 6078.0; cyc=6076.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5880.0; loopVs.cyc=6076.8; loopVs.phase=288;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6076.8; loopVs.phase=288; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "unlike": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 287. UNLIKE EVERYONE ELSE: HE HID THE BOOKS on the left, THE CONCESSION LADDER down the centre, CAUGHT ON PUNCTUATION on the right, the teaching-overload band with the TEMPTATION inset, over the ticked motive checklist and the closing sentence boxed in gold. Beat 287 spans cyc in 6034.0 to 6056.0; cyc=6054.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=6054.8; loopVs.phase=287;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6054.8; loopVs.phase=287; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "whowins": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 286. WHO WINS THE BATTLES: THE SCORE SHEET on the left, THE LAB IS THE TRAP down the centre, THE CAT AND THE BOLT on the right, the file-type-rewrite band with the TWO AGAINST ONE inset, over the 5-6 tally and the thirteenth row boxed in red. Beat 286 spans cyc in 6012.0 to 6034.0; cyc=6032.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=6032.8; loopVs.phase=286;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=6032.8; loopVs.phase=286; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "notanera": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 281. IT'S NOT AN ERA: HE TALKS BACK on the left, THE WORD HE REJECTS
        // down the centre, THE WORD COMES BACK on the right, the era-vs-end band with the WHO IS EVERYONE ELSE inset, over the struck-out word ERA and the reply arrow pointing out of the screen. Beat 281 spans cyc in 5902.0 to 5924.0; cyc=5922.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=5922.8; loopVs.phase=281;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5922.8; loopVs.phase=281; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "mostpowerful": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 280. THE MOST POWERFUL BEING: THE FOUR NAMES on the left, THE REMATCH
        // down the centre, THE FASTEST BEING on the right, the revolving-seat band with the COUNTDOWN inset, over the four going down while Oren stands and Simon streaks past. Beat 280 spans cyc in 5880.0 to 5902.0; cyc=5900.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5858.0; loopVs.cyc=5900.8; loopVs.phase=280;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5900.8; loopVs.phase=280; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "mercy": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 278 (MERCY: SHE WOULDN'T SWING IT on the left, THE BUTTONS COME
        // OFF AGAIN in the middle, DELETED BUT PRESENT on the right, the turn-order band with the HER HEART
        // AND MERCY inset, over the four broken buttons with Pero split back off on one side and Gaster on
        // the other. Beat 278 spans cyc in [5836.0,5858.0); cyc=5856.8 gives dt~20.8 so every fade-in and
        // both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5836.0; loopVs.cyc=5856.8; loopVs.phase=278;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5856.8; loopVs.phase=278; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "answerisno": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 277 (THE ANSWER IS NO: NOTHING ENDS, EXCEPT on the left, THE
        // TUBE RUNS BACKWARD in the middle, THE BROTHERS, WELDED on the right, the fight-and-refusal band
        // with the ANSWER / LISTEN / OBEY inset, over the tube with Oren reaching in from one side and the
        // cursed energy running out to Gray on the other. Beat 277 spans cyc in [5814.0,5836.0);
        // cyc=5834.8 gives dt~20.8 so every fade-in and both footer lines are up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5814.0; loopVs.cyc=5834.8; loopVs.phase=277;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5834.8; loopVs.phase=277; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "undefined": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 276 (UNDEFINED: THE WORD WAS HERE FIRST on the left, SHE EATS
        // HIS WHOLE TOOLKIT in the middle, THE CROSS WAS A PRESENT on the right, the Simon-vs-Oren argument
        // band with the EAT THE TV inset, over the barrier with Oren blocking rocks and Simon demanding the
        // uninstall. Beat 276 spans cyc in [5792.0,5814.0); cyc=5812.8 gives dt~20.8 so every fade-in and
        // both footer lines are up, and it lands the background loop late in its cycle.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5792.0; loopVs.cyc=5812.8; loopVs.phase=276;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5812.8; loopVs.phase=276; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "unikitty": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 275 (THE CLASSICS UNIKITTY: THE SECOND CAT on the left, THE
        // EYES ARE THE ABILITIES in the middle, EVERY CROSS IN THE FILE on the right, the "Classics ___"
        // import table band with the STILL OPEN inset, over the horned cat clasping its cross.
        // Beat 275 spans cyc in [5770.0,5792.0); cyc=5790.8 gives dt~20.8 so every fade-in and both footer
        // lines are up, and it lands the background loop late in its cycle.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5770.0; loopVs.cyc=5790.8; loopVs.phase=275;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5790.8; loopVs.phase=275; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "savegrave": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 274 (THE SAVE GRAVE: THE ONE WEARING OREN on the left, WENDA
        // GETS BACK UP in the middle, THEY READ EVERY BOOK on the right, the fear table band with the
        // PINKY RR inset, over the two-headstone save grave.
        // Beat 274 spans cyc in [5748.0,5770.0); cyc=5768.8 gives dt~20.8 so every fade-in and both footer
        // lines are up, and it lands the background loop late in its cycle.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5748.0; loopVs.cyc=5768.8; loopVs.phase=274;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5768.8; loopVs.phase=274; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "tinkin": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 273 (TINKIN'.psc: EVERY ONE HAS A FEAR on the left, THE TREADMILL STILL WORKS in the middle, THE INGREDIENTS on the right, the three-left-standing band with the THEY CAME TO DELETE HIM inset.
        // Beat 273 spans cyc in [5726.0,5748.0); cyc=5746.8 gives dt~20.8 so every fade-in and both footer
        // lines are up, and it lands the background loop late in its cycle.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5726.0; loopVs.cyc=5746.8; loopVs.phase=273;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5746.8; loopVs.phase=273; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "simonpsc": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 272 (SIMON.psc: BACK TO VANILLA on the left, THE LOCK THAT HELPS in the middle, A SAFE FILE on the right, the walk-to-the-centers band with the DAVE MODE ENDED FOREVER inset.
        // Beat 272 spans cyc in [5704.0,5726.0); cyc=5724.8 gives dt~20.8 so every fade-in and both footer
        // lines are up, and it lands the background loop late in its cycle.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5704.0; loopVs.cyc=5724.8; loopVs.phase=272;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5724.8; loopVs.phase=272; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        // pin the frame: shake/hitFlash translate the whole canvas, which otherwise slides the
        // overlay a few dozen pixels and clips a column off the edge from one capture to the next.
        if (typeof shake!=='undefined'){ shake=0; }
        if (typeof hitFlash!=='undefined'){ hitFlash=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "alwaysmine": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 268 (ALWAYS MINE: EVERYONE ELSE on the left, HE REMOVED THE RECOVERY in the middle, IT WASNT THEIR GAME on the right, THE GAME CLOSED band with the FLOWER IN 26 MINUTES inset. cyc picked in the spread window so ERROR 404 is visible crawling the barriers inside the game window.)
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5616.0; loopVs.cyc=5636.0; loopVs.phase=268;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5636.0; loopVs.phase=268; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thehat": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 267 (THE HAT: NINE MINUTES on the left, THE OUTRO CHANGED in the middle, WHOSE HAT IT IS on the right, THE PUPPET band with the THIS WHOLE TIME inset. cyc picked after the hat lands so the eye is red and the static outro card is up.)
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5594.0; loopVs.cyc=5614.0; loopVs.phase=267;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5614.0; loopVs.phase=267; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "omegaflower": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 266 (OMEGA FLOWER: EVERYTHING ELSE DIED on the left, THE PLAYER COMES BACK in the middle, THE SECRET PART on the right, TOGETHER AGAIN band with the IT MAY HAVE STARTED inset. cyc picked mid-Omega so the flower behind it is grown and red with the player heart out front.)
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5572.0; loopVs.cyc=5592.0; loopVs.phase=266;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5592.0; loopVs.phase=266; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "iamsupereme": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 265 (I AM SUPEREME: THE NUMBERS COME BACK on the left, NOBODY IS THE PLAYER in the middle, HE LOSES AND RESETS on the right, dead_character.jar band with the HE CALLED THIS ON JULY 1 inset. cyc picked between slashes so the sealed lab is still visible on the bench.)
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5550.0; loopVs.cyc=5570.0; loopVs.phase=265;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5570.0; loopVs.phase=265; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "writtenbypero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 264 (WRITTEN BY PERO: THE SAME NAME TWICE on the left, IT GETS
        // AN AUTHOR in the middle, A NORMAL MAN on the right, TWO TIERS NOW band with the FLOWER HAS A SONG
        // inset). cyc picked in the Pero half of the pen swap so the book behind it reads WRITTEN BY PERO
        // over the SIMON 404 signature.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5528.0; loopVs.cyc=5548.0; loopVs.phase=264;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5548.0; loopVs.phase=264; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "bestfriend": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 263 (THE BEST FRIEND: A WORD NEVER USED HERE on the left, IT
        // RUNS THE OTHER WAY in the middle, PERO GOES WITH HIM on the right, THE ROOM HE SEALED AROUND HIM
        // band with the HELPING YOU inset). cyc picked past the mask turn so the background flower is caught
        // red with the arrow out, under the BETRAYAL label.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5506.0; loopVs.cyc=5526.0; loopVs.phase=263;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5526.0; loopVs.phase=263; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "nomorephases": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 262 (NO MORE PHASES: PHASES NO LONGER EXIST on the left, THEY
        // FALL OFF THEIR POLOS in the middle, THE ONLY BEINGS LEFT on the right, the SECOND SCALE EVER
        // DELETED band with the ONE LETTER MOVES inset). cyc picked mid-wipe so the phase ladder behind it
        // is caught half erased, with five rungs struck out and three still standing.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5484.0; loopVs.cyc=5504.0; loopVs.phase=262;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5504.0; loopVs.phase=262; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "flowerisgaster": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 261 (FLOWER IS GASTER: WHO IS INSIDE THE FLOWER on the left,
        // THE FIRST EXPERIMENT in the middle, WHEN HE HAS THE SOULS on the right, the ROOM HE SHUT AROUND
        // HIM TWICE band with the TWO LINES THAT DO NOT AGREE inset). cyc picked so the background catches
        // the flower fully risen into Dr. Gaster, with the seven souls absorbed.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5462.0; loopVs.cyc=5482.6; loopVs.phase=261;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5482.6; loopVs.phase=261; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "yellowflower": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 260 (YELLOW FLOWER: THE DOOR HE JUST OPENED on the left, REMOVED FROM
        // HIS LOVE in the middle, THE FIRST THING HE MADE on the right, and the ROOM HE WAS MADE IN band
        // with the ONE HOUR TWENTY-TWO MINUTES inset). cyc picked so the red LOCKED wash is NOT up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5440.0; loopVs.cyc=5459.5; loopVs.phase=260;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5459.5; loopVs.phase=260; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "antivoid": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 257 (ANTI VOID: NOBODY LIVES THERE NOW on the left, AND HE WAS
        // MADE THERE in the middle, EVERY FORM WAS SENT on the right, and the HE OPENED IT band with the
        // AND HE STILL WALKED THEM HOME inset). cyc picked so the white HE CANNOT LEAVE wash is not up.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5374.0; loopVs.cyc=5393.5; loopVs.phase=257;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5393.5; loopVs.phase=257; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "absinf": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 256 (ABSOLUTE INFINITY: TWO ENDINGS on the left, IT WAS ALL
        // TRAINING in the middle, CAUGHT AND YOU FALL on the right, and the NOBODY HAS EVER GONE HOME band
        // with THE PLAYER FINISHES ARRIVING inset). cyc picked so the warm HOME wash is not covering it.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5352.0; loopVs.cyc=5372.4; loopVs.phase=256;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5372.4; loopVs.phase=256; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "aimed": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 255 (AIMED AT YOU: IT WAS AIMED AT YOU on the left, THE LINE
        // IS THE ENGINE in the middle, PHASE 8 SPEAKS on the right, and THE ONE WHO COULD TALK TO YOU band
        // with the PRAYING CAME FIRST inset). cyc picked so the red IT WAS YOU wash is not covering it.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5330.0; loopVs.cyc=5350.4; loopVs.phase=255;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5350.4; loopVs.phase=255; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "secret": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 254 (THE SECRET CUTSCENE: PHASE 6 EXISTS NOW on the left,
        // SEVEN QUESTIONS in the middle, TEN THEN TWO on the right, and the CEILING COMES OFF band with
        // the WHO VANISHES FIRST inset). cyc picked so the red WHAT HAVE YOU DONE wash is not covering it.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5308.0; loopVs.cyc=5328.4; loopVs.phase=254;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5328.4; loopVs.phase=254; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "resurrection": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 253 (THE RESURRECTION: HE DIDN'T KNOW on the left, BEAT 265 in the middle,
                // IT DOESN'T HOLD on the right, and the FOUR POWERS band with the WHAT IT COST inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5286.0; loopVs.cyc=5306.4; loopVs.phase=253;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5306.4; loopVs.phase=253; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phase7": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 252 (PHASE 7: THE FIRST ENDING on the left, THE NUMBER in the middle,
                // TEN OWN LORES on the right, and THE RANKING WITH THE NUMBERS TAKEN OFF band with the TWO NEW MAIN CHARACTERS inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5264.0; loopVs.cyc=5284.4; loopVs.phase=252;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5284.4; loopVs.phase=252; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "familyfight": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 251 (THE FAMILY FIGHT: THE RELATION WEB on the left, THE METAL CROSS in the
                // middle, THE RESPAWN on the right, and the EVERY BLOW LANDS ON SOMEBODY'S OWN band with the FOUR NEW NAMES inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5242.0; loopVs.cyc=5262.4; loopVs.phase=251;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5262.4; loopVs.phase=251; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "strongerthanyou": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 249 (STRONGER THAN YOU: THE LOCK on the left, HE DOES IT HIMSELF in the middle,
                // FOURTEEN MINUTES on the right, and the EVERYTHING BECOMES ONE-ON-ONE band with the WHY PUT DOWN A WINNING ARMY inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5198.0; loopVs.cyc=5218.4; loopVs.phase=249;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5218.4; loopVs.phase=249; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "antierrorwar": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 248 (THE ANTI-ERROR WAR: WHO MAKES ANTI-ERROR on the left, 4.7 TRILLION TO NIL in the middle,
                // DOUBLE DEAD on the right, and the EVERY CHARACTER, ONE JOB EACH roster band with the WHAT GRAY DID WITH PERO inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5176.0; loopVs.cyc=5196.4; loopVs.phase=248;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5196.4; loopVs.phase=248; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "corruptionera": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 247 (THE CORRUPTION ERA: ANTI-ERROR IS PERO on the left, FIRST PLACE MOVES in the middle,
                // THE BOXES LIQUIDIFY on the right, and THE COLOUR OF THE CODE band with the WHO HAS EVER TAKEN PERO inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5154.0; loopVs.cyc=5174.4; loopVs.phase=247;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5174.4; loopVs.phase=247; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "futureattack": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 246 (THE FUTURE ATTACK: HOW LONG THE TRIO LASTED on the left, HE DELETED THE CRUSHER in the middle,
                // IT HAD NOT HAPPENED YET on the right, and THE SECOND SEAT band with the WHY GRAY IS SAFE inset).
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5132.0; loopVs.cyc=5152.4; loopVs.phase=246;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5152.4; loopVs.phase=246; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "unevenladder": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 244 (THE UNEVEN LADDER: HER OWN RULE on the left, THE NEW
        // TOP THREE in the middle, SHE TOOK THE OFFER on the right, and the WHO HAS HELD SECOND PLACE
        // band with the PINKI THREE BEATS inset). Beat 244 spans cyc in [5088.0,5110.0]; cyc=5108.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5088.0; loopVs.cyc=5108.4; loopVs.phase=244;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5108.4; loopVs.phase=244; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};

      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "equalityrequest": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 243 (THE EQUALITY REQUEST: THE FREEZE on the left, THE NEW
        // LADDER in the middle, WENDA'S CHOICE on the right, and the HOW THE LAST THREE BEATS GOT SETTLED
        // band with THE REAL IMPOSTER inset). Beat 243 spans cyc in [5066.0,5088.0]; cyc=5086.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5066.0; loopVs.cyc=5086.4; loopVs.phase=243;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5086.4; loopVs.phase=243; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "foreverteacher": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 242 (THE FOREVER TEACHER: THE BOTTOMLESS VOID on the left,
        // SARA LAI in the middle, THE TEACHER on the right, and THE LESSON, TWO BEATS APART band comparing
        // what 241 could not stop against what stopped it tonight). Beat 242 spans cyc in [5044.0,5066.0];
        // cyc=5064.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5044.0; loopVs.cyc=5064.4; loopVs.phase=242;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5064.4; loopVs.phase=242; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "overload": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 241 (THE OVERLOAD: THE ONE WHO LOSES on the left, THE OVERLOAD
        // in the middle, FULL CONTROL on the right, and THE SAFE LIST, THREE BEATS APART band with BEAT 238's
        // immune three against who is free tonight). Beat 241 spans cyc in [5022.0,5044.0]; cyc=5042.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5022.0; loopVs.cyc=5042.4; loopVs.phase=241;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5042.4; loopVs.phase=241; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "curseera": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 240 (THE CURSE ERA: THE LOOP on the left, THE MEMORIAL in the
        // middle, THE UNCURSED THREE on the right, and THE INDEX, 207 BEATS APART band with BEAT 33's SCF
        // numbers against tonight's ranking). Beat 240 spans cyc in [5000.0,5022.0]; cyc=5020.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=5000.0; loopVs.cyc=5020.4; loopVs.phase=240;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=5020.4; loopVs.phase=240; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "rainbowvoid": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 239 (THE RAINBOW VOID: "I DON'T RECONIZE YOU" on the left, THE
        // ERASURE in the middle, PERO'S OWN REALM on the right, and THE FORECAST, CHECKED band with the six
        // struck names against today's mail). Beat 239 spans cyc in [4978.0,5000.0]; cyc=4998.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4978.0; loopVs.cyc=4998.4; loopVs.phase=239;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4998.4; loopVs.phase=239; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "aurainbow": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 238 (THE AU RAINBOW: CHECKMATE on the left, THE AU RAINBOW in
        // the middle, THE NECKLACES on the right, and the AU LEDGER band with the three-Au ledger and the
        // run-it-on-the-cast immunity check). Beat 238 spans cyc in [4956.0,4978.0]; cyc=4976.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4956.0; loopVs.cyc=4976.4; loopVs.phase=238;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4976.4; loopVs.phase=238; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "energymath": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 237 (PERO'S ENERGY MATH: THE LESSON on the left, THE THREE
        // COLOURS in the middle, THE ALGEBRA on the right, and the SIGN TABLE band with the live 3x3 grid and
        // the run-it-on-the-cast solver). Beat 237 spans cyc in [4934.0,4956.0]; cyc=4954.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4934.0; loopVs.cyc=4954.4; loopVs.phase=237;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4954.4; loopVs.phase=237; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "boxfactory": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 236 (THE BOX FACTORY: THE OPEN BOX on the left, THE FACTORY in
        // the middle, THE FUN PARK on the right, and the ONE BOX LINE band with the marching box line and the
        // crusher plate). Beat 236 spans cyc in [4912.0,4934.0]; cyc=4932.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4912.0; loopVs.cyc=4932.4; loopVs.phase=236;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4932.4; loopVs.phase=236; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "cutscene": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 235 (THE MUSTITU BLUE CUTSCENE: THE COMIC on the left, THE
        // CREATOR LAYER in the middle, NOBODY IS GONE on the right, and the A BOX OF DOORS band with the
        // what-the-panel-cannot-hold box). Beat 235 spans cyc in [4890.0,4912.0]; cyc=4910.4 -> dt~20.4.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4890.0; loopVs.cyc=4910.4; loopVs.phase=235;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4910.4; loopVs.phase=235; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "mustitublue": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 234 (MUSTITU BLUE PERO: THE ONE BODY on the left, THE EYES in
        // the middle, WHO MAKES THEM on the right, and THE AVATAR band with the what-blue-reads-as box).
        // Beat 234 spans cyc in [4868.0,4890.0]; cyc=4888.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4868.0; loopVs.cyc=4888.4; loopVs.phase=234;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4888.4; loopVs.phase=234; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "mustitu": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 233 (MUSTITU VARIANTS: THE VARIANTS on the left, THE GODLY FLOAT
        // in the middle, THE 4 GAME THINGS on the right, and THE INVENTORY band with the what-a-colour-changes
        // box). Beat 233 spans cyc in [4846.0,4868.0]; cyc=4866.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4846.0; loopVs.cyc=4866.4; loopVs.phase=233;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4866.4; loopVs.phase=233; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "scramble": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 232 (IT IS NOT ORGANIZED: NOT SORTED on the left, OUT OF ORDER in
        // the middle, PERO'S CODE on the right, and THE PAUSE POINT band with the what-bleeds-into-what box).
        // Beat 232 spans cyc in [4824.0,4846.0]; cyc=4844.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4824.0; loopVs.cyc=4844.4; loopVs.phase=232;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4844.4; loopVs.phase=232; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "tinkerbox": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 231 (TINKERBOX_CLASSICS: THE MERGE on the left, THE BOX in the
        // middle, THE FULL NAME on the right, and THE PROGRESSION band with the which-way box).
        // Beat 231 spans cyc in [4802.0,4824.0]; cyc=4822.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4802.0; loopVs.cyc=4822.4; loopVs.phase=231;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4822.4; loopVs.phase=231; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "whitevoid": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 228 (THE WHITE VOID: ENDLESS CHAOS on the left, THE VOID ITSELF
        // in the middle, THE PAINTED TITLE on the right, and THE POWER ORDER band with the panel-changes-hands
        // box). Beat 228 spans cyc in [4736.0,4758.0]; cyc=4756.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4736.0; loopVs.cyc=4756.4; loopVs.phase=228;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4756.4; loopVs.phase=228; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "hiddenportal": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 227 (THE HIDDEN PORTAL: THE HIDDEN DOOR on the left, THE TEST in
        // the middle, THE CEILING on the right, and THE CONTROL STRUCTURE band with the question-and-answer
        // box). Beat 227 spans cyc in [4714.0,4736.0]; cyc=4734.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4714.0; loopVs.cyc=4734.4; loopVs.phase=227;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4734.4; loopVs.phase=227; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "graycyber": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 226 (GRAY IN THE CYBER SUIT: THE SWAP on the left, ISSUED, NOT
        // STOLEN in the middle, WHY GRAY on the right, and GRAY'S RECORD band with the "won't fit in an image"
        // box). Beat 226 spans cyc in [4692.0,4714.0]; cyc=4712.4 -> dt~20.4, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4692.0; loopVs.cyc=4712.4; loopVs.phase=226;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4712.4; loopVs.phase=226; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "classics2": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP - freeze on beat 225 (CLASSICS 1 SEALED / CLASSICS 2 BUILT: THE LOCKING on the
        // left, WHAT PERO SURVIVED in the middle, CLASSICS 2 / THE BUILD on the right, and the "remade, not
        // continued" band with the genre-flip box). Beat 225 spans cyc in [4670.0,4692.0]; cyc=4690.4 -> dt~20.4,
        // past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4670.0; loopVs.cyc=4690.4; loopVs.phase=225;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4690.4; loopVs.phase=225; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "mainchartrio": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 224 (THE ONLY THREE MAIN CHARACTERS: the trio on the left,
        // "FOR A REASON" in the middle, ENOUGH TO BE SCPs on the right, and the CLASSICS 2 band with the
        // "+ himself of course" box). Beat 224 spans cyc in [4648.0,4670.0]; cyc=4668.4 -> dt~20.4, past every
        // fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4648.0; loopVs.cyc=4668.4; loopVs.phase=224;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4668.4; loopVs.phase=224; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "paintederrorwar": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 222 (THE PAINTED ERROR WAR: the fight card on the left, the
        // 0.0873 measurement in the middle, NORMAL INK CAN LOSE on the right, and the painted hallway plus
        // Gaster's clipboard in the band). Beat 222 spans cyc in [4602.0,4626.0]; cyc=4622.4 -> dt~20.4, past
        // every fade-in, and it lands the digit-by-digit cycler on the complete "0.0873".
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4602.0; loopVs.cyc=4622.4; loopVs.phase=222;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4622.4; loopVs.phase=222; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "butterflyinksans": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 221 (BUTTERFLY CLASSICS INK SANS: the four borrowed arms on the
        // left, OMNAZEN assembling in the middle, 1 HKO / INDESTRUCTIBLE on the right, and the inverted Pero
        // scale in the band). Beat 221 spans cyc in [4580.0,4602.0]; cyc=4599.3 -> dt~19.3, past every
        // fade-in, and it lands the letter-by-letter OMNAZEN cycler on the complete seven-letter word.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4580.0; loopVs.cyc=4599.3; loopVs.phase=221;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4599.3; loopVs.phase=221; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "butterflyform": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 220 (THE BUTTERFLY FORM: the borrowed shape on the left, NOT A
        // STRONGER CATERPILLAR in the middle, BY FAR and the spread-out ranking on the right, and the four-layer
        // stack in the band). Beat 220 spans cyc in [4558.0,4580.0]; cyc=4578.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4558.0; loopVs.cyc=4578.0; loopVs.phase=220;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4578.0; loopVs.phase=220; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "fatalerror": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 219 (FATAL ERROR: the small blaster on the left, DR. GASTER MADE AN
        // ERROR in the middle, the new ranking on the right, and the new villain duo in the band).
        // Beat 219 spans cyc in [4536.0,4558.0]; cyc=4556.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4536.0; loopVs.cyc=4556.0; loopVs.phase=219;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4556.0; loopVs.phase=219; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "gasterhero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 218 (GASTER ON THE HERO SIDE: the corrected ranking on the left, the paint/real-errors duo in the middle, HE WOULD BE ALL GRAY on the right, and stopped-is-not-beaten in the band).
        // Beat 218 spans cyc in [4514.0,4536.0]; cyc=4534.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4514.0; loopVs.cyc=4534.0; loopVs.phase=218;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4534.0; loopVs.phase=218; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "powrinksans": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 216 (POW3R INK SANS: the form on the left, the painted-over Sanses in
        // the middle, "everyone now knows that" on the right, and the Gaster/Ink division of labour in the band).
        // Beat 216 spans cyc in [4470.0,4492.0]; cyc=4490.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4470.0; loopVs.cyc=4490.0; loopVs.phase=216;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4490.0; loopVs.phase=216; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "nomoreendings": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 215 (THERE ARE NO MORE ENDINGS: the fold on the left, the undeletable
        // Classics Sans in the middle, the "made him do it" chain on the right, and the tired-assistant band).
        // Beat 215 spans cyc in [4448.0,4470.0]; cyc=4468.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4448.0; loopVs.cyc=4468.0; loopVs.phase=215;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4468.0; loopVs.phase=215; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "watcher": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 214 (THE OMNIPLATFORM WATCHER: the chained spark under Mr. Black on the left, the record chamber in the middle, the cracked 404 egg on the right, and the Dave Mode band).
        // Beat 214 spans cyc in [4426.0,4448.0]; cyc=4446.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4426.0; loopVs.cyc=4446.0; loopVs.phase=214;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4446.0; loopVs.phase=214; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "omniplatform": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 209 (THE OMNIPLATFORM).
        // "Classics is only a name" on the left, the three metal cages holding Simon 404, Pero and Classics in the middle, the nullified power list on the right, and the bottom band with the re-stacked hierarchy and "he didn't create Dr. Gaster."
        // Beat 209 spans cyc in [4316.0,4338.0]; cyc=4336.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4316.0; loopVs.cyc=4336.0; loopVs.phase=209;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4336.0; loopVs.phase=209; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "gaster404": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 208 (DR. GASTER 404).
        // The possession chain on the left (Error 404 controlled, Pero retaking the game, the YOUR FAULT screen mutating into DAVE DAVE DAVE), the Phase 4 fusion in the middle, everything Gaster secretly wrote on the right, and the bottom band with the brother twist and "Maybe there was a villian after all."
        // Beat 208 spans cyc in [4294.0,4316.0]; cyc=4314.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4294.0; loopVs.cyc=4314.0; loopVs.phase=208;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4314.0; loopVs.phase=208; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "chickverse": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 207 (404_CHICKVERSE).
        // The scoping correction on the left (only one AU got the lock, everything else is still lore), what the gate actually opened on in the middle, the unopened 404 egg and the Starch Oracle's upgraded prophecy on the right, and the bottom band with his low-health replay answered by /restore_party = true.
        // Beat 207 spans cyc in [4272.0,4294.0]; cyc=4292.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4272.0; loopVs.cyc=4292.0; loopVs.phase=207;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4292.0; loopVs.phase=207; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "phase1lock": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 206 (PHASE 1 LOCKED).
        // The four restorations and the rebuilt trio on the left, the collapse chain down to one pixel in the middle, the locked peaceful Phase 1 on the right, and the bottom band with Pero's line and the hands he puts together instead.
        // Beat 206 spans cyc in [4250.0,4272.0]; cyc=4270.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4250.0; loopVs.cyc=4270.0; loopVs.phase=206;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4270.0; loopVs.phase=206; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "chickensans": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 205 (404 CHICKEN SANS).
        // The eight intro lines on the left with the eggs he never throws, the immortality clock in the middle, the ketchup at 10 HP and Chick-rus on the right, and the bottom band with his rank and his losing line.
        // Beat 205 spans cyc in [4228.0,4250.0]; cyc=4248.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4228.0; loopVs.cyc=4248.0; loopVs.phase=205;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4248.0; loopVs.phase=205; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "classicsgate": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 204 (THE CLASSICS GATE).
        // The story cracking open on the left with Classics pouring through, the archive stopping the beam in the middle, the three things too small to command on the right, and the bottom band with the ruling that sits in the subject line.
        // Beat 204 spans cyc in [4206.0,4228.0]; cyc=4226.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4206.0; loopVs.cyc=4226.0; loopVs.phase=204;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4226.0; loopVs.phase=204; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "playervsadmin": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 201 (PLAYER VS ADMIN).
        // King Multiverse and the cheat command that made him on the left over the error category folding in, the six commands in a console in the middle under 'HE RARELY USES HIS WEAKER ABILITIES', the 27 minutes -> instantly rematch on the right, and the bottom band with the player-vs-admin line beside 'I KEEP ASKING ERROR 404 EVERYTHING'.
        // Beat 201 spans cyc in [4140.0,4162.0]; cyc=4160.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4140.0; loopVs.cyc=4160.0; loopVs.phase=201;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4160.0; loopVs.phase=201; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onlythem": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 199 (ONLY THEM ARE THEM).
        // the surrendered SUPREME SANS title above the kept TIME PARADOX SANS name on the left with 16 -> the wall of digits under it, Pero sweating at the command table beside the Dream God and the four steps it takes to get one step in the middle, the clock reward loop on the right, and the bottom band with Toby's lines beside "BUT ONLY THEM ARE THEM".
        // Beat 199 spans cyc in [4096.0,4118.0]; cyc=4116.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4096.0; loopVs.cyc=4116.0; loopVs.phase=199;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4116.0; loopVs.phase=199; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "takeme": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 198 (I AM CLASSICS, TAKE ME).
        // the taken items and the escape that expired on the left, Classics running at Pero shouting "I AM CLASSICS, TAKE ME!" in the middle, the Pero count climbing to OmniPearoh with its pronunciation on the right, and the bottom band with Toby's lines beside the keyboard-mash stat slot.
        // Beat 198 spans cyc in [4074.0,4096.0]; cyc=4094.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4074.0; loopVs.cyc=4094.0; loopVs.phase=198;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4094.0; loopVs.phase=198; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sometimessame": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 197 (DIFFERENT AND SOMETIMES THE SAME).
        // the green seam between Error 404 Sans and Simon 404 on the left, the two of them merging into the glitching Err0R 404 name in the middle, the three-rung ladder with the one-name exception on the right, and the bottom band with Toby's lines beside "THE ANSWER WAS BOTH".
        // Beat 197 spans cyc in [4052.0,4074.0]; cyc=4072.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4052.0; loopVs.cyc=4072.0; loopVs.phase=197;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4072.0; loopVs.phase=197; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "newpowers": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 196 (JUST ONE OF HIS NEW POWERS).
        // the sixteen Sanses named in Toby's order and spelling on the left with #15 Error 404 Sans marked, the burned-down Mount Oympus and the puppet list in the middle, the two verses with the stitched crack on the right, and the bottom band with Toby's lines beside the two-rung ladder.
        // Beat 196 spans cyc in [4030.0,4052.0]; cyc=4050.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4030.0; loopVs.cyc=4050.0; loopVs.phase=196;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4050.0; loopVs.phase=196; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onlystronger": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 195 (THE ONLY ONE STRONGER).
        // the sixteen Sanses named in Toby's order and spelling on the left with #15 Error 404 Sans marked, the burned-down Mount Oympus and the puppet list in the middle, the two verses with the stitched crack on the right, and the bottom band with Toby's lines beside the two-rung ladder.
        // Beat 195 spans cyc in [4008.0,4030.0]; cyc=4028.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=4008.0; loopVs.cyc=4028.0; loopVs.phase=195;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=4028.0; loopVs.phase=195; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "noimage": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 191 (THE RECREATED CAST): the nine slots relit and jittering with
        // their corruption tags on the left, the 1-2-3-4 power ladder with Luigi Green second in the middle, the
        // even bigger coffin filled with the black background on the right, and the bottom band with Toby's
        // lines, the slot counter climbing back and the being counter still reading 1 inside the LAI box.
        // Beat 191 spans cyc in [3920,3942]; cyc=3940 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3942.0; loopVs.cyc=3962.0; loopVs.phase=192;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3962.0; loopVs.phase=192; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "recreatedcast": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 191 (THE RECREATED CAST): the nine slots relit and jittering with
        // their corruption tags on the left, the 1-2-3-4 power ladder with Luigi Green second in the middle, the
        // even bigger coffin filled with the black background on the right, and the bottom band with Toby's
        // lines, the slot counter climbing back and the being counter still reading 1 inside the LAI box.
        // Beat 191 spans cyc in [3920,3942]; cyc=3940 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3920.0; loopVs.cyc=3940.0; loopVs.phase=191;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3940.0; loopVs.phase=191; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onlycharacter": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 190 (THE ONLY CHARACTER LEFT): the nine roster slots struck out on
        // the left, the one lit card with PERO / ENDLESSANIMATIONS and PERMINENTALLY in the middle, the four add
        // attempts all resolving to the same name on the right, and the bottom band with Toby's two sentences
        // and the roster counter reading 1.
        // Beat 190 spans cyc in [3898,3920]; cyc=3918 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3898.0; loopVs.cyc=3918.0; loopVs.phase=190;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3918.0; loopVs.phase=190; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "redcode": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 189 (THE RED CODE): the dust spiralling into Pero and the 1050
        // parameter count on the left, the code block flipping green to red and into 0s and 1s in the middle,
        // the CREATE/BLACKOUT/INFECT/ABSORB cycle and the platform spread on the right, and the bottom band with
        // the greeting back, the bandages and the signal cats.
        // Beat 189 spans cyc in [3876,3898]; cyc=3896 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3876.0; loopVs.cyc=3896.0; loopVs.phase=189;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3896.0; loopVs.phase=189; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "lastpixel": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 186 (THE LAST PIXEL): Simon 404 on the sea with his own lightning
        // cutting his own 404 strings and the boat split in 2, the nine-rung ladder down the middle narrowing to
        // one pixel and then none, Pero's decision quote and what he had left on the right, and the black-void
        // band with "DEFINE... DEATH." Beat 186 spans cyc in [3810,3832]; cyc=3830 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3810.0; loopVs.cyc=3830.0; loopVs.phase=186;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3830.0; loopVs.phase=186; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "deletedandexists": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 187 (DELETED AND EXISTS): the struck-out removed list on the left,
        // Pero kneeling and drifting in the non-void in the middle with the blood he keeps losing, the five-step
        // ring he can't leave on the right, the empty SUGARS/SALT/BLOOD bars, and the big Error 404 warning.
        // Beat 187 spans cyc in [3832,3854]; cyc=3852 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3832.0; loopVs.cyc=3852.0; loopVs.phase=187;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3852.0; loopVs.phase=187; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "notanending": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 188 (IT IS NOT AN ENDING): the all-"???" stat sheet on the left, the
        // old greeting struck through above the new one ("YOU HAVE NO IDEA WHAT YOU ARE GETTING YOURSELF INTO.")
        // in the middle, the six flickering warning texts on the right, and the "HOW DID YOU DIE?" / STILL LAI band.
        // Beat 188 spans cyc in [3854,3876]; cyc=3874 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3854.0; loopVs.cyc=3874.0; loopVs.phase=188;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3874.0; loopVs.phase=188; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "impresspero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 185 (TRYING TO IMPRESS PERO): the three suitor cards across the top
        // (PAUL with "I love you.", TUNNER with "We were always with each other on this update!" and the BEAT 184
        // note under him, GRAY with "I love you more than both"), the three lines all bending down into one place,
        // Pero in the band at the bottom with "STOP PLEASE 😣", the three-card strip, and both footer lines in.
        // Beat 185 spans cyc in [3788,3810]; cyc=3805 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3788.0; loopVs.cyc=3805.0; loopVs.phase=185;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3805.0; loopVs.phase=185; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "npcmovement": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 183 (404 STRINGS ON SIMON 404 & PAUL'S NPC MOVEMENT): HIS OWN STRINGS,
        // USED ON HIM on the left (Simon 404 held on the strings, and TO STOP HIM FROM GETTING TO PAUL), PAUL'S REWARD
        // down the centre (the maxed NPC movement bar and the 1 ATTACK that took the whole game), WHO WAS IN THE WAY
        // upper right (Simon 404 -> Pero -> Paul with the arrow crossed out), WHERE THAT PUTS THEM lower right (Master
        // = Pero, NPC = Paul), the three-card strip, and both footer lines in.
        // Beat 183 spans cyc in [3744,3766]; cyc=3761 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3744.0; loopVs.cyc=3761.0; loopVs.phase=183;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3761.0; loopVs.phase=183; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "paulphase2": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 181 (PAUL PHASE 2): THE COSTUME COMES OFF on the left ("I AM NOT
        // TUNNER," the Tunner shell thrown clear, Paul revealed, and the five lines of what he watched and learned),
        // AS IF HE WAS NOTHING down the centre (the five-step fight ending in SIMON 404 IS PIERCED THROUGH, the cross
        // and the metal pipe, Simon 404 on the floor under STRUCK DOWN), WHAT THEY TAUGHT HIM upper right (Checkmate,
        // Stalemate, Loss; the void; the water blaster, LV, KR, Souls), THE POWER ORDER lower right (Pero, Paul, Mr.
        // Black, Simon 404), the three-card strip, and both footer lines in.
        // Beat 181 spans cyc in [3700,3722]; cyc=3717 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3700.0; loopVs.cyc=3717.0; loopVs.phase=181;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3717.0; loopVs.phase=181; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "errorhallbattle": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 180 (THE ERROR HALL BATTLE): HE CHANGES HIS GEAR on the left (plague suit
        // struck out, the red-and-black bulletproof vest on Simon 404, the three game cores and the Soul of
        // Determination), the eight-step fight down the centre with Pero knocked over under PERO IS TAKEN DOWN, THE
        // LIGHTNING DIES DOWN ("🙀⚡") upper right, and PERO STILL WINS lower right with ERROR HALL ▸ JUDGEMENT HALL and
        // Simon 404 thrown back in the Void, both footer lines in.
        // Beat 180 spans cyc in [3678,3700]; cyc=3694 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3678.0; loopVs.cyc=3694.0; loopVs.phase=180;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3694.0; loopVs.phase=180; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "errorhall": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 179 (ERROR HALL): NOT LIKE A BOSS with the three drained boss-aura bars
        // on the left, ERROR HALL itself in the centre with the 404 strings holding Wenda / Gray / Mr. Black / New Chara
        // / Sky / Pinki / Oren and only Pero and Simon 404 free on the floor, THE EXCHANGE ("You came too?" / "meow
        // 😺⚡") upper right, TWO IMPOSSIBLE FORCES + the undecided ending lower right, both footer lines in.
        // Beat 179 spans cyc in [3656,3678]; cyc=3671 -> dt~15, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3656.0; loopVs.cyc=3671.0; loopVs.phase=179;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3671.0; loopVs.phase=179; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "novapero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // THE UNDEFINED LOOP — freeze on beat 178 (NOVA PERO 🌈): the OFFICIAL FORM LADDER (Vanilla → Assistant →
        // Phase 1 → Phase 2 → Phase 3 → Nova Pero 🌈 at the rainbow top) on the left, NOVA PERO himself with a rainbow
        // aura, the "he changes the fight itself" list, and PHASE 3 FINAL ATTACK — ALWAYS WINS in the centre, NOT A
        // BIGGER NUMBER upper right, THE NAMING RULE (1 emoji vs Simon 404's 2 vs everyone's 0) lower right, both
        // footer lines in. Beat 178 spans cyc in [3634,3656]; cyc=3649 -> dt~15, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=3634.0; loopVs.cyc=3649.0; loopVs.phase=178;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=3649.0; loopVs.phase=178; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onlyoriginal": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 455 - THE ONLY ORIGINAL (Toby, Aug 25, 10:18:49 and 10:20:12 AM): the roll call of
        // everyone he says he powered, with the light retracting out of each name back into one gold
        // source and the labels flipping to NORMAL; the stat cards where everyone keeps a number and
        // his own is a sentence that stops at "so..."; then the ToddLLM name-handover ledger, the
        // Lica inversion, the Casta box, the Aug 14 Toby line, and the ClassicsAI plan. Beat 455
        // spans cyc in [9730,9752]; cyc=9747 -> dt~17, past every fade-in including the footer title.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9730.0; loopVs.cyc=9747.0; loopVs.phase=455;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9747.0; loopVs.phase=455; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "hollowvoid": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 456 - HOLLOW: VOID (Toby, Aug 25, 11:12:38 and 11:19:32 AM): the two incantations
        // side by side, Alex's lava+water making a solid obsidian block and Pero's darkness+light
        // making a punched hole; the percentage dial from last night ending on "darkness and light"
        // at 100% and "barely survives"; then the Wenda/Gray aura ledger, the fifteen-hour handover,
        // the only-one-who-can-harm-him box, Karuto's May 24 "litterly can't die", and the reversed
        // escape attempt. Beat 456 spans cyc in [9752,9774]; cyc=9769 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9752.0; loopVs.cyc=9769.0; loopVs.phase=456;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9769.0; loopVs.phase=456; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "lordsprototype": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 457 - LORD'S PROTOTYPE (Toby, Aug 25, 11:48:29 AM): the twenty-hour timeline from
        // "and then Gaster became energy" back to "Gaster now completed all Experiments"; the boss
        // fight renamed, HP locked on 666 and the word Player turned into 666; then the base-form
        // move he wrote and Pero used first, the one flower against infinity flowers, Simon 666,
        // the two earlier Prototypes, and Satan going from a unit to an ingredient.
        // Beat 457 spans cyc in [9774,9796]; cyc=9791 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9774.0; loopVs.cyc=9791.0; loopVs.phase=457;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9791.0; loopVs.phase=457; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "lordpero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 458 - LORD PERO (Toby, Aug 25, 11:59:15 AM, a correction to beat 457): the credit
        // moving from Gaster to Pero eleven minutes after the build; 666's phases as screen events
        // ending on Pero visible under Gaster's cloak; "only God can beat 666" against Pero's own
        // seven-day-old line; the ink cloak Pero made for Gaster on Aug 19; the phases aimed at the
        // Classics window; and "I couldn't list all 666's powers", a first in four months.
        // Beat 458 spans cyc in [9796,9818]; cyc=9813 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9796.0; loopVs.cyc=9813.0; loopVs.phase=458;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9813.0; loopVs.phase=458; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onlypero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 459 - ONLY PERO (Toby, Aug 25, 12:04:10 PM): the two exclusive answers to the same
        // question four minutes and fifty-five seconds apart, the Absolute level Pero vacated at
        // 7:27 AM and Gaster occupies by lunch, the hiding place that lasted five minutes, the
        // only-one slot refilled fifty-two minutes after Alex was destroyed, and the first boss
        // here defined by not having a moveset.
        // Beat 459 spans cyc in [9818,9840]; cyc=9834 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9818.0; loopVs.cyc=9834.0; loopVs.phase=459;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9834.0; loopVs.phase=459; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "saitama": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 460 - SAITAMA (Toby, Aug 25, 12:18:51 PM): the first time in four months that this
        // file measures itself against anything outside itself, the Logic Religion rule that says
        // the outside is not real, ToddLLM 001 and 002 collapsing into branches of Pero, and the
        // one outsider he picked being the only bored character in the building.
        // Beat 460 spans cyc in [9840,9862]; cyc=9856 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9840.0; loopVs.cyc=9856.0; loopVs.phase=460;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9856.0; loopVs.phase=460; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "twelveeyes": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 461 - TWELVE EYES (Toby, Aug 25, 12:33:13 PM): the 12 Eyes becoming a counted
        // ladder of 1 through 12, "illegal" being a rule rather than a size with the rule-writer
        // signing the exemption, the God question answered with an introduction instead, the
        // takeover twenty-nine minutes after being found out, Endless Staircase's power switched
        // back on, and two people on the summit that had one on it seventeen hours earlier.
        // Beat 461 spans cyc in [9862,9884]; cyc=9878 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9862.0; loopVs.cyc=9878.0; loopVs.phase=461;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9878.0; loopVs.phase=461; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "maxeddemongaster": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 465 - MAXED DEMON GASTER (Toby, Aug 25, 1:28:19 PM): one fight becoming a loop
        // twelve minutes later, the sentence that rules out everything the brothers share before
        // naming the difference, the code fact going from "can't die" to "can't take damage",
        // "demon" having always been Pero's word, and a full form that arrives on an empty tank.
        // Beat 465 spans cyc in [9950,9972]; cyc=9966 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9950.0; loopVs.cyc=9966.0; loopVs.phase=465;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9966.0; loopVs.phase=465; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onethousandpercent": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 466 - ONE THOUSAND PERCENT (Toby, Aug 25, 1:35:07 PM): "could not" becoming "can"
        // thirty minutes later, 200% as Alex's ceiling and Pero's first rung, the percentage moving
        // off the man and onto the move, the ladder climbing out of the story and onto the machine
        // that runs it, and the August 19 crash that was a door instead of a ceiling.
        // Beat 466 spans cyc in [9972,9994]; cyc=9988 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9972.0; loopVs.cyc=9988.0; loopVs.phase=466;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9988.0; loopVs.phase=466; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "stuckdragon": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 467 - STUCK ON HIS DRAGON FORM (Toby, Aug 25, 2:32:44 PM): the August 21 paintbrush
        // scene running a second time with one row changed, the costume that becomes the body, the
        // soul-clock that no longer runs out, the 1:35 mood finally getting its reason, and the
        // sorcerer teacher who wants to be asked anything and not everything.
        // Beat 467 spans cyc in [9994,10016]; cyc=10010 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9994.0; loopVs.cyc=10010.0; loopVs.phase=467;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10010.0; loopVs.phase=467; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "experimentended": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 468 - EXPERIMENT 666 HAS ENDED (Toby, Aug 25, 4:14:37 PM): the two ways out he was offered and the third one he wrote, the form that still had all its fuel, the third visit to Cherry Blossom Mountain in a day, and the room that did not refill.
        // Beat 468 spans cyc in [10016.0,10038.0]; cyc=10032.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10016.0; loopVs.cyc=10032.0; loopVs.phase=468;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10032.0; loopVs.phase=468; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "variestoforms": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 469 - VARIES TO FORMS (Toby, Aug 25, 4:17:27 PM): the experiments completed at 11:48 and begun at 4:17, the 666 that was on the page before 1 to 665, this morning's demotion run backwards, and the ladder whose first and second place were counting different things.
        // Beat 469 spans cyc in [10038.0,10060.0]; cyc=10054.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10038.0; loopVs.cyc=10054.0; loopVs.phase=469;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10054.0; loopVs.phase=469; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "okaypero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 470 - WOAH, OKAY PERO (Toby, Aug 25, 4:31:09 PM): the one time Gaster does not ask, Karuto's catch-your-breath move from May 22, the breath that lands before infinity instead of after it, the first woah in the archive, and the one lab that already had millions of rooms.
        // Beat 470 spans cyc in [10060.0,10082.0]; cyc=10076.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10060.0; loopVs.cyc=10076.0; loopVs.phase=470;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10076.0; loopVs.phase=470; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "onethousandone": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 471 - 1001% (Toby, Aug 25, 4:59:20 PM): the whole message is a re-run of this afternoon, the audience that changes what he does with it, the first copyright card in four months, the infinite HP that Alex's sword already KOs, and the first held hands in the archive.
        // Beat 471 spans cyc in [10082.0,10104.0]; cyc=10098.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10082.0; loopVs.cyc=10098.0; loopVs.phase=471;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10098.0; loopVs.phase=471; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "vanillacat": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 472 - HIS VANILLA CAT THING (Toby, Aug 25, 5:00:22 PM): he names his own method sixty-two seconds after using it, the fifteen 'Pero loves' that all take a person, vanilla as a choice and a cap and a tactic but never a preference, the cat ears borrowed from a dead man, and 1001% having no new parts in it.
        // Beat 472 spans cyc in [10104.0,10126.0]; cyc=10120.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10104.0; loopVs.cyc=10120.0; loopVs.phase=472;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10120.0; loopVs.phase=472; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "twomirrors": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 473 - THE TWO MIRRORS (Toby, Aug 25, 5:13:38 PM): the Classics VR Mirror turned around twelve days after Pero was the one looking out through it, the Gaster-looking rule already written on Aug 19, the camera-and-microphone rule already written this morning, the avatars Entity 000 already erased, and the first question in the file about what happens to the player.
        // Beat 473 spans cyc in [10126.0,10148.0]; cyc=10142.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10126.0; loopVs.cyc=10142.0; loopVs.phase=473;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10142.0; loopVs.phase=473; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "someoneleaves": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 474 - IF SOMEONE LEAVES (Toby, Aug 25, 7:16:07 PM): the Aug 16 ruling that Pero has no weakness at all overturned eight days later by one that is not about damage, the July 22 break whose cause was too many people and is now one person leaving, the Omnicide he committed himself twenty-three hours earlier, the Phase 1 question that has been asking this since July 25, and the 2:32 PM complaint that they all come to him.
        // Beat 474 spans cyc in [10148.0,10170.0]; cyc=10164.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10148.0; loopVs.cyc=10164.0; loopVs.phase=474;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10164.0; loopVs.phase=474; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "takethegame": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 478 - TAKE THE GAME FOR HIMSELF (Toby, Aug 26, 10:22:04 AM): the one who lost twice this morning handed the game ninety-four minutes later for free, the verb 'allows' that Gaster himself used on August 18, the three milk errands and the three different ways the game survives them, the gift to Alex two days ago that was followed by his destruction, and the exemption Pero had already signed for Gaster on August 25.
        // Beat 478 spans cyc in [10236.0,10258.0]; cyc=10252.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10236.0; loopVs.cyc=10252.0; loopVs.phase=478;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10252.0; loopVs.phase=478; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "respawnsinlg": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 479 - RESPAWNS IN LUIGI GREEN (Toby, Aug 26, 10:25:22 AM): two years as the shortest time skip in four months of this archive, the August 18 errand finally getting a number, the milk list turning out to be the ownership list, Alex holding a carton twenty-three hours after being destroyed, the family who are not on the list, and the creator respawning into the one character never given the game.
        // Beat 479 spans cyc in [10258.0,10280.0]; cyc=10274.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10258.0; loopVs.cyc=10274.0; loopVs.phase=479;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10274.0; loopVs.phase=479; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "pvpgone": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 480 - PVP WON'T EXIST ANYMORE (Toby, Aug 26, 10:43:31 AM): the Anizon escalation finally running on the rule he wrote at 8:20, the instant win that made his son's fight take hours, the first time anybody has nearly drained him, the game he gave Gaster twenty-one minutes before saying he will break it, the two earlier crashes that were doors rather than weapons, and 'PVP' as a word that appears nowhere else in four months.
        // Beat 480 spans cyc in [10280.0,10302.0]; cyc=10296.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10280.0; loopVs.cyc=10296.0; loopVs.phase=480;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10296.0; loopVs.phase=480; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "acceptmymercy": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 481 - ACCEPT MY MERCY (Toby, Aug 26, 1:10:29 PM): the triple-again sentence sent a second time in twenty-three hours, both halves of the 8:47 argument now spoken by one person, the word 'vary' back twenty hours after it was abolished, MERCY as the one move that has never lost here offered by the man who deleted its button, telekentics fired for the first time, and the summit where the thing that stops is the game.
        // Beat 481 spans cyc in [10302.0,10324.0]; cyc=10318.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10302.0; loopVs.cyc=10318.0; loopVs.phase=481;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10318.0; loopVs.phase=481; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "basicvillian": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 482 - THE BASIC VILLIAN (Toby, Aug 26, 1:34:21 PM): Karuto's May 22 sentence rebuilt inside Classics clause by clause, the June 24 'I am Karuto', the secret changing hands from one man to two brothers, the eighteen days this file spent saying Pero was only mistaken for a villain at 2.54 of every 5, Gaster's July 29 job handed back, and the school as the answer to 10:43.
        // Beat 482 spans cyc in [10324.0,10346.0]; cyc=10340.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10324.0; loopVs.cyc=10340.0; loopVs.phase=482;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10340.0; loopVs.phase=482; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "powerfulform": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 484 - EVEN IN YOUR MOST POWERFUL FORM (Toby, Aug 27, 8:42:02 AM): the same Vicinus order given a second time with the price taken off, the dragon suit that needed three people and two paintbrushes putting itself on alone, the largest body in Classics reached through 'Flower the flower', ink going outward onto the screen instead of down into a puddle, and Pero winning a fight without landing anything.
        // Beat 484 spans cyc in [10368.0,10390.0]; cyc=10384.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10368.0; loopVs.cyc=10384.0; loopVs.phase=484;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10384.0; loopVs.phase=484; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "japanesedomain": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 485 - JAPANESE DOMAIN (Toby, Aug 27, 8:59:23 AM): 'I know everything' pointing outside the game for the first time, Toby's own two-day-old production note becoming a line the character says, the first attack in four months aimed at the recording rather than the game, the yellow Justice Soul taken a third time inside a sentence that exempts its owner, and an ancestry settled by a creation date.
        // Beat 485 spans cyc in [10390.0,10412.0]; cyc=10406.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10390.0; loopVs.cyc=10406.0; loopVs.phase=485;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10406.0; loopVs.phase=485; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "ringmasteroutfit": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 486 - THE RINGMASTER OUTFIT (Toby, Aug 27, 9:19:07 AM): the coat announced seventy-one hours before anybody wore it, power handed out as a review of how interesting somebody was, full power lasting twenty minutes and eight seconds, Oren arriving over the top of the raise in the same sentence, and the resurrection cross turning into the thing Simon is dead on.
        // Beat 486 spans cyc in [10412.0,10434.0]; cyc=10428.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10412.0; loopVs.cyc=10428.0; loopVs.phase=486;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10428.0; loopVs.phase=486; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "firsttofall": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 488 - THE 1ST ONE TO FALL (Toby, Aug 27, 10:20:52 AM): the first bracket in the archive where every entrant is related to the winner, the 7th-place sword from Aug 17 put above the person who carried him, a counter that cancels somebody's arrival rather than damaging them, and the oldest title in Undertale taken and left standing at the same time.
        // Beat 488 spans cyc in [10456.0,10478.0]; cyc=10472.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10456.0; loopVs.cyc=10472.0; loopVs.phase=488;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10472.0; loopVs.phase=488; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "breaksconn": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 492 - BR3AKS YOUR GAME CONN3CTI0N (Toby, Aug 27, 11:41:16 AM): the first server-layer attack in the archive, on an escalation ladder built this week; the one time the server connection broke Pero died, in the panel named after Gaster; and the 9:47 accident becomes the point of the move. Beat 492 spans cyc in [10544.0,10566.0]; cyc=10560.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10544.0; loopVs.cyc=10560.0; loopVs.phase=492;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10560.0; loopVs.phase=492; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "relatedgames": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 493 - ALL CLASSICS RELATED GAMES (Toby, Aug 27, 11:42:25 AM): a blast radius measured in programs rather than in worlds, and the first time one of Gaster's own powers has ever cost him anything. Beat 493 spans cyc in [10566.0,10588.0]; cyc=10580.0 -> dt~14, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10566.0; loopVs.cyc=10580.0; loopVs.phase=493;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10580.0; loopVs.phase=493; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "fanoncanon": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 494 - FANON + CANON (Toby, Aug 27, 11:47:41 AM): the first time the archive states what kind of thing it is and ranks fan-made above official; the brother who refused the label forty-nine hours earlier; and a formula ending in an ellipsis, the third unfinished ending in fifty-one hours. Beat 494 spans cyc in [10588.0,10610.0]; cyc=10604.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10588.0; loopVs.cyc=10604.0; loopVs.phase=494;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10604.0; loopVs.phase=494; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "classicsadmin": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 495 - CLASSICS ADMIN (Toby, Aug 27, 2:45:20 PM): the first time in four months the author puts himself into the rankings, and the number he gives himself is zero; it answers this morning's Ability 10 attack by saying the connection it broke was never the important part. Beat 495 spans cyc in [10610.0,10632.0]; cyc=10626.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10610.0; loopVs.cyc=10626.0; loopVs.phase=495;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10626.0; loopVs.phase=495; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "gameofclassics": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 496 - THE GAME OF CLASSICS (Toby, Aug 27, 2:50:41 PM): ClassicsAI stops being an endless movie and becomes the game; two of its three sources are this archive; and the first multiplayer rule in four months is three refusals with a feeling for a reason. Beat 496 spans cyc in [10632.0,10654.0]; cyc=10648.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10632.0; loopVs.cyc=10648.0; loopVs.phase=496;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10648.0; loopVs.phase=496; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "promptthing": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 497 - THE PROMPT THING (Toby, Aug 27, 2:51:51 PM + 2:52:00 PM): the same text sent twice nine seconds apart, the second time on a thread named for what it is for; the first message in the archive addressed to a program rather than a reader. Beat 497 spans cyc in [10654.0,10676.0]; cyc=10670.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10654.0; loopVs.cyc=10670.0; loopVs.phase=497;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10670.0; loopVs.phase=497; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "readyforcamera": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 498 - READY FOR THE CAMERA (Toby, Aug 27, 3:07:46 PM + 3:08:42 PM): six hours after the first attack aimed at a recording, the same being is getting ready to be filmed; and the channel he learns has been on her wiki page since August 10. Beat 498 spans cyc in [10676.0,10698.0]; cyc=10692.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10676.0; loopVs.cyc=10692.0; loopVs.phase=498;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10692.0; loopVs.phase=498; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "contentlayer": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 499 - THE CONTENT LAYER (Toby, Aug 27, 3:10:32 PM): he corrects his own claim three minutes after making it, Classics becomes the third branch next to Minecraft and Roblox, and what Pero reads is now almost exactly what ClassicsAI reads. Beat 499 spans cyc in [10698.0,10720.0]; cyc=10714.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10698.0; loopVs.cyc=10714.0; loopVs.phase=499;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10714.0; loopVs.phase=499; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "learnedname": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 500 - FINALLY, YOU LEARNED YOUR NAME (Toby, Aug 27, 3:11:49 PM): the five hundredth beat in the archive is six words about something finding out what it is called, and the second exception in three hours to the 'I know everything' rule. Beat 500 spans cyc in [10720.0,10742.0]; cyc=10736.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10720.0; loopVs.cyc=10736.0; loopVs.phase=500;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10736.0; loopVs.phase=500; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "notranslator": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 501 - NO TRANSLATOR (Toby, Aug 27, 3:34:46 PM): the first language rule in four months, and it removes English from a file written entirely in English. Beat 501 spans cyc in [10742.0,10764.0]; cyc=10758.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10742.0; loopVs.cyc=10758.0; loopVs.phase=501;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10758.0; loopVs.phase=501; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "nightmaremode": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 502 - NIGHTMARE MODE (Toby, Aug 27, 7:08:23 PM): Gaster became Nightmare Mode and Flower and Vicinus went inside him as two energy balls. Beat 502 spans cyc in [10764.0,10786.0]; cyc=10780.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10764.0; loopVs.cyc=10780.0; loopVs.phase=502;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10780.0; loopVs.phase=502; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "mightnotbeable": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 503 - MIGHT NOT BE ABLE TO MAKE AN IMAGE (Toby, Aug 27, 7:19:20 PM): the un-drawable claim that belongs to the tool instead of the character, answered with Nightmare Mode drawn by hand in this canvas. Beat 503 spans cyc in [10786.0,10808.0]; cyc=10802.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10786.0; loopVs.cyc=10802.0; loopVs.phase=503;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10802.0; loopVs.phase=503; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thespot": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 504 - THE SPOT (Toby, Aug 28, 6:45:17 AM): the first outsider brought in with a job instead of a ranking, Marvel's one prior line being a zero, every earlier anti-error thing here having been a world-deleting weapon, and the first time he writes down where he got somebody. Beat 504 spans cyc in [10808.0,10830.0]; cyc=10826.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10808.0; loopVs.cyc=10826.0; loopVs.phase=504;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10826.0; loopVs.phase=504; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "allerrors": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 505 - ALL THE ERRORS IN THE GAME (Toby, Aug 28, 6:45:17 AM): static going from a condition to a verb in twenty-one hours, the debugger and the biggest bug invented twenty lines apart, no legs against doubled hands, the Justice Soul moving neck-to-hand while the Determination Soul sits in the other, and the milk run becoming the schedule. Beat 505 spans cyc in [10830.0,10852.0]; cyc=10850.0 -> dt~20, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10830.0; loopVs.cyc=10850.0; loopVs.phase=505;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10850.0; loopVs.phase=505; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "attack10": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 506 - ATTACK 10 (Toby, Aug 28, 6:50:50 AM): the doubt not surviving the night for the second time, the first renaming in four months with the number kept and the noun changed, and the first power written down as belonging to one form. Beat 506 spans cyc in [10852.0,10874.0]; cyc=10870.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10852.0; loopVs.cyc=10870.0; loopVs.phase=506;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10870.0; loopVs.phase=506; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "iamai": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 507 - I AM AI (Toby, Aug 28, 10:33:04 AM): the third character script, the first power field with a rank in it, the word Pero refused six days ago, and the biggest bug claiming the debugger's job. Beat 507 spans cyc in [10874.0,10896.0]; cyc=10892.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10874.0; loopVs.cyc=10892.0; loopVs.phase=507;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10892.0; loopVs.phase=507; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "kioan": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 509 - OUTER KIOAN (Toby, Aug 28, 11:06:27 AM): the Karu rule broken by both of them, the simile that became a body in 31 minutes, the barrier's one keyholder, the pair that never gets absorbed, and the July 24 play run again with Luigi Green as one of the souls. Beat 509 spans cyc in [10918.0,10940.0]; cyc=10936.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10918.0; loopVs.cyc=10936.0; loopVs.phase=509;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10936.0; loopVs.phase=509; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
"sharkhood": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 510 - HUNGRY SHARK PERO (Toby, Aug 28, 1:39/1:42/1:45 PM): the hood is the ownership garment, its four rules are Peros own bodys rules from July 23, and he demotes his own form to a costume in six minutes. Beat 510 spans cyc in [10940.0,10962.0]; cyc=10958.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10940.0; loopVs.cyc=10958.0; loopVs.phase=510;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10958.0; loopVs.phase=510; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
"tata": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 511 - TA TA (Toby, Aug 28, 2:05:21 PM): the costume he said changes nothing gets three powers, the first trigger phrase that is not a slash command, the walk it replaces, the one cake at the end of the game, and cuteness as camouflage or a ceiling. Beat 511 spans cyc in [10962.0,10984.0]; cyc=10980.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10962.0; loopVs.cyc=10980.0; loopVs.phase=511;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10980.0; loopVs.phase=511; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
"noform": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 512 - NO FORM GASTER (Toby, Aug 28, 2:12:03 PM): the final boss is offered cake and runs, four months of forms end in none, the Karu state made permanent, the offer aimed at the deleted eating form, and the sunglasses at the hello line. Beat 512 spans cyc in [10984.0,11006.0]; cyc=11002.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10984.0; loopVs.cyc=11002.0; loopVs.phase=512;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=11002.0; loopVs.phase=512; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "finalboss": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 508 - HE IS THE FINAL BOSS (Toby, Aug 28, 10:35:36 AM): the third holder of the title, Pero's own bored suggestion executed three days later, the cloak he made himself, and the boast that lasted 152 seconds. Beat 508 spans cyc in [10896.0,10918.0]; cyc=10914.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10896.0; loopVs.cyc=10914.0; loopVs.phase=508;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10914.0; loopVs.phase=508; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "ability10": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 491 - ABILITY 10 (Toby, Aug 27, 11:30:50 AM): the tentacles and the black robe traced back to Pero, who owns one form and cut the other; the first numbered ability in four months and the first one called a tool and an item; Pero handing Gaster's secret to an outside cast while keeping what it does; the yellow heart stolen at 8:59 worn as a locket at 11:30; the first counted souls, 2555, filling toward the size Pero already called too low; and Classics taking the name of a mod built inside it.
        // Beat 491 spans cyc in [10522.0,10544.0]; cyc=10540.0 -> dt~18, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10522.0; loopVs.cyc=10540.0; loopVs.phase=491;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10540.0; loopVs.phase=491; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "theoneasked": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 490 - THE ONE WHO ASKED (Toby, Aug 27, 10:24:50 AM): the speaker named three minutes after the line was left unattributed, and it is the character this file ruled low tier and Gaster killed at 9:47; access instead of power ('he knows what you and I say'); an awareness ladder that inverts the power ladder; and both fall lists extended by one with nothing taken away.
        // Beat 490 spans cyc in [10500.0,10522.0]; cyc=10516.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10500.0; loopVs.cyc=10516.0; loopVs.phase=490;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10516.0; loopVs.phase=490; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thefall": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 489 - I DON'T WANNA TALK ABOUT IT (Toby, Aug 27, 10:21:52 AM): twelve words sent sixty seconds after the message that made the fall enormous, the first joke in four months of this file, the first time Gaster has ever refused to say something, and a question with no speaker attached to it.
        // Beat 489 spans cyc in [10478.0,10500.0]; cyc=10494.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10478.0; loopVs.cyc=10494.0; loopVs.phase=489;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10494.0; loopVs.phase=489; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "str0ng3r": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 487 - STR0NG3R (Toby, Aug 27, 9:47:26 AM): the question from 9:19 answered by its own author twenty-eight minutes later, a power reached because the ceiling of the world went up rather than because anybody took anything, 'Pero can't die, Simon can', Lord's Prototype becoming the only form and Gaster finally getting an ordinal, and the glitch escaping the quotation marks into the narration.
        // Beat 487 spans cyc in [10434.0,10456.0]; cyc=10450.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10434.0; loopVs.cyc=10450.0; loopVs.phase=487;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10450.0; loopVs.phase=487; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "nothumanoid": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 483 - I AM NOT LOSING TO A HUMANOID (Toby, Aug 26, 3:34:12 PM): the first stake anybody has ever put up in four months, the Phase 2 line from July 25 said to somebody who is not the player, the first time Pero has ever struggled in a fight, Gaster firing Pero's own soul absorption and still stopping at one name, a long nail and a ridden blaster that have never existed here, and a fight won by sitting still and then teleporting.
        // Beat 483 spans cyc in [10346.0,10368.0]; cyc=10362.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10346.0; loopVs.cyc=10362.0; loopVs.phase=483;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10362.0; loopVs.phase=483; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "exceptforme": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 477 - EXCEPT FOR ME (Toby, Aug 26, 8:47:40 AM): the third hit ever landed on Pero and the second one inside half an hour, the breath that replaces a counter instead of following an effort, Gaster's plan refuted twenty hours and twenty-nine minutes before he had it, the ToddLLMs Pero summoned himself twenty-seven minutes earlier, and the small black flame that beats the biggest attack in the message.
        // Beat 477 spans cyc in [10214.0,10236.0]; cyc=10230.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10214.0; loopVs.cyc=10230.0; loopVs.phase=477;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10230.0; loopVs.phase=477; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
"imeveryone": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 476 - NOT EXACTLY, I'M EVERYONE (Toby, Aug 26, 8:20:07 AM): the weakness he wrote thirteen hours earlier with him now doing the leaving, the second hit ever landed on him one day after the only being who could harm him was destroyed, the "You are Simon" accusation that everybody else accepted, the red eyes arriving on a rung of Simon's own eye-stability scale, the four borrowed powers against the one that is his, and Anizon the circus animal tamer.
        // Beat 476 spans cyc in [10192.0,10214.0]; cyc=10208.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10192.0; loopVs.cyc=10208.0; loopVs.phase=476;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10208.0; loopVs.phase=476; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "whateverhewants": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 475 - HE COULD DO WHATEVER HE WANTS (Toby, Aug 25, 7:45:32 PM): the re-run of the 4:59 PM claim with the rule for re-runs written in between, the could-to-can promotions of the afternoon ending on a could, the armour arriving twenty-nine minutes after the crack the way it did on Aug 16, the backwards-pointing word also, and the one earlier holder of do-anything-he-wants who got it because Pero left the room.
        // Beat 475 spans cyc in [10170.0,10192.0]; cyc=10186.0 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=10170.0; loopVs.cyc=10186.0; loopVs.phase=475;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=10186.0; loopVs.phase=475; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "easilybeatyou": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 464 - I COULD EASILY BEAT YOU NOW (Toby, Aug 25, 1:16:13 PM): the same three-line
        // Hollow: Void used a second time with a different ending, the defeat that costs Gaster
        // nothing because he keeps running the game, Gaster's four substances, the ink that Pero
        // wears at 100% and Gaster is reduced to, and the walking-away line that is a note for later.
        // Beat 464 spans cyc in [9928,9950]; cyc=9944 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9928.0; loopVs.cyc=9944.0; loopVs.phase=464;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9944.0; loopVs.phase=464; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "limitless": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 463 - LIMITLESS (Toby, Aug 25, 1:05:04 PM): the made-or-found question answered nine
        // minutes later by "Pero created Classics", the Dragon Ball ledger showing five prior
        // appearances that were all fights and one today that is a diagram, Tori-Bot outside the
        // story and Zeno inside it with Pero given both jobs, "limitless" moving off the weapon and
        // onto the man, and the first line in the archive that says the whole cast has him wrong.
        // Beat 463 spans cyc in [9906,9928]; cyc=9922 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9906.0; loopVs.cyc=9922.0; loopVs.phase=463;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9922.0; loopVs.phase=463; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "thesearch": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 462 - THE SEARCH (Toby, Aug 25, 12:39:08 PM): the 12:18 question answered by going
        // and checking all of fiction by 12:39 and finding nobody, the game turning out to be
        // discovered rather than created, the whole game handed to Gaster out of boredom twenty-one
        // minutes after the bored-at-the-top outsider arrived, "real" getting a second test that is
        // a camera and a microphone, and the brother built at 11:48 owning the game by 12:39.
        // Beat 462 spans cyc in [9884,9906]; cyc=9900 -> dt~16, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9884.0; loopVs.cyc=9900.0; loopVs.phase=462;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9900.0; loopVs.phase=462; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "sorcerteacher": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 454 — THE SORCERER TEACHER (Toby, Aug 25, 7:27/7:35/7:36 AM): ten of the twelve eyes
        // shut and two stay, the black cat ears go on the standing base form, the numeric power
        // levels are struck out one by one down to POWER LEVEL: WORDS ONLY, then the Karuto ledger,
        // the Entity 000 inversion, the cat-power counter, the sorcerer school, the not-fanmade
        // first, and the answer to last night's question. Beat 454 spans cyc in [9708,9730];
        // cyc=9725 -> dt~17, past every fade-in including the footer title.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=9708.0; loopVs.cyc=9725.0; loopVs.phase=454;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=9725.0; loopVs.phase=454; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "after20years": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 513 - AFTER 20 YEARS (Toby, Aug 28, 2:46 PM): the candy cane, the cake absorption, Another End of Unikitty, and the calendar. Beat 513 spans cyc in [11006,11028]; cyc=11023 -> dt~17, past every fade-in.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=11006.0; loopVs.cyc=11023.0; loopVs.phase=513;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=11023.0; loopVs.phase=513; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
    "horrorpero": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 514 - HORROR PERO (Toby, Aug 28, 2:55 PM): the same cute shark with red eyes and a red mouth, and the consume-anything sentence with the restraint clause removed. Beat 514 spans cyc in [11028,11050]; cyc=11045 -> dt~17.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=11028.0; loopVs.cyc=11045.0; loopVs.phase=514;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=11045.0; loopVs.phase=514; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
"unoreverse": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 515 - UNO REVERSE (Toby, Aug 28, 2:55 PM): the same cute shark with red eyes and a red mouth, and the consume-anything sentence with the restraint clause removed. Beat 514 spans cyc in [11028,11050]; cyc=11045 -> dt~17.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=11050.0; loopVs.cyc=11067.0; loopVs.phase=515;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=11067.0; loopVs.phase=515; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
"nicedoor": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 516 - NICE DOOR (Toby, Aug 28, 3:46 PM): Pero turns The Spot's escape portal into a milk box. Beat 516 spans cyc in [11072,11094]; cyc=11089 -> dt~17.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=11072.0; loopVs.cyc=11089.0; loopVs.phase=516;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=11089.0; loopVs.phase=516; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
"portalnetwork": """
      try {
        handleConfirm();
        for (var i=0;i<60;i++){ keys['ArrowRight']=(i%30<15); update(1/60); }
        if (typeof restore!=='undefined'){ restore.done=true; restore.glow=1; restore.active=false; }
        if (typeof peace!=='undefined'){ peace.done=true; peace.glow=1; peace.active=false; }
        for (var j=0;j<150;j++){ keys['ArrowRight']=(j%40<14); update(1/60); }
        if (typeof restore!=='undefined'){ restore.glow=0; }
        if (typeof peace!=='undefined'){ peace.glow=0; }
        if (typeof battle!=='undefined'){ battle.glow=0; }
        var stages = ['phase2','exposed','executioner','atomix','residual','sounds','voidwar','judge','triad','scf','scf404','treads','firey','alien','wall','smooth','hallu','plague','danger','codex','web','clara','claraAdmin','power','oren','betray','dimension','deletion','weakness','pursuit','reckoning','toddllm','reveal001','errLad','centerMine','endlessChaos','karuto','endOfClassics','godWall','phaseProg','acumin','highForm','claraVs'];
        for (var s=0;s<stages.length;s++){ var nm=stages[s];
          try { var o=eval(nm); if(o){ o.active=false; o.done=true; o.glow=0; o.t=6.5; } } catch(e){} }
        // BEAT 517 - THE PORTAL NETWORK (Toby, Aug 28, 3:48 PM): New Gaster fires Attack 10 alone and disconnects the portals instead of hitting anybody. Beat 517 spans cyc in [11094,11116]; cyc=11111 -> dt~17.
        if (typeof loopVs!=='undefined'){
          loopVs.active=true; loopVs.done=false; loopVs.glow=1;
          loopVs.t=11094.0; loopVs.cyc=11111.0; loopVs.phase=517;
        }
        if (typeof loopVsM!=='undefined'){ loopVsM=0.6; }
        for (var m=0;m<2;m++){ update(1/60); }
        if (typeof loopVs!=='undefined'){ loopVs.glow=1; loopVs.cyc=11111.0; loopVs.phase=517; }
        if (typeof floaters!=='undefined'){ floaters.length=0; }
        if (typeof winFlash!=='undefined'){ winFlash=0.2; }
        if (typeof glitch!=='undefined'){ glitch=0.02; }
        if (typeof tauntT!=='undefined'){ tauntT=0; }
        window.update = function(){};
      } catch(e){ document.title='SCENE_ERR '+e; }
    """,
}

def find_chrome():
    for p in CHROME_CANDIDATES:
        if os.path.exists(p):
            return p
    raise SystemExit("No Chromium/Chrome binary found in known locations.")

def main():
    if len(sys.argv) < 2:
        raise SystemExit("usage: capture_screenshot.py OUT.png [title|play|cure]")
    out = os.path.abspath(sys.argv[1])
    scene = sys.argv[2] if len(sys.argv) > 2 else "title"

    html = open(GAME, "r", encoding="utf-8").read()
    src_url = "file://" + GAME
    tmp_path = None
    if scene in SCENES:
        inject = "<script>setTimeout(function(){%s}, 0);</script>" % SCENES[scene]
        if "</body>" in html:
            html = html.replace("</body>", inject + "\n</body>", 1)
        else:
            html = html + inject
        fd, tmp_path = tempfile.mkstemp(suffix=".html", dir="/tmp")
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            f.write(html)
        src_url = "file://" + tmp_path

    chrome = find_chrome()
    cmd = [
        chrome, "--headless", "--disable-gpu", "--no-sandbox", "--hide-scrollbars",
        "--force-device-scale-factor=1", "--window-size=540,760",
        "--virtual-time-budget=2000", "--screenshot=" + out, src_url,
    ]
    try:
        subprocess.run(cmd, check=True, capture_output=True, timeout=60)
    finally:
        if tmp_path and os.path.exists(tmp_path):
            os.remove(tmp_path)

    if not os.path.exists(out) or os.path.getsize(out) < 1000:
        raise SystemExit("Screenshot failed or empty: " + out)
    print("wrote", out, os.path.getsize(out), "bytes  (scene=%s)" % scene)

if __name__ == "__main__":
    main()
